# M11 工作树排错记录

> 编码：UTF-8，简体中文。
>
> 状态：持续维护；本次整理覆盖 2026-07-28 22:53 进入 M11 专属工作树之后至 2026-08-05 的问题。
>
> 上位总表：[开发排错总文档](DevelopmentTroubleshooting.md) · 工作树规则：[多工作树协作集成规范](ABTSMultiWorktreeDevelopmentGuide.md)
>
> 相关设计：[M11-A 求解器](M11AGravityAssistSolverDesign.md) · [M11-B 布局与认证](M11BFinaleLayoutCertificationDesign.md) · [M11-B v2.1 候选搜索](M11B21CandidateSearchDesign.md) · [M11-C 交互与播放](M11CFinaleInteractionAndPlaybackDesign.md) · [M11 终局 HUD 优化](M11CFinaleLaunchHUDOptimizationDesign.md) · [M11 v2 优化总设计](M11V2FinaleOptimizationDesign.md) · [M3R-5.2/M11 预览接线](M3R52M11PreviewFinaleIntegrationDesign.md) · [M11 镜头导演](M11FinaleCameraDirectionDesign.md) · [M1 观测与离线判据](M11FinaleCameraObservationAndOfflineCriteria.md) · [M2 Assist1 双目标导演](M11FinaleCameraM2SingleAssistDirection.md) · [M11 连续飞行拖尾](M11FinaleBirdTrailPresentationDesign.md)

## 1. 用途、边界与更新规则

本文是 `feature/m11-finale` 工作树的增量排错账本，不替代共享的
[开发排错总文档](DevelopmentTroubleshooting.md)。M11 工作树只在本文记录；每次合并到集成工作树时，由集成工作树筛选、去重并整理进总文档。

记录规则：

- 每条必须保留“现象—根因—处理—防回归验证”，不能只写最后一次参数改动。
- `【已修复】` 表示代码或合同已经落地；`【设计已替代】` 表示历史故障仍值得保留，但当前交互合同已经改变；`【候选未认证】` 表示只足够做 PIE 比较；`【开放】` 表示仍需证据或修复。
- 候选布局的 PIE 手感、5000 点 ScreenAim 统计和 M11-B v2.2 完整认证是三种不同证据，禁止互相代替。
- 自动化若使用 `NullRHI`，只能证明数据、生命周期和绘制命令合同；SceneCapture、材质、光照、实际像素以及镜头构图仍以 fresh-process 可见 PIE 为最终证据。
- 后续 M11 会话发现新问题时，先更新对应条目；同一根因的复发补充证据，不重复新增近义条目。

## 2. 构建、线程与异步生命周期

| 现象 | 根因 | 处理 | 防回归验证 |
| --- | --- | --- | --- |
| `M11-BUILD-001` 【已修复流程】M1 增量编译首次没有进入编译动作，而是 `IOException`：无法访问 UnrealBuildTool 默认日志；重试即可通过。 | 另一工作树同时启动 UBT，多个进程争用引擎默认 UBT 日志路径。代码本身没有编译错误；多工作树只隔离项目产物，不会自动隔离未显式指定的引擎级默认日志。 | 本工作树所有 UBT 调用显式传入 `-Log=<M11 工作树>/Saved/Logs/<唯一运行名>-UBT.log`，并继续遵守重型任务串行；不得结束其他工作树的 UBT/UE 进程。 | M1 使用唯一日志完成增量编译和 `-ForceUnity -DisableAdaptiveUnity` 全链接；最终 ForceUnity 4 个 action、`Result: Succeeded`，日志 `M11-M1-ForceUnity-20260806-000224-UBT.log`。 |
| 【已修复】M7 工作树做默认 Development Editor Unity 全链接时，M11 的 `ABTSM11GravityAssistSolver.cpp` 报 `C2668: IsFiniteVector 调用不明确` | Unity Build 把多个 `.cpp` 合入同一翻译单元；`ABTSM11GravityAssist::IsFiniteVector` 与 `ABTSM11FinaleSystem.cpp` 匿名命名空间中的通用 `IsFiniteVector` 同时进入候选集。类似地，M11-C 两个 `.cpp` 各自的 `SameInput` 也会在强制 Unity 下重定义。单文件编译通过不能证明 Unity 安全。 | 将职责不唯一的帮助函数改为文件域唯一名称：`IsFiniteFinaleBoundaryVector`、`SameInteractionInput`、`SameSolvedInput`。修复起点为提交 `85e9088`，后续 Unity 门一并收口。 | 关闭本工作树 Editor，分别执行默认 Development Editor 和强制 Unity 全链接；不能只跑 non-unity 或增量编译。 |
| 【已修复】进入终局发射模式时偶发 `Ensure condition failed: IsInGameThread()`，堆栈落在 `FAppTime`、Renderer、RenderCore | 旧实现从普通 ThreadPool worker 直接 `AsyncTask(GameThread)` 发布结果。该 worker 不是由 Game Thread/TaskGraph 继承出来的任务，没有继承 `FAppTime` 上下文；从它派生的 SceneCapture/Renderer 工作沿错误上下文进入渲染线程。 | worker 只计算并返回纯数据 `TFuture`；由终局 Actor 的原生 Game Thread Tick 轮询、消费和发布，SceneCapture 只在该 Tick 尾部触发。v2.1 又把捕获收敛为首次有效结果或目标切换，同目标新结果只更新 HUD 局部轨迹。落地于 `b37968b` 及后续 v2.1。 | 自动化验证 worker 不触碰 UObject、HUD、SceneCapture；fresh 可见 PIE 连续进入/退出/重进终局并快速调参，日志不得再出现 `FAppTime`/`IsInGameThread()` ensure。`NullRHI` 不能代替这项渲染验收。 |
| 【已修复】移动一次输入后可能约半秒才更新轨道全览，连续移动还会显示过时结果 | 每个输入都执行较长轨迹积分；旧链路没有严格的 revision/最新结果语义，过时求解和 SceneCapture 更新会挤占后续输入，渲染捕获又被错误地绑定到每次鼠标微调。 | 同时最多保留一个 in-flight 纯数据求解；新输入只更新 revision/dirty，Tick 只发布仍与当前输入一致的结果并丢弃 stale result。捕获按目标缓存，同目标只刷新局部线条。标准 C++ Core 通过稀疏输出、宏步进和分段细化降低单解成本。 | HUD/日志同时显示 solve ms、端到端 latency 和 discarded 数；快速往返调参后只允许最新 revision 生效。性能门看同帧/下一帧反馈，不能只看单次积分耗时。 |

## 3. 终局输入、失败恢复与相机

| 现象 | 根因 | 处理 | 防回归验证 |
| --- | --- | --- | --- |
| 【设计已替代】早期终局发射的鼠标被接管且隐藏，袋子不直接跟随鼠标、左右方向相反、灵敏度和普通弹弓不同；进入模式的同一次松键还可能没有发射，之后需要再次点击 | 初版把相对鼠标位移直接映射成求解器 Yaw/Pitch，并同时承担“点击进入”和“松开发射”两种手势；视觉袋位置与 canonical solver pouch 未分层，输入捕获和 release gate 也不清晰。 | `b37968b` 先分离进入点击与后续有意的按下/松开，并修正方向、焦点丢失和视觉袋映射。HUD-1A/B 后正式替代旧合同：点击太空弹弓只进入控制台；鼠标可见、`GameAndUI`；Yaw/Pitch/Power 由三旋钮和 `1x/0.1x/0.01x` 档控制；只有 `LAUNCH` 提交。视觉袋可动但 canonical pouch 不动。 | 验收必须按当前 HUD 合同，不再用“与 M6 松鼠标立即发射完全相同”作为预期。检查进入点击不误发射、旋钮捕获互斥、LAUNCH 按下/释放只提交一次、焦点丢失取消捕获。 |
| 【已修复】错误发射后鸟返回主星附近并扎进地表，既不出现失败黑屏，也无法继续 | 失败表现沿权威积分点直接走到解析球体内部或恢复碰撞早于安全回位；失败时间线没有统一的可读停留、全黑和恢复边界，长 miss 还可能没有有界终点。 | 表现终点解析到 `BodyRadius + BirdClearance`，不改权威结果/Hash；失败统一走“可读停帧 → 淡黑 → 全黑安全恢复 → 淡出黑屏”。全黑时先关闭碰撞，再恢复原始 Transform/Movement，最后恢复碰撞；长 miss 也有上限。落地于 `b37968b`。 | `ABTS.M11C.Unit.PIERegressionContracts` 覆盖时间线合同；可见 PIE 分别测试撞主星、撞助推行星、飞歪和长时间无事件，均须黑屏恢复到进入终局前，且鸟不穿地、不残留禁用碰撞。 |
| 【已修复】太空弹弓相机在 Blueprint 中调整后，与前面普通弹弓相机的最终效果不同 | M11 曾直接生成原生 `AABTSM6SlingshotCamera::StaticClass()`，绕过 M6 运行时实际配置/生成的 Blueprint 子类，所以 BP 默认值和组件配置没有继承。 | M11 从唯一的 M6-owned 运行时相机解析精确类并生成同一子类；找不到或出现多个候选时 fail closed，同时输出 `CameraClassParity`。修复提交 `5af544e`。 | 自动夹具加载 `/Game/Blueprints/BP_ABTSM6SlingshotCamera` 并验证精确类相等；可见 PIE 对比普通/太空弹弓相同 BP 参数的构图，而不是只检查 C++ 基类。 |
| 【已修复】发射后主镜头停在太空弹弓原处，不跟随权威轨迹中的鸟 | M11-C 只插值移动鸟体，没有为发射态建立独立 ViewTarget 和沿轨道更新的相机姿态。 | 新增瞬态 `AABTSM11FinaleFlightCamera`，只消费权威位置/速度；用平行运输维护沿轨 Up，不参与 Chaos、积分或命中。Release 时切换 ViewTarget，失败/重置先回 Aim Camera，再交还 Party Camera。落地于 `9e504e7`。 | 自动化验证相机不改权威状态；可见 PIE 检查发射、三次近掠、成功/失败恢复期间 ViewTarget 顺序和连续性。 |
| 【开放】实飞一段后鸟离开画面；远处 UFO 也在世界中但几乎不可见，画面整体被浅蓝雾化 | 当前证据支持两个叠加原因：飞行相机以有限 `FollowLagSpeed` 追赶每帧按权威路径跳转的鸟，高速段稳态跟随误差会放大并把鸟推离视锥；关卡仍有 `ExponentialHeightFog`、`VolumetricCloud`、`SkyAtmosphere`，终局尺度下远处小物体与蓝色背景对比被吃掉。暂未发现 MaxDrawDistance/Hidden 直接剔除的证据。这一根因仍是待验证推断。 | 尚未修复。先做 A/B：关闭雾云但不改相机；再把相机位置改为权威同步基座、仅平滑局部表现偏移，并记录 Bird–Camera 距离和视锥状态。若只关闭雾云就恢复 UFO，则把终局天空/雾状态纳入 M11-D/集成接线；共享地图灯光资产仍由集成工作树修改。 | fresh 可见 PIE 分别验证无 lag、无雾云、两者同时开启；全程鸟须在安全框内，三颗行星/UFO 在预期距离保持可读。修复前不得把此项标为光照已确认。 |

## 4. 画中画、轨迹引导与表现语义

| 现象 | 根因 | 处理 | 防回归验证 |
| --- | --- | --- | --- |
| 【已修复】远端画中画只能看见行星模型，看不到预计轨迹；若直接随每个新解重摆相机，画面还会随微小调参乱晃 | SceneCapture 只能捕获注册到场景的组件，解析轨迹只是纯数据，不会自动出现在捕获纹理中；把相机姿态绑定每次最近点又会造成构图抖动和高频捕获。 | SceneCapture 只画当前目标 Actor；HUD 在同一 PIP 框上叠加当前权威结果的局部轨迹。目标始终居中，视向固定为“上一目标 → 当前目标”（首段为 pouch → Planet1），Up 固定为 Finale 局部 `+Z`；同目标的新解不重拍背景，只更新线条。另提供单目标 Wedge。落地于 `9e504e7`。 | 可见 PIE 检查三个行星和 UFO：轨迹必须与目标同时可见；轻微调旋钮时背景不乱晃、线条即时移动；切换目标时才允许背景重构图。 |
| 【已澄清】AUTO PIP 开关前后看到的内容不像同一台画中画，玩家难以判断两者关系 | AUTO 和 Probe 共用同一条权威轨迹，但观察锚点不同：AUTO 按当前最深前缀自动选择下一目标；Probe 固定在玩家从轨道全览点选的轨迹时刻/局部 frame，直到 REBASE、重新点选或恢复 AUTO。二者不是两套积分器，也不应互相覆盖选择状态。 | HUD-1C 明确两种状态和切换操作；`8b6eeee` 统一物理 RenderTarget、屏幕矩形和边框，保留不同的标题/局部轨迹语义。低模资产以后只替换 SceneCapture 中的目标 Actor 表现，不替换全览解析图标或求解器半径。 | 固定一条轨迹分别切 AUTO/Probe：权威 ResultHash 不变；AUTO 随前缀换目标，Probe 保持所选时刻；REBASE 只更新 Probe 基准；画框位置和尺寸不跳。 |
| 【已修复】AUTO PIP 与玩家点选轨迹后的 Probe PIP 大小和位置不同，切换时画框跳动 | 自动预览和轨迹探针走了两条独立绘制路径，各自计算 RenderTarget 矩形、边框和标题。 | 两套语义共享同一个 RenderTarget、布局函数和框体，只替换相机/叠加信息；修复提交 `8b6eeee`。 | 在同一分辨率下反复切换 AUTO/Probe，框体左上角、尺寸、边框厚度和标题基线完全不跳动。 |
| 【已澄清】F4 合格终端拦截包络大于 UFO 视觉本体时，权威轨迹可能未碰视觉网格就判定成功 | “可玩的终端资格”与“物理可见接触”曾被混为同一半径。大包络用于保持 F4 成功岛可玩，若直接把它当撞击点就会出现隔空命中。 | 保持 `Qualified TargetHit/TargetApproach` 与约 800–1000 cm 的 `Physical TargetContact` 分层。进入合格包络只确认终局传递；表现轨迹再沿冻结的确定性 terminal transfer 到 UFO 视觉接触点，之后才播放撞击/救援。不能用 Chaos 把鸟临时硬拉并反向修改求解结果。 | 日志和播放事件必须能区分 Qualified Endpoint 与 Physical Contact；白闪、UFO 受击、救援只允许发生在可见接触后。Hash/认证仍以冻结权威结果为准。 |
| `M11-T2B-RO-001` 【已修复合同】若按 Actor 名称、模型、距离或当前 PIP 目标推断 T2-B 类别，主星、普通卫星、候选辅助物或二维轨迹图元可能被误当成终局 3+1；若 M11 直接配置 SceneCapture 风格，又会提前改变 AUTO/Probe 生命周期和 PIP 像素 | 名称和视图状态都不是终局对象权威；唯一稳定来源是已提交 `AABTSM11FinaleSystem` 布局中的 Assist1/2/3、Target 身份，以及系统实际持有的已配置表现 Actor。远端 PIP 已由 `AABTSM11FinaleInteractionSystem` 的原生 SceneCapture 负责生命周期，M11 只应声明语义而不消费渲染 Profile。 | 新增 `TryGetStylizedObjectClass()`：仅当前系统持有且与已绑定布局 Stable ID/Role 一致的三颗表现行星返回 `FinalePlanet`，当前 Target Actor 返回 `FinaleUFO`，其余一律 `None`/false。InteractionSystem 只以 `const` owner/component 暴露既有捕获，并声明 `FinaleRemotePreview`；不设置 Custom Depth、Stencil、Profile、后处理或 Capture 参数。 | `ABTS.M11B.Runtime.StylizedSemanticAdapter` 覆盖 production 3+1、未知/候选辅助物 fail closed、销毁重建、Core Result/事件/认证 Hash 不变，以及 SceneCapture 入口 const、相机状态不变且不自行实现风格化；`PreviewFinaleFrameCompatibility` 继续覆盖 Candidate Rank 11 的 3+1。NullRHI 不能替代 Integration 对 PIP 像素/Profile 的正式验收。 |
| `M11-T3A3-MAT-001` 【代码/资产合同已完成；视觉待验收】若 T3-A3 通过 Actor 名称、世界遍历或“有几个材质槽”推断对象，再直接设置组件材质，遍历顺序、外来辅助物或缺失资产会造成错误覆盖；关闭 Style 时也可能覆盖外部系统已接管的接口。 | 终局的唯一身份权威是 Ready `AABTSM11FinaleSystem` 持有且 Stable ID/Role 与冻结布局一致的 3+1 Actor；材质槽恢复权归 Integration 的单一 T3-A0 注册表。M11 只应声明绑定，且材质缺失必须保留原接口。 | 新增 `FABTSM11StylizedMaterialAdapter::CollectBindings()`：只消费系统 3+1 getter、`TryGetStylizedObjectClass()` 与 Actor 的 `GetVisualMeshComponent()`；按 Certified Stable ID 排序，逐合法槽发布 `FinalePlanet/FinaleUFO`，缺材质/外来/辅助组件 fail soft。M11 不实例化私有注册表，不改 ViewClass/CVar/AVI 路径。 | `ABTS.M11.StylizedMaterials` 独立覆盖合同、遍历顺序稳定、合法槽位、缺材质、辅助/轨迹/碰撞排除，以及共享 Registry Style On/Off 后求解点/事件、Scenario/Preset/Certification/Bundle/Frame/Camera Pose Hash、Actor Transform 和原材质接口完全不变。NullRHI 只证明合同；背光和 MainWorld/PIP/AVI 像素仍需 fresh 可见验收。 |
| `M11-T3A3-MAT-002` 【已修复资产合同】从 Planet 母材质复制 UFO 图后，UFO 可能仍为 Two Sided、Description 仍写 Planet，母材质/实例也可能继承 Mars 默认贴图；材质能编译但 UFO 会显示错误表面身份。 | 复制材质图保留了非参数资产属性和 Texture Parameter 默认资源；只改八个风格参数与 HLSL 不会自动替换这些内容。UE 5.8 Python 的 `set_material_instance_texture_parameter_value()` 执行成功时还可能返回空值，不能把返回值直接当布尔成功。 | 在 Editor 关闭后用唯一 UE 5.8 无界面修正 `M_M11_FinaleUFO` 和 `MI_UFO_FinaleUFO`：UFO 改为单面、Description 改为 FinaleUFO，母材质默认纹理和实例显式覆盖都指向 `/Game/StaticMesh/UFO/T_UFO_*`；写入后用 getter 比对对象身份，再保存。 | fresh 命令行重载确认 UFO `TwoSided=false`、Custom `0.35`、14 个输入连接、Normal Sampler、四张 UFO 纹理和全部有效参数正确，重编译 `0 error(s), 0 warning(s)`；证据为 `Saved/Logs/M11-T3A3-MaterialAssetVerify-20260807.log`。可见背光与高光仍需 PIE/AVI 验收。 |

## 5. 求解器性能、候选构造与认证方法

| 现象 | 根因 | 处理 | 防回归验证 |
| --- | --- | --- | --- |
| 【已修复流程】早期一次 M11-B 候选搜索耗时约 7.5 小时，而且每调一次手感参数都可能重新付出 UE 编译和完整认证成本 | 搜索与 UE Runtime 耦合，候选构造、手感筛选和全域认证没有分阶段；旧全局 4096-work 枚举还对大量显然失败布局做完整求解。 | M11-A v2.1 提取不依赖 UE 的标准 C++ `M11Core`，UE 只保留显式适配层；M11-B CLI 与 Runtime 编译同一份 Core。Python 仅负责启动/分片/恢复/聚合 C++ CLI，不积分、不分类、不算 Hash、不排名。正式顺序改为：B v2.1 找候选 → UE parity 快测 → C v2.1 PIE 手感 → B v2.2 完整认证 → C v2.2 正式绑定。 | standalone corpus 与 UE corpus 的输入、结果、事件和 Hash 逐项 parity；产物记录 Core/Tool Source Hash。任何 Python 数值重实现都视为第二权威并拒绝。 |
| 【已修复构造策略】旧候选常在三颗行星同侧画平缓弧线，偏转不明显；Planet1 几乎覆盖全部可行鼠标区，Planet1/2/3/UFO 又接近共线 | 旧固定候选池和总分更重视“能依次到达”，没有把投影后真实转角、左右换侧、ScreenAim 前缀留存率和各级独立凸包作为足够强的构造门。 | 新增逐星条件粒子束构造器：先在父可行域边缘提出并锁定 Planet1，再只在通过父前缀的样本上提出下一星；每级检查真实偏转、换侧、无偏 5000 点 ScreenAim 比例和独立凸包。旧候选保留为对照，不被新构造器覆盖。 | 每个候选报告三次实际偏转角、符号/换侧、总飞行时间、S1/S0、S2/S1、S3/S2、各级 Hull 图；最终仍须 PIE 判断“引力弹弓感”，统计不能单独放行。 |
| 【候选未认证】在 Candidate 353 上局部提高 `F3/F2` 并合并 F4 后，新候选的偏转明显变小，还失去原 Rank 3 的交叉换侧性质 | 局部目标函数主要奖励前缀留存率和 F4 连通，未把基线候选的每段偏转下限、符号序列和投影路径相似度设为不可牺牲约束；优化器因此可用“把轨迹拉直”换取更大的成功域。 | 后续局部搜索必须同时约束三次最小偏转、换侧符号/次数、相对基线的轨迹距离和总时长；若这些门失败，即使 `F3/F2` 或 F4 topology 更好也只能作为诊断候选，不能覆盖原 Rank。 | 每轮局部优化前后并排输出偏转角、符号序列、ScreenAim/Hull、F4 分量和 flight time；PIE 必须能从全览直接辨认交叉偏转。 |
| 【已修复节奏目标】v1 权威轨迹可长达约 558–700 秒，行星之间存在很长的无偏转航段 | v1 的远场合同允许 700 秒，尺度、速度和虚拟动量使慢速长航成为合法解；求解正确不等于一分钟内有可玩的演出节奏。 | v2 将目标总时长收敛到 60 秒内，单个强偏转区约 5 秒；提高引力作用圈、虚拟动量和初速度，并在搜索评分中加入时长与平淡期门。稀疏输出和事件附近细化同时降低预览点数。 | 候选报告总时长、三段进入/退出时间和段间最长平淡期；可见 PIE 以正常播放速度完整观看，不能用快放掩盖 700 秒权威轨迹。 |
| 【候选未认证】Rank 3 在 v2.2 预认证中早停：稀疏/half-cell/局部细化都出现多个 F4 六邻域分量 | 这不是单纯网格太粗造成的“对角线误断”。递归细化后仍存在一个主岛和多个极小真实碎片：终端包络、Assist3 出口和方向锥相交形成狭窄旁瓣。把六邻域改成 18/26 邻域只会掩盖拓扑，不会证明输入域唯一。典型证据：half-cell `F1/F2/F3/F4=2067/553/72/27` 且 F4 为 9 分量；更细桥区 F4 仍有一个 1521 点主岛和 31 个不超过 7 点的碎片。 | 认证按设计早停，没有伪造通过。先试 UFO 小范围平移/半径和方向锥修复；不足时回到 Assist3 B-plane/虚拟动量/terminal mapping 做局部重构，再从粗网格重新认证。 | 正式门仍是完整域六邻域唯一 F4 连通分量、`F4 ⊂ F3 ⊂ F2 ⊂ F1`、边界递归闭包、单星消融/错序/多圈/迟到旁路失败。不得只报告最大分量占比。 |
| 【已修复认证语义】Candidate 353 在 PIE 中看似正常，但日志/认证出现 `TargetHit` 早于 `Assist3 Exit` | 临时候选分类只检查“最终完成三次 assist 且曾进入目标包络”，没有把事件发生顺序纳入 F4；较大的终端包络与第三颗行星作用区重叠，轨迹在尚未合法退出 Assist3 时就先触发 TargetHit。视觉上仍像飞过第三星，因此 PIE 不容易察觉。 | 收紧成功事件序列为 `Assist3 Exit → TargetApproach → TargetHit`，CLI、Core 测试和认证分类使用同一语义。Candidate 353 及其派生 Rank 只能继续作为 preview，不能以旧计数晋级认证。 | 测试显式断言事件单调顺序；错序命中必须归为失败。PIE“看不出异常”不是事件序列证据。 |
| 【已澄清】同一候选的“5000 → 727 → 185 → 29 → 10”看起来接近理想，但 half-cell 网格的比例和分量变化很大 | 前者是在最大 Power 下对二维 ScreenAim 做固定 5000 点撒点，衡量鼠标手感/凸包；后者是 `Yaw × Pitch × Power` 三维结构网格及 half-cell 偏移，衡量体积与六邻域连通性。采样维度、测度和邻接规则都不同，计数不可直接比较。窄斜带还可能在粗六邻域中暂时断开。 | 所有报表强制标注 Domain、Power slice、屏幕坐标映射、采样种子/分辨率和邻接规则。ScreenAim 只决定可玩比例/Hull，局部细化解释粗网格断带，完整三维网格决定认证。 | 任何比例都必须附 `ScreenAim@Power=...` 或 `FullLaunchDomain/Grid` 标签；不得拿 2D 满功率结果证明低功率无旁路或三维 F4 唯一。 |
| 【候选未认证】Rank 8 的 PIE 和满功率 Hull 合格，但全 Power 粗扫发现 F1 最低 Power 约 0.75，未达到预期 0.88 门槛 | “把 Planet1 放远即可严格单调提高 Power 门槛”只是近似直觉。改变初始速度还会改变到达相位、入射方向和引力捕获，四天体刚性平移也不保证 F1 阈值按距离单调变化。若局部认证从 0.875 起扫，更不能证明 0.875 以下不存在旁路。 | 先在完整 Power 范围做廉价粗扫并定位 Planet1 初次可达区间，再在近最大 Power 的 `Yaw × Pitch × Power` 细扫；Planet1 径向外移约 5900 cm 只作为 Rank10 构造起点，后续允许各星小幅角向修复。 | 慢认证前必须输出全 Power 粗扫、最低合格 Power 和低功率旁路计数；最终仍需覆盖完整 Power 闭包，不能因为设计上“第一星较远”就删除 Power 维。 |
| 【候选未认证】仅按统一比例放大视觉/碰撞/引力半径、星间距、初速度和时间步后，原候选没有自动保持可用 | 连续物理在所有有量纲参数严格相似变换时才可能保持无量纲轨迹；实际搜索还含固定的角域、时间上限、步长/扫掠误差、事件阈值、方向锥、评分和凸包门。时间插值若按错误方向缩放还会改变离散积分误差，因此“所有数值乘同一倍数”不等于认证不变。 | 首次放大实验按失败证据保留（`2388762`），没有硬绑定；随后用逐星搜索重新调距离/角度和 terminal 参数，产出视觉半径 5000 cm、解析碰撞半径 5500 cm 的 Rank11 候选（`3ff0485`，PIE 暴露于 `910a52f`）。 | 每次尺度变化都重新跑 Core parity、扫掠碰撞、事件顺序、ScreenAim/Hull 和完整域认证；候选身份与 Hash 必须改变。Rank11 仍是 Candidate/NOT CERTIFIED。 |
| `M11-CERT-V22-001` 【已修复调度假早停】Rank11 首次正式 base 扫描的 16,359 点全部完成，但调度器在合并后以 `nominal_f4_missing` 早停。 | Rank11 Nominal 为 `(-1.75°,26.25°,1)`，不落在以完整域端点为原点的 `2°×3°×0.025` base 或 `1°×1.5°×0.0125` half-step 节点上；C++ 的 `nominalF4=false` 只表示网格中没有精确同点，不能推翻独立 Nominal 重放。 | 保留首轮早停目录，不覆盖证据；正式调度新增独立单点 Nominal F4 preflight/merge，并在全域 discovery 显式传入 `--allow-off-grid-nominal`。ScreenAim 同时继续核对冻结 Source/Request/Result Hash。 | pinned portable CTest `9/9`；fresh 单点 `F1–F4=1/1/1/1`、四级分量均为 `1`、`NominalF4=true`；随后同一新输出根完成 base 与 half-step，不再触发 `nominal_f4_missing`。若独立单点未来失败仍须 fail closed；不得简单删除 Nominal 门。 |
| `M11-CERT-V22-002` 【认证早停 / 候选未认证】Rank11 通过身份与独立 Nominal 门，但 fresh 全输入域发现层在 half-step 早停。 | base 的 F4 为 `9` 点、`3` 个六邻域分量；half-step 的 F4 增至 `54` 点、`9` 个六邻域分量，并出现 `1` 个 `EarlyTargetHit`。这既不满足唯一 F4 岛，也违反 Assist3 合法退出后才允许 TargetHit 的顺序门。 | 保留 Rank11 为唯一冻结认证输入，但状态写为 `early_stopped_half_step_non_unique_f4_and_early_target_hit`；不继续边界递归、消融、Trust Region 或 Bundle，也不以 18/26 邻域或最大岛占比绕过门槛。 | base `F=389/101/30/9`、分量 `3/1/3/3`、Hash `0x12588609d360fc8c`；half-step `F=3014/827/229/54`、分量 `3/1/5/9`、Hash `0x23416df242cb995a`；`Early/Bypass/Nesting=1/0/0`。后续若调整布局或终端资格必须产生新 Candidate/Variant Hash 并从 base 重启。 |
| `M11-CERT-V22-003` 【已修复诊断调度参数错误】Target-only 小偏移搜索的首次启动在 fresh baseline 合并时被 CLI 以 `MissingValue:--require-nominal-f4` 拒绝。 | 调度器把 C++ CLI 的默认布尔门误当成无值开关显式传入；该参数实际要求后续值，导致解析失败。失败发生在候选搜索前，不能当成物理早停或零命中证据。 | 保留失败输出根 `Intermediate/M11V22TargetOffset/Rank11-20260812-103428`，删除错误实参并依赖 CLI 默认；只在离网格局部闭包传 `--allow-off-grid-nominal`。随后重跑 pinned portable 套件和全新搜索根。 | 修复后 portable CTest `9/9`；fresh baseline 精确复现冻结 half-step `F=3014/827/229/54`、分量 `3/1/5/9`、Hash `0x23416df242cb995a`，正式 Target-only 247 点搜索正常完成并产生非零独立 Variant Hash。 |
| `M11-CERT-V22-004` 【小偏移搜索早停 / 无新候选】在不改冻结 Rank11 的前提下，Target-only 247 个变体及 Target+Assist3 出口映射 182 个联合变体均未达到全域晋级门。 | Target 位移能把局部 F4 从 54 降到 51，但九个 F4 六邻域分量全部保留，EarlyTargetHit 也始终至少为 1；再开放 Assist3 B-plane `±600 cm` 与 Sigma `0.90–1.10` 后，全部 182 个联合样本仍为 F4 `51–54`、分量 `1/1/5/9`、Early `1`。该窗口只改变已有岛内/边缘命中量，没有填充连接岛屿所需的 F3/F4 桥。 | 两阶段都要求独立 Nominal F4，并以独立 VariantSourceHash 隔离；只有 `Early/Bypass/Nesting=0/0/0` 且 F4 分量显著收敛才可晋级。实际 `eligible/promoted/full=0/0/0`，因此不启动昂贵 base/half-step，不覆盖冻结 Manifest。 | Target-only 报告 `.../Rank11-20260812-104005/search_report.json`，SHA-256 `0B70A185...1B9C8`；联合报告 `.../Rank11-20260812-105558/search_report.json`，SHA-256 `C234BD58...C288F`。最佳诊断为 Target `(-950,-2300,-850) cm`、Assist3 B-plane `(0,0) cm`、Sigma `0.94`，F4=51、分量=9、Early=1。此结论只覆盖已声明的小偏移窗口；若继续必须产生 Assist3 几何/映射局部重构的新 Candidate。 |
| 【已澄清】提高三颗助推行星的视觉半径后，担心解析轨迹未碰撞但画面穿进模型 | VisualRadius、CollisionRadius 和 InfluenceRadius 是三种职责；只放大视觉而不提高解析 swept collision 半径就会出现视觉穿模。 | 当前放大候选以 VisualRadius 5000 cm、CollisionRadius 5500 cm 留出鸟体和数值净空；碰撞继续由标准 C++ 解析扫掠判断，不交给 Chaos。InfluenceRadius 可更大，但不能反向当实体表面。 | 测试最小近掠距离必须大于 `CollisionRadius + BirdClearance`；可见 PIE 检查近掠外轮廓。替换低模后须按实际包围球重新核对，而不是只改缩放。 |

| `M11-CERT-V22-005` 【已修复证据包装假绿】一次 portable 重建把相对日志路径交给 `Tee-Object`，脚本工作目录切换后日志父目录不存在；PowerShell 管道末端又覆盖了真实退出码，外层错误显示为成功。 | 相对证据路径依赖调用方当前目录，且只读取管道后的 `$LASTEXITCODE` 不能证明前序构建与 CTest 成功。该次命令没有形成可用日志，也不能作为通过证据。 | 后续所有正式构建先把日志解析为仓库内绝对路径；命令完成后不仅检查进程码，还解析最后一条 `abts.m11_core.build.v2` JSON 并要求 `passed=true`。失败尝试保留为流程诊断，不计入物理搜索或 portable 通过次数。 | 后续多次全新输出根均产出绝对日志；最终 pinned portable CTest 必须明确显示 `100% tests passed, 0 tests failed out of 9`，并由机器摘要再次断言 `passed=true`。 |
| `M11-CERT-V22-006` 【扩大搜索早停 / 无合格新候选】从 Assist2 映射与真实行星2径向位置重开，在保持 Rank11 名义点和手感硬门的前提下，F4 从冻结基线的 `9` 分量、Early `1` 改善到 `4` 分量、Early `0`，但始终不能达到唯一分量。 | Assist2/Assist3/Target 的位置、B-plane、Sigma、虚拟速度及径向补偿能清除 Early 并合并大部分岛；残余三个小岛只有 `5/4/2` 点，却与 `56–58` 点主岛同为合法终端到达族。累计 `2446` 个有效局部变体没有出现 `1–3` 个 F4 分量，表明已声明范围内存在稳定的四分量拓扑下限，而不是样本偶然漏掉单个优解。 | 使用分阶段 Halton、盆地精修、坐标探针、插值、Target 小立方体、Assist3 末端补偿及真实 `assist2-radial-delta`；所有结果保持独立 Variant Hash，冻结 Rank11 不变。四个最佳点另做完整 base/half-step 诊断提升，仍全部为 `4` 分量，因此按唯一连通门 fail closed，不生成 candidate handoff。 | 最近诊断 `0x58840ee73ddd70f5`：half-step `F=1841/790/238/67`、分量 `1/1/6/4`、最大 F4 `56`、`Early/Bypass/Nesting=0/0/0`；偏转误差 `0/6.03%/9.23%`、时长误差 `0/2.53%/6.37%`、总时长误差 `0.04%`。完整提升四点 F4 为 `67/72/70/69` 且均 `4` 分量。 |
| `M11-CERT-V22-007` 【已隔离无效半径扫描】尝试把冻结 Rank11 的 `TargetHitRadius=41250 cm` 小幅收紧到 `30000–42000 cm` 时，200 个调度项全部瞬时失败。 | CLI 的显式 `--target-hit-radius` 诊断覆盖合同只允许 `4500–12000 cm`；冻结候选的 authored 41250 cm 只有在不传覆盖开关时才合法。该失败是接口合同拒绝，不是 200 个物理反例。 | 保留失败根 `Intermediate/M11V22TerminalRadius/Rank11-20260812-135801`，从物理样本计数中剔除；调度器对 authored 41250 cm 不再显式传开关，并把默认覆盖范围改回 CLI 合同。随后改用已开放的 `targetMinimumCorridorQuality=0.05–0.50` 扫描 152 点。 | 半径失败 stderr 全部为 `TargetHitRadiusOutsideSearchContract`；质量门 152 点全部有效，却仍最少 `4` 分量。若要真正小幅调整 41250 cm 半径，须由集成工作树先审议并扩宽稳定诊断合同，功能工作树不得自行绕过。 |
| `M11-CERT-V22-008` 【手感测试隔离】用户希望直接比较诊断变体 `0x58840ee73ddd70f5` 与冻结 Rank11 的手感，但该变体已知不满足六邻域唯一 F4 门。 | “加入 Editor 候选列表”是可逆的人工体验入口，不等于认证晋级；若复用 Rank11 或修改冻结 Manifest，会让手感反馈污染唯一认证输入身份。 | 新增列表末尾 Rank12，完整重建变体几何/映射并冻结独立 Source/Request/Result/Evidence Hash；状态明确为 `UNCERTIFIED / handfeel test only`。Rank11 冻结文件、默认 Rank0 和生产绑定均保持不变；原内部诊断 Rank12 平移为 CLI Rank13。 | Rank12 固定 5000 点 ScreenAim 为 `615/139/18/8`；portable `10/10`、UE 5.8 Development Editor 完整链接成功，fresh NullRHI 候选目录 `1/1`，核对 `Source=0x58840ee73ddd70f5`、Nominal `Result=0xf746bbe4ca7b9748` 且仍为 F4。人工测试只设置 `abts.M11.CandidateRank 12` 并重启 PIE，反馈不得作为认证通过证据。 |
| `M11-CERT-V22-009` 【合同交接 / 待集成】Rank12 half-step 在 18 邻域下为单分量，容易被误记为“认证完毕”。 | 18 邻域会把六邻域的 `56/5/4/2` 四个分量通过两轴斜边连接；它只说明粗发现格上存在对角相邻，尚未证明连续输入域内存在 F4 桥。并且改变 Connectivity 会使 ScanContract、CertificationHash 与 Bundle 全部失效。 | 用户批准下一版采用“18 邻域初判＋桥区递归闭包”：斜边仅作为待证明桥边，递归到冻结最终精度后由连续 F4 路径决定保留或删除。稳定合同版本/Hash/兼容由集成工作树落地；M11 合并新基线后负责 Rank12 fresh 扫描、桥闭包、消融、Trust 和认证候选 Bundle。 | 现有只读复核为 6 邻域 `4` 分量 `56/5/4/2`、18/26 邻域均 `1×67`、`Early/Bypass/Nesting=0/0/0`。当前状态只能写 `18-neighbor discovery precheck passed`，不得写 Certified；完整步骤和所有权见 M11-B 设计稿的 Rank12 交接检查点。 |
| `M11-CERT-V3-010` 【认证早停 / 桥闭包通过但精化超预算】Rank12 fresh v3 已证明三条必要斜桥，但不能继续进入最终精化、消融和 Bundle。 | base/half-step 的 F3 证据包络为 Yaw `[-5,0]`、Pitch `[19.5,31.5]`、Power `[0.675,1]`；按冻结的一个 coarse-cell halo、Nominal 锚点和最终精度构造后，需要 `49×73×113=404,201` 点，超过稳定合同 `250,000` 点上限。桥通过只关闭连通证据门，不能绕过后续资源与宽度门。 | 新增逐字节对齐共享 v3 合同的 portable 镜像、桥区 C++ 权威和 Rank12 Python 纯调度；fresh 重算 nominal/base/half-step，三桥各 `845` 点、各有 `17` 点连续 F4 路径，随后在实际求解精化网格前执行确定性预算门并 fail closed。未运行后续消融/Trust，未生成 Bundle，生产绑定保持 false。 | 输出根 `Intermediate/M11V3Certification/Rank12-20260813-03`：Plan `0xb4cc770aeda2ce17`，Policy `0x40ccc25283f67c8c`，桥 `3/3`，Closure `0x240d8b2055832d36`；refinement budget `404201>250000`，状态 `early_stopped/refinement_budget`。继续需要 Integration 审批新预算/区域构造版本，或回到 M11 产生更窄 F3 包络的新候选。 |
| `M11-CERT-V3-011` 【认证早停 / 最终精化揭示 F4 多岛与提前命中】Integration 将 v3 精化预算提高到 `500000` 后，Rank12 的 `404201` 点最终精度网格全部完成，但不能进入宽度、Trust、消融或 Bundle。 | half-step 的 18 邻域初判与三条局部桥只证明冻结桥区在最终精度上各自存在连续路径；把全部 F3/终端证据包络统一细化后，F4 仍分裂成 `30` 个六邻域分量，并有 `305` 个轨迹在完成三助推前提前命中。局部桥闭包不等于最终全闭包单连通。 | 合并 Integration `41f34ce`，冻结/调度预算同步为 `500000`；fresh 重算 Nominal、base、half-step、桥闭包和 `49×73×113` 精化网格。调度在 C++ 合并摘要返回 `EarlyTargetHitCount=305`、`F4Components=30` 后按稳定门 fail closed，不执行后续阶段。 | canonical 根 `Intermediate/M11V3Certification/Rank12-20260813-05`；精化 `F=199316/92749/29580/9225`、分量 `1/1/23/30`、最大 F4 `9096`、NominalF4=true、Nesting/Bypass=`0/0`、Hash `0x45f9e5508f5a4f66`。Plan/Status SHA-256=`34526D0B...E86D7F7 / 38F1183C...2983FC`；候选仍为唯一冻结 v3 输入但 `UNCERTIFIED`，生产绑定保持 false。 |

## 6. 轨道全览 HUD 的坐标、模式与裁剪

| 现象 | 根因 | 处理 | 防回归验证 |
| --- | --- | --- | --- |
| 【已修复】按钮画在一个位置，实际点击热区整体偏到左侧；第一轮修复后 SELECT/MOVE 等按钮仍残留偏移 | 这是两个叠加坐标变换。第一层：`UCanvas` 用 DPI 调整后的 `ClipX/ClipY` 绘制，输入仍是原始 viewport 像素；第二层：`GameViewportClient` 会按 `SceneView::UnscaledViewRect.Min` 平移玩家 Canvas，鼠标仍相对完整 viewport。只修 DPI，没有扣除 player-view origin，因而残留固定偏移。 | `9836a0a` 统一 raw viewport → HUD Canvas logical space；`d0fcf16` 再纳入 `UnscaledViewRect` 的 origin/size。所有按钮、旋钮、轨迹点选、拖拽、滚轮和 release hit test 都只消费转换后的 HUD 坐标。 | 自动化覆盖 DPI≠1 和非零 viewport origin；在 PIE 实测窗口缩放、编辑器嵌入视口、不同分辨率，点击每个控件中心和边缘均与绘制一致。 |
| 【已修复】ROTATE 拖动不符合直觉，稳定 Up 约束让全览像被强行扶正；随后上下拖动方向又与画面旋转相反 | 把局部 Up 固定为世界/Finale Up 会抵消玩家希望的自由轨迹球滚转；屏幕 Y 向下与轨迹球俯仰符号又被映射了两次。 | `026b60b` 改为绕全览中心的自由 trackball/四元数旋转，不再每帧重建固定 Up；`c79ae0e` 修正垂直符号。随后 `f8191da` 将模式改名为 MOVE：左键平移枢轴，右键绕枢轴旋转，初始枢轴为包围球中心；SELECT 不读取或改动枢轴。 | 右拖上下左右时目标运动方向与手势一致，连续斜拖允许自然滚转；切换 SELECT 不改变视图枢轴，RESET 恢复包围球中心。 |
| 【已修复】主星蓝色外圈、经纬线、行星影响圈或轨迹线可能越过圆形轨道全览边界 | 早期只裁剪轨迹中心点或部分图元，忽略线宽、圆半径、相交线段和文本真实尺寸；主星投影圆靠近边缘时尤其明显。 | `f1e5be6` 将主星/网格、行星/UFO、影响圈、实虚轨迹、Hover/Probe 全部送入统一圆裁剪，按线宽向内缩；标签按实际文本 bounds 收口。只有外框本身允许落在边缘。 | `ABTS.M11C.HUD.Unit` 与 `ABTS.M11C.Unit` 覆盖几何合同；可见 PIE 在平移、缩放、旋转及极端候选下检查所有图元，无一像素明显溢出圆框。 |

## 7. M3 生成世界与候选预览接线

| 现象 | 根因 | 处理 | 防回归验证 |
| --- | --- | --- | --- |
| 【已修复接线】M11 候选最初只在本地独立预览坐标中成立，不能证明接入 M3 Task Graph 生成世界后仍相对终局弹弓正确摆放 | 候选使用终局局部布局预设，但 M3 生成的 launch frame、月度世界位置和编辑器预览入口没有统一桥接；若退回世界坐标硬编码，会随世界生成结果失效。 | M3R-5.2 输出冻结的 Finale Preview Frame，M11 只把候选局部位置变换进该 frame；提交 `a87cfda` 接通，`5b62856` 补充月度 frame 验证。PIE 前可用 `abts.M11.CandidateRank <n>` 切换 Candidate Catalog，不修改 production 认证绑定。 | 对多个月度/M3 frame 验证平移旋转兼容、哈希和局部相对关系；控制台必须明确显示 `EDITOR CANDIDATE / NOT CERTIFIED`、Rank 和 Source Hash。共享地图、Task Graph 稳定契约仍由集成工作树修改。 |

## 8. 终局镜头独立录制

| ID / 现象 | 根因 | 处理 | 防回归验证 |
| --- | --- | --- | --- |
| `M11-CAP-001` 【已修复】命令行 startup `LevelCapture` 能创建 AVI 并自动退出，日志也有完整发射/命中链，但最终 AVI 主头 `TotalFrames=0`，文件只有 65536 bytes；仅以“文件存在且非空”会假绿。 | UE 5.8 旧 `MovieSceneCapture`/FrameGrabber 在本项目的 `UnrealEditor.exe -game` unattended 生命周期中没有持续取得有效游戏 backbuffer；改成 JPG 协议或普通窗口也不能产生可验收全程帧。 | M11 Runner 不再使用 startup LevelCapture。显式捕获进程创建独立 `SceneCapture2D + TextureRenderTarget2D`，逐帧复制当前 PlayerCameraManager 最终 View 并同步写 JPG；TargetHit 后在同一进程内封装 MJPEG AVI。原生 AVI 直写在参数解析层 fail closed。 | AVI 验收必须解析 `avih.dwTotalFrames`、视频 `strh.dwLength` 和 `idx1` 条目数，三者均须等于连续 JPG 数；首尾索引须指向合法 JPEG。Rank11 三渲二 fresh 证据为 949 帧、1280×720、30 fps、31.633 s、AVI 21757000 bytes。 |
| `M11-CAP-002` 【已修复】独立 M11 地图具备 Finale Slot Pair，但没有已安装的 Space Stake/Cord，自动录制等待到超时，无法进入正常发射链。 | 终局交互正确要求与 Finale Slot Pair 精确匹配的已装配太空弹弓；录制工具不能假设玩家库存/安装步骤已在 fresh process 中发生，也不能绕过交互直接搬运鸟。 | 只在显式 `-ABTSM11CameraCapture` 进程中，根据唯一 Finale Slot Pair 创建瞬态 Space Stake/Cord 夹具；随后仍调用 `TryEnterFinale`、精确 `NominalInput`、ReleasePending、求解、发布和权威播放链。夹具不写地图、不改库存。 | Manifest 必须记录 `captureFixtureCreated=true`；日志须依次出现 `CaptureFixtureCreated → NominalAttemptQueued → Release CandidateQualified=1 → FlightCamera FollowStarted → TargetHit`，Rank 1–11 始终标为 `UNCERTIFIED`。 |
| `M11-CAP-003` 【已修复工具；旧镜头问题保留】直接从 Actor Tick 读 GameViewport 或在首帧请求 Slate Screenshot 会得到黑帧、D3D11 `TextureRHI` ensure 或卡在首次截图；成功录到旧镜头后，发射初段可见鸟，随后大部分帧只剩浅蓝天空。 | 前半是生命周期问题：Actor Tick 早于可读 backbuffer，且 unattended/offscreen 窗口不能作为稳定截图源。后半是既有旧镜头问题，不是录制器丢帧：`AABTSM11FinaleFlightCamera` 在高速段不能持续把鸟/叙事行星保持在画面内，与本文“实飞后鸟离框”的开放项一致。 | 验收器改为自身拥有的 RenderTarget，在 `TG_PostUpdateWork` 复制已结算相机 View 后 CaptureScene/读回；预热完成且至少两个起始帧落盘后才发射。M0 不修镜头导演，忠实保留“鸟离框/天空空镜”作为旧版基线。 | 抽检起始帧、发射帧、1/4、1/2、3/4 和终帧；验证图像非黑、尺寸一致和编号连续。M1 起用屏幕空间指标量化鸟/当前行星离框；不得把录制器成功误写成旧镜头构图通过。 |

| `M11-CAP-004` 【离屏风格接线已修复；镜头可读性仍开放】合入包含三渲二实现版本 3 的 master 后，Rank 11、Stylized=1 的命令行录屏在发射初段之后只剩浅蓝天空；抽检第 4 帧可见鸟和弹弓，第 250/500/750/948 帧均未见鸟、行星或 UFO 轮廓。 | 录屏工具使用独立 `USceneCaptureComponent2D`；全局扩展正确地拒绝 `InView.bIsSceneCapture`，但录制组件此前没有像已知 PIP 一样直接注册语义视图。Manifest 中的实现版本只证明进程加载了代码，不能证明该组件实际消费 Tone/Outline。旧飞行镜头构图问题同时存在，但与渲染接线是两个故障。 | Integration 新增 `FinaleCinematicCapture`，由 M11 Runner 在首帧前直接注册并在完成/失败/EndPlay 注销；继续拒绝未知 Scene Capture。合同版本 3 记录 ViewClass、注册状态和 policy，字段命名明确为 PolicyEnabled，像素结论必须读取 AVI。 | ForceUnity；fresh NullRHI `ABTS.Rendering.Toon` 7/7 与 `ABTS.M11C.CameraCapture.Config` 1/1；fresh Rank11 Stylized DX11 命令行录制 `Rank11-Stylized-FinaleCapture-v6-20260805-230434` 为 949 帧。AVI 第 3、250、500 帧可见鸟/行星轮廓，证明接线生效；UFO 仍无法在终段可靠辨认，归后续镜头导演。AVI SHA-256 `FE16E64797EE8101E414E1781C51EE730EF7B9EC45D400966B6B8A81C65CA3A1`。 |
| `M11-CAP-005` 【已修复合同】M11 最新镜头提交合并 `master` 时，Runner 头文件在 `ContractVersion` 和尾部状态字段发生冲突；若直接选择任一整文件版本，会丢失“中途切换渲染模式即 fail closed”或 M1/M2 CSV/导演遥测之一。 | M11 在旧共同基线 v3 上把 M1 观测定义为 v4、M2 导演扩展定义为 v5；期间集成线未先让 M11 同步，却独立把另一项全程三渲二状态合同也定义为 v4。两个工作树并行占用了同一版本号，且集成提交直接触及 M11 专属 Runner，造成同号异义和字段冲突。 | 不回滚有效修复，也不在候选分支选 `ours/theirs`。由 M11 所有者合并最新 `master`，逐项保留状态门、CSV 与导演字段；CPP 自动合并后仍审计检查顺序和 Manifest。已有 v4/v5 证据不可改写，完整合同升为 v6，并在初始化显式重置两组状态。以后修改 Runner 合同前必须先同步最新 `master`、检查当前最高版本；集成线若需改变 M11 专属合同，应先走所有权交接或让 M11 落地，禁止并行占号。 | 合入集成自洽修复 `182a3cd` 后，Development Editor 与 ForceUnity 均为 `Succeeded`（`M11-V6-Default-20260806-183657-UBT.log`、`M11-V6-ForceUnity-20260806-183657-UBT.log`）；fresh NullRHI 依次通过 Contracts `2/2`、M110 `4/4`、M11A `15/15`、M11B Unit `8/8`、Runtime `6/6`、M11C `23/23`、Rendering Toon `9/9`。fresh Rank11 Stylized M2 v6 录制 `M11-V6-Rank11-Stylized-M2-20260806-184528` 为 949 帧，CSV Schema 2/行数一致，AVI 三处帧计数均为 949，SHA-256 `E62C08BF626C4C60222AB3EA64A1F4A519ED1081FF5B986596E77B0122C642F0`；鸟丢失、导演越界和位置/旋转/FOV 跳变均为 0，`lucyPacingObserved=true`。联合 `T2C1-FinaleRemotePreview-On-SP100-20260806-184831` 同样完成 949 帧，v6 Manifest 为 `Complete/TargetHit`、`stylizedRuntimeStateMaintained=true`、失败帧 `-1`，日志零 `StylizedRuntimeStateDrift`；抽检第 3/250/500/750/948 帧轮廓全程保持。配置单测冻结 `ContractVersion=6`。 |
| `M11-CAM-M1-001` 【已修复观测】M1 首次 Rank11 观测把鸟的像素半径算到数千像素，出现“鸟始终覆盖全屏”的假指标。 | `GetActorBounds` 合并了鸟 Actor 上与镜头构图无关的附属组件；Actor 级包围体不是主控鸟视觉本体。 | 鸟观测优先读取 `GetBirdVisual()` 的 Skeletal Mesh 渲染 bounds；只有视觉组件缺失时才回退到 Actor 位置和保守固定半径。首次异常录制只保留为被拒绝的诊断，不进入 M1 四象限证据。 | `ABTS.M11C.CameraCapture.Config` 验证投影合同；修复后四象限报告中鸟像素半径和屏幕坐标有限，Rank0/11 两种渲染的鸟丢失分类一致。 |
| `M11-CAM-M1-002` 【已修复离线判据】Rank0 的 Stylized 0/1 有效飞行段完全相同，阶段窗口和失败计数也一致，但旧离线指纹仍判定正交失败；两次总帧数分别为 1146/1147。 | fresh 进程的风格预热可使发射前等待相差一帧；旧指纹错误地把绝对 `frameIndex`、PreLaunch 行和呈现 Actor 的连续浮点观测混进“阶段决策身份”。 | 指纹拆成三层：有效飞行段的权威 Stage/Target/Reason 决策指纹、逐帧阈值分类指纹、只用于追踪的原始观测指纹。M1 正交门要求前两者相等，不让发射前调度噪声冒充导演分支变化；M5 再要求稳定导演的相机数值 Hash。 | Rank0 与 Rank11 各自的 Stylized 0/1 均为 `allDecisionFingerprintsEqual=true`、`allCriteriaFingerprintsEqual=true`、`allIdentityFingerprintsEqual=true`；Rank0 原始观测指纹不同被明确保留，未被隐藏。 |
| `M11-CAM-M1-003` 【已修复 Schema 消费者兼容】M11 合入集成工作树后，录制器正常输出 CSV Schema 7 和完整 Manifest，但随分支合入的 `analyze_camera_observations.py` 在读入阶段以“只支持 1–6”拒绝分析。 | M4 在提交 `718adbc` 中为终点权威和 UFO Acquire 把观测合同升至 Schema 7，但当时只更新了录制器、Manifest 与设计稿，没有同步 Python 消费者。若只把 7 加进白名单，旧工具还会把 `TerminalAcquire` 的 Assist3→UFO 桥误计入 M3 双行星窗口，并把 M4 导演帧误报为越界。 | 工具完整支持 Schema 1–7：Schema 7 强制验证 `endpointAuthority` 列及枚举；M3 只消费 Assist→Assist 桥，M4 按 `TerminalAcquire/TerminalTrack` 独立统计 Acquire 空窗、鸟/UFO/Endpoint 丢失和镜头跳变；决策/判据指纹在 Schema 7 纳入终点身份和 M4 分类。物理接触仍以 Manifest 的 PlaybackPlan Authority 为准，不用 BirdVisual Bounds 伪算。 | Python 契约回归 5/5，覆盖 Schema 6 兼容、Schema 7 缺列/非法枚举 fail closed、M4 空窗失败及 M3/M4 隔离。真实 Schema 6 基线可正常出报告；两条 M7 F4 Schema 7 均正常分析且 M4 闭合通过。M5 Rank0、Rank11 各自 Stylized 0/1 的决策/判据/身份指纹继续全等；所有六份 Schema 7 报告的 M4 帧数和九项失败计数与各自 Manifest 一致。 |
| `M11-CAM-M2-001` 【已修复构图】M2 第一版沿旧追尾射线最多后退 12000 cm，再瞄准鸟/Assist1 角边界中点；目标丢失略有改善，但鸟在 Assist1 窗口丢失 162/193 帧。 | Rank11 近掠时鸟与 Assist1 球心相距约 4.6–6.8 万 cm；沿轨后退不能保证两目标进入固定 FOV，视轴取中只会把二者分别推到画面两侧。 | 改为把相机放在“Assist1 中心 → 鸟”的外法线延长线上：鸟保持可读前景、行星在其后；Approach 依靠距离自然拉近，Periapsis 再增加最多 6000 cm 的径向后拉。位置平滑后从实际相机位置重新求双目标视轴。 | 中间轮次把 Assist1 目标丢失降到 26–29 帧，鸟丢失由 162 降至 63、再降至 10；纯数学测试覆盖径向共线和位置平滑后的二次构图。 |
| `M11-CAM-M2-002` 【已修复过渡】径向构图主体通过后，Assist1 Exit 仍连续 10 帧丢鸟；相机到鸟距离同时降到约 592 cm。 | 径向相机偏移和旧追尾偏移接近反向；直接对两个世界位置做线性插值会沿弦穿过鸟附近，触发近裁和巨大视差。 | 相机偏移方向围绕鸟做球面插值，偏移距离单独插值；反向向量使用确定性正交轴，避免 180° 插值退化。旋转继续从实际平滑位置重算双目标边界。 | 最终 Rank11 Stylized 0 A/B：鸟丢失 0，Assist1 目标丢失 193→29，导演越界 0，位置/旋转/FOV 跳变 0，`director-ab --require-pass` 通过；Stylized 1 关键帧可见鸟/行星轮廓，颜色仍不作结论。 |
| `M11-CAM-M2-003` 【已修复节奏】首版 M2 到第 237 帧才第一次看到 Assist1，而且目标半径已经 106.38 px；前面约 7.9 秒只有鸟和无关远景球体，玩家不知道鸟飞向哪里。 | 导演资格窗口从 `Assist1Enter` 才开始，进入后还要再用 Approach 前 22% 做混合；权威 Enter 是物理作用区事件，不适合作为视觉预告的起点。 | 不改权威事件或 Stage：把 Assist1 的 `CruiseToBody` 纳入导演资格，Cruise 15% 开始 SmoothStep、50% 达满权重；Approach 入口保持 1。离线工具新增首次 Cruise 混合/目标可见帧，录制器越界判据同步允许 Assist1 Cruise。 | Rank11 fresh A/B：第 36 帧开始混合，第 55 帧首次看到 47.26 px 的 Assist1 边缘，第 70 帧可见比例 87.8%、第 80 帧完整入画；鸟丢失、导演越界和位置/旋转/FOV 跳变均为 0，Stylized 0/1 三类指纹全等。 |
| `M11-CAM-M2-004` 【已修复导演方向与连续性】提前看见 Assist1 后，鸟与行星主要保持径向上下关系，Approach→Periapsis 的拉近/拉远切换仍显生硬；画面没有 Lucy 示例中“接近在左、离开在右”的单向穿越。 | 第一版只按当前鸟—行星径向即时构图，阶段内各自生成后拉曲线；Closest 两侧虽位置连续，但缩放速度/加速度不连续，且没有从权威 Enter/Closest/Exit 冻结叙事画面基。 | 从权威事件冻结 encounter right/up；相机从径向向 encounter side 最多偏转 8°，Approach 最后 35% 只建立 10% 后拉，Periapsis 从同值、零一二阶导数继续释放剩余 90%；Cruise 最后 25% 才转入冻结 Up。离线判据增加双主体可观测时的相对 X 与最近点 ±30 帧导数。 | Rank11 Stylized1：Approach 开头相对 X `-51.29 px`、Periapsis 可观测末段 `+242.41 px`，左→右成立；最近点行星半径最大加速度 `42.11→25.20 px/s²`、jerk `463.72→48.87 px/s³`；鸟丢失 0，位置/旋转/FOV 跳变 0。 |
| `M11-CAM-M2-005` 【已修复调参误区】把侧视角提高到 30°并把 M2 相机响应从 30 降到 14 后，Assist1 窗口鸟丢失 94 帧、目标丢失 132 帧；即使侧视角降回 8°，低响应仍导致鸟丢失 84 帧。 | Rank11 权威播放速度很高，位置 lag 会迅速累积成相机—鸟大距离误差；侧视又把鸟/行星间距投影到较窄的纵向 FOV。单纯降低响应制造“电影感”会破坏双主体构图，不能替代显式导演曲线。 | 恢复 `M2FollowLagSpeed=30`，将侧视上限限定为 8°；电影感只由冻结 encounter basis 与 C2 后拉包络产生。失败录制仅保留在 `Saved` 作为调参证据，不进入最终配置。 | 同一 Rank11 再录制后鸟丢失恢复为 0，目标丢失 70，首次 Cruise 目标可见第 60 帧；单元测试同时覆盖 encounter basis 正交/时间方向、Approach/Periapsis 位置旋转与后拉量连续。 |
| `M11-CAM-M2-006` 【已修复 Lucy 假阳性】M2.1 离线报告显示 Approach 左、Periapsis 右，但逐帧画面中鸟始终沿行星盘外运动；193 个可观测帧中鸟心盘内和轮廓相交均为 0，最近边缘仍相隔 63.44 px。 | 旧门只检查相对 X 的首尾符号；冻结 basis 又把最近点径向映射到画面方向，8°侧视和实际位置 lag 将鸟—行星分离保持在盘外。即使改写目标弦，若仍在冻结世界平面计算，镜头转向也会把目标偏移压缩，形成数值设定正确、最终投影错误。 | 最近点径向改为相机深度，Up 取轨道平面法线；行星中心固定为视轴锚点，鸟沿低位投影弦从 `-1.60 R` 到 `+1.60 R`。目标弦在最终相机 Right/Up 平面内迭代求解，Cruise 画面轴与导演混合同步建立；相机置于鸟外侧以保证 `Camera→Bird→Planet`。离线门新增盘内、轮廓连续相交和前景深度判据。 | Rank11 Stylized1 fresh 录制：鸟心盘内 118 帧、轮廓相交 123 帧且最长连续 123 帧、179/179 个可观测帧鸟在前、最小中心距 `0.266 R`；Approach 开头 X `-66.36 px`、Periapsis 末段 `+295.22 px`，鸟丢失 0、Assist1 目标丢失 63，位置/旋转/FOV 跳变 0。 |
| `M11-CAM-M2-007` 【已修复 Lucy 节奏与推拉】M2.2 中鸟迅速贴到行星左缘后停驻，再在盘面前缓慢移动；行星屏占变化不足，近星点的高速感没有被镜头放大。 | Cruise 穿越 X 固定为 `-1.60 R`，Approach 使用两端零速度的 SmootherStep；固定 50° FOV 使行星峰值半径仅 136.87 px、相对 Approach 开头只增大 1.36 倍。M2 响应 30 又让实际投影滞后于单调导演曲线，接管末段仍有短暂外移。 | Cruise 从 Lead-in 起按 `-3.00 R→-1.35 R` 持续接近；Approach 用非零初速、持续加速的二次映射到 `+0.55 R`；Periapsis 快出慢收到 `+2.50 R`。FOV 在 Approach 从 50° 平滑收至 30°，Periapsis 前 80% 恢复；M2 响应提高到 60。离线工具新增稳定盘外/盘内中位速度比、峰值屏占增益和组合 `lucyPacingObserved`。 | Rank11 Stylized1 fresh 录制：第 110–223 帧约 `2.14 R→1.09 R` 持续接近；盘外/盘内中位速度 `8.34/80.79 px/s`，加速比 `9.68×`；峰值半径 238.22 px、增益 2.33×、最低 FOV 30°。鸟心盘内 100 帧、轮廓相交 114 帧、鸟丢失 0、位置/旋转/FOV 跳变 0，最大单帧旋转 10.39°。 |
| `M11-CAM-M3-001` 【开放：Handoff 几何不可兼得】把 M2.3 构图直接复用于 Assist2/3 后，两次 Handoff 会经过只剩浅蓝背景的摇摄，独立限制位置和旋转还会把鸟甩出画面；延长混合只能延长失败。 | Handoff 起点的旧行星构图与新行星构图不是相邻相机姿态。Rank11 第一次交接处，两颗行星相对当前相机约相隔 123°；在保持当前鸟像素尺度和常规 FOV 时，不存在同时容纳鸟及两颗行星的单镜头构图。世界 Transform 线性插值还会沿弦靠近鸟，位置/朝向独立限速则形成不一致位姿。 | M3 明确分离 CurrentBody 与 FramingBody，交接权重改用真实经过秒数；过渡位置围绕鸟球面插值并限制轨道角步长，朝向从实际位置重新解算鸟优先视轴。录制/离线合同升至 v7/Schema 3，目标空窗继续 fail closed；最终需要在鸟锚定有意转场、广角远景桥或显式匹配切镜中完成导演选择，禁止用放宽阈值假通过。 | 精确自动化冻结三 Assist 资格、M2/M3 互斥、Handoff-only 顺序切换和合同字段；每轮 Rank11 Stylized1 fresh 离屏录制单列 M3 鸟丢失、Handoff 当前目标丢失、最大位姿变化。只有 Rank0/11 均达到 M3 窗口鸟丢失 0、目标空窗 0、位姿/FOV 跳变 0 后才关闭本条。 |
| `M11-CAM-M3-002` 【已修复边界跳变；判据不足】Rank11 三颗行星均在 Approach 开始后约 0.75 秒突然倒回；第一颗帧 `245→246` 的鸟—行星相对 X 从 `+72.63` 跳到 `-61.69 px`。 | `M3ApproachTransitionSettleSeconds` 到期时用布尔条件关闭 Handoff 安全解算器，下一帧立即改用普通行星锚定解算器。 | 初次修复用五次平滑交叉淡化对齐 0.75 秒边界并限制最终旋转步长；该方案消除了单帧硬切，但未消除窗口内部两套构图方向相反的问题。 | `M3NoReversalFinal-20260809-191519` 的大幅单帧回跳降为 0，但后续人工复核发现 230–242 帧仍连续回摆；本条的单帧修复证据保留，完整根因由 `M11-CAM-M3-003` 关闭。 |
| `M11-CAM-M3-003` 【已修复 Approach 慢回摆与离线假阳性】第一颗行星帧 230–242 逐帧向左倒退，三颗 Assist 都有同类现象，但旧报告仍给出 `m3NoApproachReversal=true`。 | Approach 同时运行鸟优先安全构图与行星锚定 Lucy 构图，交叉淡化把一次硬切摊成长回摆；旧门只检查单帧负向变化是否超过 20 px，遗漏累计 `49.23/146.65/100.17 px` 的小步回撤。 | 安全位置解算只归 Handoff，并在 Handoff 末尾 0.50 秒完成释放；Approach 从第一帧起只运行唯一 Lucy 位置解算，安全层只限制旋转。离线工具新增历史最右位置后的 `maximumApproachBackwardExcursionPixels`，组合门以累计回撤判定。 | fresh Rank11 Stylized1 `M3ApproachContinuityFinal-20260809-200351`：三颗 Assist 的单帧负跳与累计回撤均为 0，三颗左→右成立，M3 鸟丢失 0；帧 230→242 为 `-98.02→-89.01 px`，245→246 为 `-86.12→-85.82 px`。Handoff 目标空窗仍为既有 22 帧；轨迹/计划 Hash 不变。 |
| `M11-CAM-M3-004` 【已修复：物理阶段与镜头节奏耦合】火星帧 334 已穿出但镜头持有至 415，木星约帧 454 才开始且已经贴盘；土星帧 634 进入 Handoff 后到 731 才开始偏转，最后约 15 帧集中迎合左→右穿越。 | `ResolveStage` 同时承担物理作用域和镜头叙事节奏；固定 0.40 秒 Lead-in / 0.50 秒释放只能在长度差异很大的物理 Handoff 尾部工作。上一行星 Periapsis 的剩余时间无法借给下一行星，镜头也没有显式的“离场保持/提前揭示/入口匹配”状态。首轮改造又暴露相机限速器把鸟的高速权威平移误当成绕鸟轨道步长，导致土星转场跟不上鸟。 | 保留权威 Stage 和 CurrentBody，叠加 `OutgoingHold/IncomingReveal/IncomingEntryMatch`。下一 Assist 按 Enter 前 3.25 秒反向调度，同时保证上一 Closest 后至少 0.75 秒；Hermite 揭示曲线从 `-3.00 R` 到 `-1.35 R`，末端屏幕速度解析匹配 Approach。OutgoingHold 保持上一行星构图并把鸟平滑收向画面中心；IncomingReveal 先完整携带鸟的权威平移，只对绕鸟偏转限速。录制合同升 v8、CSV 升 Schema 4。 | fresh Rank11 Stylized1 `M3ShotStateFinalC-20260809-211423`：木星/土星 ShotPhase 分别从帧 357/648 开始，首次可见帧为 368/666；土星 648–746 最大旋转 `9.069213°`、10° 限制命中 0。M3 鸟丢失、位置/旋转/FOV 跳变和三颗 Approach 累计回撤均为 0，三颗左→右成立，`m3ShotPlanPassed=true`。新目标仍有 11/18 帧几何空窗，`m3HandoffPassed=false`，继续由开放项 `M11-CAM-M3-001` 跟踪。 |
| `M11-CAM-M3-005` 【已修复：首目标获取延迟】Rank11 发射后约 50 帧看不到火星；旧证据第 52 帧才擦入、第 70 帧才完整可见。 | M3 ShotPlan 只遍历 Assist2/3，Assist1 继续使用旧 M2 Cruise 百分比 Lead-in。首段约 7.3 秒时，`0.15` 起始比例制造约 1.1 秒零导演权重；火星第 50 帧已有 46.5 px 半径，但球心仍在画面上方，排除“目标太小”。 | 把发射点建模为虚拟 `LaunchAnchor`，Assist1 从 playback 0 进入 `IncomingReveal → IncomingTrack → IncomingEntryMatch`；零权重保留发射构图，首段鸟锚定桥固定 0.40 秒释放，后续仍按全段 Hermite 轨迹匹配 Approach。录制合同升 v9、CSV Schema 5；离线门按秒要求 0.10 秒内启动混合、0.75 秒内首见、1.00 秒内完整入画。 | Development Editor 编译通过；fresh NullRHI `ABTS.M11C.Unit.FlightCameraAuthorityFrame`、`ABTS.M11C.CameraCapture.Config` 各精确 1/1 成功。fresh Rank11 Stylized1 `M3LaunchAcquire-20260810-111225`：第 3 帧（`0.033 s`）启动混合，第 7 帧（`0.167 s`）火星首见，第 8 帧（`0.200 s`）完整入画，首段鸟丢失 0，`m3FirstBodyAcquisitionPassed=true`；M3 导演窗口位姿/FOV 跳变均为 0，轨迹/计划 Hash 不变。既有 Handoff 目标空窗继续由 `M11-CAM-M3-001` 跟踪。 |
| `M11-CAM-M3-006` 【已修复空窗；连续位姿门仍开放】火星→木星和木星→土星之间出现只剩鸟的空镜；直接延长 Transform 混合会在大夹角下把鸟甩出画面。 | Rank11 首次交接两行星相对旧相机约隔 123°；50° Lucy 近景没有三主体解。位置与朝向独立插值还会形成不一致相机位姿，四元数短弧并不等价于主体构图连续。 | 新增 `DualBodyBridge`：85° 三包围球拟合，上一行星固定在左、下一行星固定在右；桥后用鸟＋下一行星双主体拟合推入，`IncomingEntryMatch` 跨 Enter 保持 0.50 秒。合同 v10/Schema 6 逐帧记录两颗桥接行星并新增独立门。历史版本曾用鸟体 `1×→10×→1×` 做桥接可读性补偿；该补偿在拖尾阶段撤销，鸟体恢复恒定原始 Scale。 | fresh Rank11 Stylized1 `M3DualBodyBridgeFinal4-20260810-133000`：桥接 36 帧、双行星同框 36/36、零行星帧 0、Handoff 目标丢失 0、M3 鸟丢失 0、Approach 回撤 0，`m3DualBodyBridgePassed=true`。桥两端仍超过旧位姿/FOV 单帧阈值，故 `m3HandoffPassed=false`；不得据此关闭 M3-B/M3-C。撤销倍率后的像素结果由拖尾阶段 T2 重新验收。 |
| `M11-CAM-M3-007` 【已修复：跨状态相机基底与入口匹配不连续】逐帧复核显示多处结构性跳变：222→223 `IncomingEntryMatch→Authority/Approach` 时鸟回退约 20 px；356→357 相机约 1274 m/29.4°；374→375 约 2242 m/171.8°；468→469 约 1097 m/129.1°。木星→土星同路径复现 1752–2744 m/96.6–178.6°，并曾让鸟缩至 0.90 px。 | 旧状态机在边界更换不相容的解算基底：上一行星局部构图、两行星连线三主体拟合、鸟—下一行星双主体拟合各自决定 Transform；`IncomingReveal` 因判断次序不可达，`EntryMatch/PostEnterSettle` 没有匹配 Authority Lucy 构图，Authority 首帧又直接释放累计跟随误差。 | 改为共享连续链：Outgoing 从上一 Lucy 解连续拟合鸟＋双星，Bridge 保持同解，Reveal 在三主体同框下预转到下一 Lucy 朝向，Track 沿同一朝向推入 Lucy 解，EntryMatch 与 Authority 共用端点 Transform/FOV。阶段使用局部进度；删除 PostEnterSettle；跨行星和导演完全接管后的首目标构图直接消费完整约束 Transform；桥接符号确定化。鸟体始终保持原始 Scale。自动化冻结五个状态边界的位置、旋转、FOV 端点相等。 | Development Editor `-ForceUnity -NoHotReloadFromIDE` 完整链接成功；fresh NullRHI `ABTS.M11C.Unit.FlightCameraAuthorityFrame` 精确 1/1 成功（`Saved/Logs/M11-CameraState-R5-20260810-205839-FreshAutomation.log`）。fresh Rank11 Stylized1 `M3StateContinuityR5-20260810-205922` 为 949 帧 `Complete/TargetHit`，AVI SHA-256 `23C46A89B7C013932DE9CFFBBD6E58DCB42BDC5151EDF12A1D78AD53F21184F9`；`222→223` 鸟 X `516.52→517.44 px`，M3 鸟丢失 0，Approach 最大累计回撤均小于 1 px，桥接 36/36 双星同框；16 个状态边界均低于 `1001 cm/0.98°` 且 FOV 连续。旧总报告仍因 M4 失败和固定世界位移阈值为 false，不冒充整体 M3 绿灯。 |
| `M11-CAM-M3-008` 【已修复：双星退出后仍延迟匹配下一颗穿越入口】状态边界连续后，木星段帧 431 的鸟仍先到画面右下，再于 439 回到左侧；土星段 712→730 同样出现晚回拉。 | `IncomingTrack` 才开始用对称 SmoothStep 从三主体远景位置匹配 Lucy 位置，朝向又由 Reveal/Track 各自控制。匹配主要变化后置，物理屏幕运动先把鸟送过入口，末段相机才集中纠正；状态本身连续，但缺少跨 Reveal/Track 的共享屏幕构图时序。 | 从 `DualBodyBridge` 退出首帧启动统一 Match，覆盖完整 `IncomingReveal + IncomingTrack`；二次 Ease-out 后接 SmoothStep 前置主要转换，位置、桥→Lucy 朝向和 `85°→50°` FOV 共用同一 Alpha，EntryMatch 继续保持精确 Lucy 端点。 | Development Editor `-ForceUnity -NoHotReloadFromIDE` 成功；fresh NullRHI `ABTS.M11C.Unit.FlightCameraAuthorityFrame` 1/1 成功（`Saved/Logs/M11-CameraTrackMatch-R5-20260810-FreshAutomation.log`）。fresh Rank11 Stylized1 `M3TrackMatchR5-20260810-213900` 为 949 帧 `Complete/TargetHit`，AVI SHA-256 `2BAE60A3C47E26FA2E1336477372C613F48E54D709F5F31E3888ABF490251CCB`；木星旧 `431→439 = 706.30→526.65 px` 改为 `525.75→526.65 px`，土星旧 `712→730 = 639.58→525.86 px` 改为 `534.28→525.79 px`。逐帧目检确认两段从双星退出即逐渐进入下一行星左侧穿越入口，未再先越过右侧后回拉。 |
| `M11-CAM-M3-009` 【已修复：提前匹配穿越入口后丢失上一行星渐退镜头】`M3TrackMatchR5` 消除了鸟的晚回拉，但相较 `M3StateContinuityR5`，双星桥接退出后上一颗行星已在视野外，丢失了 413→421 一带逐渐后退离场的优秀表现。 | 008 的统一 Match Alpha 同时驱动位置、桥→Lucy 朝向和 `85°→50°` FOV，并从 `IncomingReveal` 首帧前置；它把下一颗入口匹配和上一颗退场绑定为同一速度，位置/焦段过早向下一颗近景收敛。 | 解耦两项艺术职责：`IncomingReveal` 保持 85° 三主体宽景并只预转 Lucy 朝向；`IncomingTrack` 固定该朝向，以二维鸟 NDC 锚包络解析相机 Right/Up，以独立慢包络控制 Forward 深度和 `85°→50°` FOV。EntryMatch/Authority 继续共享精确 Lucy 端点，不恢复晚回拉。 | Development Editor `-ForceUnity -NoHotReloadFromIDE` 成功；fresh NullRHI `ABTS.M11C.Unit.FlightCameraAuthorityFrame` 1/1 成功（`Saved/Logs/M11-CameraEgressAnchor-R5-20260810-FreshAutomation.log`）。fresh Rank11 Stylized1 `M3EgressAnchorR5-20260810-222300` 为 949 帧 `Complete/TargetHit`，AVI SHA-256 `7CAF922ABAC238EA67427A198FE27CED52033CA46C49E512E7F8F5F72165432A`；火星在 413–420 帧、木星在 644–667 帧连续缩退离场，鸟在两次交接窗口最大单帧屏幕位移为 `10.18/7.40 px`，M3 鸟丢失 0，`m3NoApproachReversal=true`。逐帧目检确认保留退场表现且两段直接进入下一颗 Lucy 左侧入口。 |
| `M11-CAM-M3-010` 【已修复：尚未穿出行星轮廓即开始脱镜】Rank11 火星第 328 帧、木星第 548 帧刚越过 Closest，鸟仍位于行星盘内，镜头却已经开始拉远并进入双星桥接准备。 | `MinimumDepartureHoldSeconds` 只限制 Reveal 起点；旧 `OutgoingStart=max(Closest, Reveal-Release)` 会被钳回 Closest。`OutgoingHold` 又从首帧同时改变 FOV 和相机 Transform，因此物理 Periapsis 一开始就抢走 Lucy 前景穿越构图。 | 在 `Closest→Exit` 上增加默认 `0.23` 的归一化前景清轮廓门，门前保持 Authority；门后 Outgoing 使用五次 SmootherStep。调度器硬保留最少 `0.50 s` 拉远、`0.60 s` Bridge、`1.30 s` Reveal、`0.60 s` Track、`0.50 s` Entry，并在事件预算不足时 fail closed；窗口充足仍保留首选 2 秒拉远。 | Development Editor `-ForceUnity -DisableAdaptiveUnity -NoHotReloadFromIDE` 7 actions 成功（`Saved/Logs/M11-ForegroundClear-R6d-ForceUnity-20260810-UBT.log`）；fresh NullRHI `ABTS.M11C.Unit.FlightCameraAuthorityFrame` 1/1 成功（`Saved/Logs/M11-ForegroundClear-R6d-20260810-FreshAutomation.log`）。fresh Rank11 Stylized1 `M3TransitClearR6D-20260810-230300` 为 949 帧 `Complete/TargetHit`，AVI SHA-256 `5E18D6FA1EC598F4468E6E3CDBA944FF76F75AC90DE501B3DEB44D3BAFCA1B5D`；328/548 保持 Authority，348/568 才进入 Outgoing，整个拉远段最小包围圆净空 `2.99/22.83 px`，两次桥 36/36 双行星同框，既有渐退和直飞入口表现保留。总离线报告仍受既有全局/M4 与远景鸟像素门影响，不冒充整体 M3 放行。 |
| `M11-CAM-M3-011` 【已修复：双星桥接准备阶段鸟体无叙事意图地抬升】木星→土星 575–625 帧中，鸟从画面 Y `436.54` 抬到 `276.23 px`，最大单帧上移 `19.03 px`；两颗行星需要位于屏幕两端，但这并不要求鸟被抬到画面上部。 | 双星 Bridge 只固定行星连线为 Camera Right，剩余 Up 沿 transported Up；三主体位置使用世界算术均值后只解 Forward fit distance，没有鸟的屏幕 Y 约束。鸟在第二段 planet-line 的 Up 正侧很远，Outgoing 向该 Bridge rotation/centroid 收敛时，相机贡献约 192 px 上抬，物理轨迹反而向下。 | 为 `OutgoingHold→DualBodyBridge→IncomingReveal` 增加共享 `BirdNdcY=+0.05` canonical 锚：Outgoing 五次平滑获取、Bridge 持有、Reveal 平滑释放。只平移 Camera Up，不改变双星水平轴和鸟 X；锚后对三投影球按 `1.15` margin 重新验算，必要时沿 `-Forward` 二分后退。Outgoing 下一星按 IncomingFitAlpha 从零半径虚拟主体连续生长，避免提前强制入画。 | Development Editor ForceUnity 无警告完整链接成功；fresh NullRHI `ABTS.M11C.Unit.FlightCameraAuthorityFrame` 1/1（`Saved/Logs/M11-BridgeBirdAnchor-R7b-20260811-FreshAutomation.log`），新增非共面鸟 Y 锚、双星水平和三球 margin 判据。fresh Rank11 Stylized1 `M3BridgeBirdAnchorR7-20260811-114000` 为 949 帧 `Complete/TargetHit`，AVI SHA-256 `2838D7F9E23D9B6F77E04A53AACF69D477A20DFAD08F9626810A568775943C09`；575–625 范围降至 `65.70 px`、最大步长 `2.31 px`、无方向反转，两次桥 36/36 双星完整同框，Outgoing 鸟/离场行星 visible ratio 全程 1。全局/M4 与远景鸟像素门仍独立开放。 |
| `M11-CAM-M3-012` 【已修复：开局 Reveal→Track 轻微后退跳变】R7 第 40→41 帧由 `IncomingReveal` 进入 `IncomingTrack` 时，鸟屏幕 X 从 `523.37` 回到 `511.83 px`，而行星基本不动；R6D/R5 亦有同类边界回退，故不是 011 鸟 Y 锚的回归。 | Reveal 末帧的开局鸟携带限制器向标准 lag 后的相机位置释放；Track 首帧导演权重到 1 后绕过限制器，直接采用精确导演 Transform。两条路径终点不同，近鸟视差把约 70–80 cm 残差放大为约 11.54 px 回退。 | `LaunchAnchor→Assist1` 的非跨行星 incoming 全段保持同一限制器；位置从安全携带解直接释放到精确导演位置，旋转响应也随 release alpha 收敛到 1。新增纯数学端点合同：alpha 0 精确等于安全位置，alpha 1 精确等于导演位置；跨行星链、Authority 和双星鸟 Y 锚不变。 | Development Editor ForceUnity 成功；fresh NullRHI `ABTS.M11C.Unit.FlightCameraAuthorityFrame` 1/1（`Saved/Logs/M11-LaunchBoundary-R8-20260811-FreshAutomation.log`）。fresh Rank11 Stylized1 `M3LaunchBoundaryR8-20260811-134500` 为 949 帧 `Complete/TargetHit`，AVI SHA-256 `99967DB08C2CC3A2ADC97FAF40613C137BD2DAB0B037F7D61C1C2789298AB039`；40→41 鸟 X 步长降为 `-0.069 px`，相机步长 `528.33 cm` 与邻帧连续；41 帧后与 R7 数值等价，222→223 仍同向 `+0.925 px`。 |
| `M11-CAM-M5-001` 【已修复：认证 Rank0 的镜头阶段被加速回放压缩】M5 首轮 Rank0 Stylized1 虽完成物理接触，但两个 Handoff 各连续 10 帧鸟离框；阶段只在下一颗 Enter 前很短时间启动，Bridge/Reveal/Entry 各被压到约 1–3 个输出帧，最大单帧旋转 `144.48°`、FOV 步长 `17.90°`。Rank11 没有该现象。 | 认证路线用 `PlaybackTimeScale=18` 推进轨迹秒，而 M3 的 2 秒拉远、0.6 秒桥接、1.3 秒取得、0.6 秒 Track 和 0.5 秒 Entry 也被错误地当作轨迹秒。状态机因此在 18 倍呈现中整体缩短；长 Handoff 前段仍由已切到下一星的 Authority 接管，鸟先飞出画面，导演链才在临近 Enter 时仓促启动。 | 明确 ShotSettings 时间字段为呈现秒，并用唯一 `BuildPlaybackClockSettings()` 按有效 PlaybackTimeScale 同比换算到 Stage Resolver 的轨迹钟；`ForegroundTransitClearProgress` 保持物理归一化值。生产相机和录屏观测器共用换算结果，非法倍率 fail closed。候选倍率 1，Rank11 已验收镜头数值不变。M5 正交比较只在同为 TelemetryOnly 的 Stylized 0/1 间进行；SceneCapture 与 NullRHI 因 Skeletal Bounds 更新时机不同，不直接比较 Camera Hash。 | UE 5.8 ForceUnity 完整链接成功（`Saved/Logs/M11-M5-PresentationClock-ForceUnity-20260811-UBT.log`）；fresh NullRHI `ABTS.M11C.Unit.FlightCameraAuthorityFrame` 1/1（`Saved/Logs/M11-M5-PresentationClock-FlightCamera-Fresh.log`）。Rank0 R3 正式录像 1140 飞行帧鸟丢失/空构图 `0/0`，最大旋转 `10°`、FOV 步长 `3.48°`，800 cm 接触和 M4 闭合通过。Rank0、Rank11 的 Stylized 0/1 Telemetry 各自 Released/Plan/Stage/Camera Hash 全等；正式证据与 AVI Hash 见 `Docs/M11FinaleCameraM5RenderOrthogonality.md`。 |
| `M11-CAM-PIE-001` 【已澄清并加诊断：PIE 仅第一颗行星使用导演镜头】Rank0 PIE 中第一颗行星按新构图穿越，随后恢复旧飞行镜头。 | 本次日志显示先执行 `M2.Enabled 0`，随后误执行 `M2.Enabled 1`，且没有执行 `M3.Enabled 1`。M2 合同只覆盖 Assist1，发射时冻结为 `M2=1/M3=0` 后，该现象正是预期 A/B 行为；Rank0 后续 Assist 事件和 M3 状态机没有丢失。 | 不把 M2 隐式升级为 M3，保留验收开关语义。PIE 完整终局镜头统一在发射前设置 `M2=0, M3=1`。Flight Camera 发射时新增 `DirectorFrozen` 模式摘要；检测到 M2-only 时输出 `M2OnlyFirstAssist` 警告和正确命令。 | 2026-08-11 原 PIE 日志 `Saved/Logs/AngryBirdsToSpace.log` 第 2382–2385 行只有 M2 的 0→1 命令，第 2451–2453 行确认 Rank0 正常 Release/Follow。UE 5.8 ForceUnity 6 actions 完整链接成功；fresh `FlightCameraAuthorityFrame` 1/1（`Saved/Logs/M11-PIE-DirectorModeDiag-20260811-FreshAutomation.log`）。Rank0 M3 Telemetry 为 1140/1140 帧 M3、800 cm 接触（`M11-PIE-DirectorModeDiag-Rank0-20260811.log`）；M2-only 对照准确输出冻结摘要与警告（`M11-PIE-DirectorModeDiag-M2Only-20260811.log`）。最终可见 PIE 仍需用户按正确开关复测。 |
| `M11-CAM-M7-001` 【已修复：自定义 F4 路线被镜头预算误判为玩法失败】玩家在 `Path qualified` 后发射，约 9.4 秒后触发 `FlightCameraDirectorSampleRejected`，黑屏恢复；三组不同 Release Hash 均复现。 | 交互层每帧从可变的 `LatestQualifiedResult` 解析镜头；固定 M3 时长在某些自定义 F4 的短 Handoff 中无法同时满足轮廓清空与后续阶段预算，Resolver 返回 Unavailable。表现层拒绝又错误调用 `FailInteraction`，把有效 F4 播放转换成玩法失败。 | M7 发射时复制 Released Result 并预构建带 Hash 的专属 ShotPlan；短窗口同比压缩 Outgoing/Bridge/Acquire/Track/Entry，保持严格次序与端点连续。所有导演 sample 构造拒绝统一只记一次 fallback，禁止改变玩法状态。录屏合同 v15 增加完整自定义 `Yaw/Pitch/Power` 输入。 | 最终 ForceUnity 链接成功；fresh FlightCamera/Config/RandomF4Witnesses 均 1/1。随机种子 `0x4d3757a1` 的两条 Rank11 Stylized1 最终录屏 `M7CustomF4-A-Final-20260811-183000` / `M7CustomF4-B-Final-20260811-183500` 分别 1014/995 帧，Released Hash `0x668901FF090C69FE` / `0xCC31B4566FD9728F` 且 CameraPlan Hash 完全一致；均 `Complete/PhysicalContact/M4Closure`、终点 800 cm、Unavailable 0、无 fallback。AVI SHA-256 `94A7B3...AFFFA` / `FACD9E...709CA`；Rank11 仍为 UNCERTIFIED。 |
| `M11-CAM-M4-001` 【已修复：FinalApproach 未建立 UFO 构图】Assist3 Exit 后鸟出现大角度跳变，FinalApproach/Terminal 中 UFO 全程出画，部分帧连鸟也丢失。 | Director 只把阶段标签改成 `FinalApproach/UFO`；`IsM3AssistWindow` 排除了 UFO，Flight Camera 因而退出 M3 约束构图并恢复终点附近快速变化的瞬时轨迹切线跟随。TargetHit 后又只冻结最后一个错误 Transform。镜头状态还把候选合格截获和物理接触都压成同一个 `TargetHit`。 | M4 在 Assist3 轮廓清空后调度 `TerminalAcquire`：前半从土星 Lucy 连续进入鸟＋土星＋UFO 的 85° 三主体桥，后半收束到冻结终端基底的鸟＋UFO 双包围球 fit；两段均使用零端斜率。FinalApproach/Terminal 共用 55° FOV、1.20 margin 的同一解，不缩放鸟体。新增 `CandidateQualified/PhysicalContact` 单值终点权威及 v11/Schema 7 M4 离线门。 | Development Editor ForceUnity 完整链接成功；fresh NullRHI `ABTS.M11C.Unit.FlightCameraAuthorityFrame` 与 `ABTS.M11C.CameraCapture.Config` 均 1/1 通过（`Saved/Logs/M11-M4-UFOTerminalBridge-Automation.log`、`M11-M4-CaptureConfig-Automation-R3.log`）。fresh Rank11 Stylized1 `M4UFOTerminalBridgeR2-20260811-124700` 为 949 帧 `Complete/TargetHit`，AVI SHA-256 `B3D8A8444CF4BDDC2ACE14CA8024FB2EBBF9BCDA718E8C905560C3D92E6DA509`；Acquire 56 帧目标空窗 0，终端 51 帧鸟/UFO/身份丢失均 0，位置/旋转/FOV 跳变 `0/0/0`，`m4TerminalClosurePassed=true`。本次终点为未认证候选 `CandidateQualified`，不是物理接触。 |
| `M11-CAM-M4-002` 【已修复：候选离线录屏停在合格包络，未撞到 UFO】首轮 M4 镜头能持续看见鸟和 UFO，但 Rank11 最终鸟心至 UFO 仍约 41,250 cm，画面在隔空处进入 `TargetHit`。 | 编辑器候选模式统一调用 `BuildCandidateQualified()`，该路径逐点复制 Released Result 后立即结束。41,250 cm 是 F4 搜索的合格截获包络，不是 800 cm UFO 解析接触球；镜头虽正确跟随播放终点，却没有可消费的接触段。 | 新增未认证候选专用 `BuildCandidatePresentationContact()`：原始 Released 前缀和 Hash 逐样本保持不变，从合格点的位置/速度/估算加速度构造确定性五次曲线，追加显式 `VisibleTerminalTransfer` 并到达来向侧 800 cm 接触点。转移要求中心距离单调减小、不得提前入球、最终误差不超过 `1e-3 cm`、行星 clearance 与候选表现加速度/jerk 门通过，否则 fail closed。合格历史与物理接触可依次同时为真，单值终点 Authority 取 `PhysicalContact`；录制合同升 v12。 | Development Editor ForceUnity 成功；fresh NullRHI `ABTS.M11C.V2_1` 3/3、Capture Config 1/1、FlightCamera 1/1（最终日志 `M11-M4-CandidateContact-V21-Final.log`、`M11-M4-CandidateContact-CaptureConfig-Final.log`、`M11-M4-CandidateContact-FlightCamera-Final.log`）。fresh Rank11 Stylized1 `M4PhysicalContactR4-20260811-133500` 为 1021 帧 `Complete/TargetHit`，AVI SHA-256 `98E576BE5555C52859EF0E3E93B855D8CEA7587703F7651D10C0BFF3301190D3`；转移 `30.6807317466→33.0834287947 s`，最终 Authority 距离 `799.9999999999568 cm`，鸟/UFO/身份丢失和 M4 位置/旋转/FOV 跳变均为 0，两个 M4 门均通过。Released Hash 保持 `0x505F3312AC8AE07F`，Playback Plan Hash 变为 `0x0F0E8466ED5BD3AD`，Authority 仍是 `UNCERTIFIED`。 |
| `M11-CAM-M4-003` 【已修复：终端鸟/UFO 对称质心构图缺少冲击感】旧终端镜头虽能同时容纳鸟和 UFO，却让两者围绕共同质心漂移；UFO 没有稳定叙事锚，鸟的屏幕逼近速度也随世界几何和 fit 距离变化。 | 旧 `FinalApproach` 每帧只做鸟＋UFO 对称包围球 fit，只有“装进画面”约束，没有 UFO 中心锚、鸟屏幕轨迹合同或相机到鸟距离曲线；事件解析器对 FinalApproach 的阶段进度又长期给出 0，无法驱动完整终端 dolly。 | 录制合同升 v13：以 Assist3 Exit→PlaybackPlan 物理接触终点建立真实终端时间轴；解析相机把 UFO 固定 NDC `(0,0)`，鸟沿中央下方从 NDC Y `-0.42→-0.22` 线性运动，相机到鸟距离用五次 SmootherStep `40000→5000 cm`。TerminalAcquire 后半直接匹配 progress 0，Terminal 冻结 progress 1；全程不缩放鸟、不改 Released 轨迹或接触曲线。 | UE 5.8 ForceUnity 完整链接；fresh NullRHI FlightCamera/Config/V2_1 为 1/1、1/1、3/3。fresh Rank11 Stylized1 `M4CenteredDollyR9-20260811-160100` 为 1021 帧 `Complete/TargetHit`，AVI SHA-256 `D74E62DD9AD5745CDBAAEE429291C72F7E37BD4DF9A35A912F6177DCB8D33CDA`；UFO 中心最大误差 `0.000011 px`，鸟中央线偏差最大 `1.98 px`，相机到鸟约 `40014.64→5009.14 cm`，M4 位姿/FOV 跳变 `0/0/0`，最终 Authority 距离仍为 `799.9999999999568 cm`，两个 M4 门通过。 |
| `M11-TRAIL-001` 【历史线带基线已完成；当前实现由 002 替代】鸟在普通飞行没有持续拖尾，历史桥接期仅以 10 倍鸟体补偿远景可读性，导致效果只在镜头切换期突变且污染主体尺寸。 | 可读性被错误地绑在 ShotPhase 和 BirdVisual Scale 上；项目又没有一条独立于候选、镜头状态和 Stylized 的主控鸟演出轨迹历史。 | 当时新增 M11 自有、无项目资产的 `UABTSM11FinaleBirdTrailComponent`：发射全程消费最终鸟世界坐标，保留 0.40 秒/最多 48 点历史，以世界深度测试的双层屏幕空间线带绘制；增加 `abts.M11.FinaleBirdTrail.Enabled` A/B 开关。删除桥接倍率参数和状态缩放逻辑，世界恢复时显式恢复原始 Scale。不修改共享 Build.cs、Niagara、材质、Config、Blueprint 或地图。首录 0.90 秒尾迹在 UFO 终段跨越整屏，故依据像素证据收敛寿命和线宽，不改镜头状态。 | Development Editor 编译成功；fresh NullRHI `ABTS.M11C.FinaleBirdTrail.History` 精确 1/1 成功。fresh Rank11 Stylized1 `M3TrailT2Final-20260810-150000`：合同 v10、949 帧、`Complete/TargetHit`、RenderVersion 19、云关闭；普通段、三次穿越、两次桥接和 UFO 终段均有尾迹，桥接鸟仅约 `0.34–0.55 px` 时仍可读，轨迹/计划 Hash 不变。当前路线始终盘前，故盘后深度遮挡尚无像素样本；未来出现盘后轨迹时补回归。镜头 7/10/19 个位置/旋转/FOV 跳变帧仍由 `M11-CAM-M3-007` 跟踪。 |
| `M11-TRAIL-002` 【按帧点粒子历史基线已完成；当前由 003 替代】线带逐帧观察呈现明显分节和周期闪烁：鸟与最近线段之间会先拉开，再约每 12 帧补齐一段；每次补段后尾迹整体偏亮，补段前偏暗。 | 0.40 秒寿命在 30 fps 下对应约 12 帧，历史点成批进入/退出；旧实现又用 `RemoveAllSwap` 删除过期点，破坏时间顺序，而渲染仍把数组相邻项连线。新点加入时邻接关系、线长和整段 Alpha 同时变化，因此“补段”“闪亮”和错误回连共用同一根因，不是录屏掉帧或三渲二轮廓闪烁。 | 将同一 M11 组件改为项目资产无关的点状粒子池：每帧以当前鸟位置为中心发射 4 点，按稳定序号施加 `4.2–14 cm` 的三维确定性扰动及轻微尺寸变化；粒子生成后固定于世界空间，各自按 0.40 秒独立变暗并缩小。最终使用 UE 5.8 `PDI->DrawPoint` 绘制 3 px 暖橙外点＋1.25 px 亮金核心，年龄数组只做稳定前缀裁剪，硬上限 192；不修改镜头、轨迹、Rank、Stylized、共享 Build.cs 或项目资产。探索版 `DrawSprite` 在正式 SceneCapture 主通道不可见，`DrawPoint` 首版 8 px 又过大，均已由最终参数替代。 | Development Editor 增量编译成功（`M11-PointTrail-DevV4-20260810-UBT.log`），最终 `-ForceUnity -DisableAdaptiveUnity` 7 actions 成功（`M11-PointTrail-Final-ForceUnity-20260810-UBT.log`）。fresh NullRHI `ABTS.M11C.FinaleBirdTrail.History` 精确 1/1 成功（最终 `M11-PointTrail-Final-History-20260810.log`），冻结单簇数量、扰动半径、离轴厚度、重置可复现、年龄顺序、过期与容量上限。fresh Rank11 Stylized1 `M3PointTrailT2P-V4-20260810-162000` 为 `Complete/TargetHit`、949 帧，AVI SHA-256 `797C67E691C5D5E893FE0ED7201658CFE0B9CDA95E115AE89C6073C9AB2B1733`；终段 906–918 显示独立点列逐点退出，轨迹/计划 Hash 不变。镜头 7/10/19 跳变仍由 `M11-CAM-M3-007` 跟踪；该版本后续暴露的按帧空间/时间混叠由 `M11-TRAIL-003` 继续处理。 |
| `M11-TRAIL-003` 【主要采样/时序混叠已修复；DrawPoint 密度与外观待主观定型】按帧点粒子在近景形成固定间隔的“打点计时器”：同帧 4 点在鸟后同一中心扭动；当拖尾每帧屏幕位移接近相邻簇间距时，旧粒子又被外观相同的新粒子替换到近似同一屏幕位置，产生静止残影。 | 发射器把“一帧”当成一个纵向采样站点，同站点粒子共享出生时刻；球形扰动只加厚粒子团，不能填补纵向间隔。严格等距弧长虽能消除帧率密度差，仍可能与输出帧率周期锁相；还必须破坏纵向格点周期并按真实亚帧时刻老化。 | 改为跨帧保留距离余数的世界弧长重采样；64 cm 名义分层内使用确定性一维蓝噪声位置抖动，相邻间距默认 38.4–89.6 cm，每站点只生成 1 点。按线段比例赋 `(1-t)Δt` 初始年龄与横移位移。横向速度在轨迹法平面服从二维正态，径向 `3σ`/硬上限为鸟速 1/5；寿命随横向速度从 0.40 s 降至 0.18 s，形成密实慢核和稀薄外逸层。保留 3 px/1.25 px `DrawPoint`，总数上限 384、单次发射保护 96；异常大位移清空重建，不跨跳变补粒子。 | Development Editor 与 `-ForceUnity -DisableAdaptiveUnity` 6 actions 成功；fresh NullRHI `ABTS.M11C.FinaleBirdTrail.History` 1/1 成功（`Saved/Logs/M11-ArcTrail-Final-20260810-172912-FreshAutomation.log`）。fresh Rank11 Stylized1 M3 `M3ArcTrailT2Q-20260810-173359` 为 `Complete/TargetHit`、949 帧，启动到退出 50.67 秒、采集区间 20.382 秒，AVI SHA-256 `7230C4C20FE3471A1C59B042B0727EF52456298712A03C68680C532361D8FF1A`；未触发发射保护，轨迹/计划 Hash 不变。38→39、50→51、329→334 未复现 12 帧整批补段、同步闪亮或原地粒子团，远景桥接呈收束彗尾；近景仍是稀疏橙色方点列、亮度层次偏弱，故只关闭主要混叠根因，保留表现调参。镜头状态仍冻结并由 `M11-CAM-M3-007` 跟踪。 |
| `M11-TRAIL-004` 【已修复方块外观；亮面可读性由 005 继续处理】弧长蓝噪声已经消除主要时序混叠，但 `DrawPoint` 在近景仍显示为橙色方点，无法读成独立尾迹光点；历史探索曾据一次不可见结果判断动态 Sprite 不进入正式 SceneCapture。 | `DrawPoint` 的底层屏幕 quad 没有径向透明轮廓，缩小只能得到更小方块。历史 Sprite 探索没有同时满足运行时纹理资源、透明相关 ViewRelevance 和加法混合的 RGB 衰减合同；一次失败不足以证明 SceneCapture 不支持 `PDI->DrawSprite`。 | 保留 T2-Q 的粒子历史，只把默认 `RenderMode=1` 表现替换为纯 C++ 运行时 `64×64` 径向纹理＋Halo/Core 双层 `PDI->DrawSprite`。纹理 RGB/Alpha 同时径向衰减以适配 One/One 加法混合；每 View 按投影换算恒定屏幕像素直径，Core/Halo 独立年龄曲线并轻微扩散。九个外观 CVar 支持命令行 A/B；`RenderMode=0` 保留旧点模式。纹理创建失败则 fail closed，不创建材质、Niagara、`.uasset`、Blueprint、地图或 Build.cs 改动。 | Development Editor 无警告构建成功；最终 `-ForceUnity -DisableAdaptiveUnity` 6 actions 成功（`Saved/Logs/M11-SoftSprite-Final-ForceUnity-20260810-UBT.log`），默认值与九项非默认 CVar 的 fresh NullRHI 均为 `ABTS.M11C.FinaleBirdTrail.History` 1/1（`M11-SoftSprite-Final-20260810-FreshAutomation.log`、`M11-SoftSprite-CVarSmoke-20260810-FreshAutomation.log`），后者 `Begin` 日志确认全部覆盖值实际生效。fresh Rank11 Stylized1 M3 `M3SoftSpriteT2R-20260810-182551` 为合同 v10、`Complete/TargetHit`、949 帧、采集区间 21.852 秒，AVI SHA-256 `6F46637CC422CC13064D523EF2FD9584888E207270A327A413A713CCFE062BA5`；日志确认 `RenderMode=1`、纹理 64×64。38/39、245、329–331 的矩形边缘消失，430/700 仍为连续彗尾，906–948 分别衰减退出。轨迹/计划 Hash 不变；镜头状态继续由 `M11-CAM-M3-007` 跟踪。 |
| `M11-TRAIL-005` 【已修复并验收：亮色行星表面的 Sprite 核心对比不足】全加性软 Sprite 在黑色星空上清晰，但飞过火星、木星、土星亮面时趋近背景白色，812 帧附近几乎不可辨；只增大核心和不透明度的 A/B 没有建立稳定边界。 | 全加性 Halo/Core 只能增加辐亮度，背景接近曝光上限后没有可用亮度余量；首轮混合版虽增加 AlphaComposite Core/Shell，却先画 Shell 再画 Halo，后画的加性柔光重新抬亮了窄对比区。问题不在粒子采样、三渲二轮廓或轨迹历史。 | 保持 Halo 为加性混合，把核心改成预乘 RGB 的琥珀 `SE_BLEND_AlphaComposite`，并在两者之间加入深色预乘 Contrast Shell；最终绘制顺序固定为 Halo→Shell→Core，默认直径 `5.5/4.4/3.0 px`，Halo 强度 `0.30`、Shell 不透明度 `0.45`、Core 强度/不透明度 `0.95/0.80`。新增三项命令行 CVar，总计十二项外观参数；同一 64×64 运行时纹理复用三次，不创建资产或修改共享 Build.cs。 | `-ForceUnity` 4 actions 成功（`Saved/Logs/M11-HybridContrast-v2-ForceUnity-20260810-UBT.log`）；fresh NullRHI `ABTS.M11C.FinaleBirdTrail.History` 精确发现 1 项、通过 1/1（`Saved/Logs/M11-HybridContrast-v2-20260810-FreshAutomation.log`），冻结 `Halo > Shell > Core`、两项合成不透明度和十二个 CVar。fresh Rank11 Stylized1 M3 `M3HybridContrastV2-20260810-195545` 为合同 v10、`Complete/TargetHit`、949 帧、采集区间 20.298 秒，AVI SHA-256 `905A37B67687416C55B0B489D324A628AFD9575D44877568260CE5713298B452`，轨迹/计划 Hash 不变。316–322、554–560、809–815 旧/新序列显示亮面点获得暖色核心与轻微暗边，暗面仍发光，未出现黑珠串或逐帧闪跳；2026-08-10 用户主观验收通过。镜头状态仍冻结并由 `M11-CAM-M3-007` 跟踪。 |
| `M11-TRAIL-006` 【资源不可见问题已修复并经可见 PIE 确认】同一次自定义 F4 发射中，Profile 2 与 `RenderMode=1` 没有尾迹，切为 `RenderMode=0` 后点粒子立即可见。 | 两种模式共用粒子历史、SceneProxy、bounds 和主视图可见性，故故障只在纹理 Sprite 分支。UE 5.8 的 `CreateTransient(..., ImageData)` 已在内部调用 `UpdateResource()`；旧代码设置属性后又调用一次，异步释放/替换第一份资源，而 SceneProxy 保存原始 `FTexture*`，PIE 主视图可能捕获即将失效的指针。离线同步 SceneCapture 通常等到第二份资源就绪，因而掩盖问题。 | 改为无 ImageData 创建 CPU 纹理，先设置 SRGB/流送/过滤/寻址/LOD，再直接写唯一 mip，最后只调用一次 `UpdateResource()`；可渲染进程要求 `GetResource()!=nullptr`，NullRHI 只要求 CPU 合同。`Begin` 增加 `ResourceReady`，自动化增加可渲染进程资源就绪断言。粒子采样、三层混合、CVar、镜头和 Stylized 均不变。 | UE 5.8 Development Editor `-ForceUnity -NoHotReloadFromIDE` 完整链接成功（`Saved/Logs/M11-PIE-SoftSpriteResource-R5-NoHotReload-20260811-UBT.log`）；fresh NullRHI 与 fresh 离屏 D3D 的 `ABTS.M11C.FinaleBirdTrail.History` 均为 1/1，后者连续记录 `Texture=64x64 ResourceReady=1`（`M11-PIE-SoftSpriteResource-R5-FreshAutomation-20260811.log`、`M11-PIE-SoftSpriteResource-R5-OffscreenD3D-Automation-20260811.log`）。用户随后确认 Soft Sprite 已存在；运动中偏淡属于独立时域 AA 问题，由 007 跟踪。 |
| `M11-CAM-M6-002` 【已修复并完成离屏验证：飞行模型速度/视角姿态】终局鸟体虽以速度建立 Actor 前向，但 Roll 使用 Finale Local Frame Up；导演相机转场时，可见鸟体上方不跟随屏幕上方。 | `UpdateFormationPlayback` 在 `FlightCamera::UpdateAuthoritySample` 之前用 `MakeFlightRotation(WorldVelocity, Frame.Up)` 写姿态，因此既使用了错误的 Up 权威，又早于本帧相机最终 Transform。Chaos 鸟的 `UpdateChaosVisualFrame` 还会在自身 Tick 重写 Mesh 世界姿态；模型组件另有默认 `Yaw=-90°` 导入轴修正，若把该值重复乘入 Actor 世界旋转会造成双重修正。 | 位置先按弧长编队更新供相机安全框求解；Interaction System 显式依赖四鸟 Tick，相机结算后以各成员轨迹速度为 Actor `+X`、相机 Up 的速度法平面投影为 Actor `+Z`。从各鸟类 CDO 冻结 authored Mesh 轴修正，最终写 `FlightActorRotation × VisualAxisCorrection`；奇异角依次使用上一 Actor Up 和相机 Right，非法输入 fail closed。新增纯数学 `BuildVelocityViewRotation` 与默认模型轴自动化断言。 | Development Editor 完整链接成功；fresh NullRHI `ABTS.M11C.Unit` 11/11。Rank11 Stylized1 自定义 F4 `M6ViewRotationR4-20260811` 为 1014 帧 `Complete/TargetHit`，Released/Plan Hash `0x668901FF090C69FE / 0x525966177CEBF5FF`，M6 丢失/顺序/间距失败均为 0，最终 UFO 距离 `800.0000000000989 cm`，AVI SHA-256 `C31C1E03F962ADF42F073C1793344FCBF86632539F93EF1B6B4876F29E93F9A9`；319/557/812 帧近景抽检通过。 |
| `M11-ENV-001` 【已修复合同；Integration 消费接线已合入】进入终局瞄准就把主世界完整切到太空 Profile，地表昼夜、大气和发射上升过渡立即丢失。 | M11 的 `IsFinaleActive()` 从 `Aiming` 起为真是 HUD/输入的正确租约；Integration T4-A3 却把该布尔量直接等同于“主世界已进入深空”。玩法激活、主世界环境阶段与远端 PIP 视图语义被压成了同一个条件。 | M11 新增纯只读 `GroundLaunch / AtmosphereTransition / DeepSpace / Recovering` 契约和 `GetFinaleEnvironmentStage()`。`Launched` 仅在冻结 Released Result 到达 `AssistEnter(1)` 后进入深空；缺事件、零 Hash 或非法时间保持过渡。M11 不改 `IsFinaleActive()`，不选择 Profile，也不改变 PIP；实际 `GroundDay → 高度连续过渡 → FinaleSpace` 装配由 Integration 落实。 | UE 5.8 Development Editor `-ForceUnity -DisableAdaptiveUnity` 完整链接成功；fresh NullRHI 专项 `ABTS.M11C.Unit.EnvironmentStageContract` 为 1/1，完整 `ABTS.M11C.Unit` 为 12/12（`M11-EnvironmentStage-ForceUnity-20260812.log`、`M11-EnvironmentStage-Contract-20260812.log`、`M11-EnvironmentStage-M11CUnit-20260812.log`）。专项覆盖瞄准、待发射、首事件精确边界、成功、缺失/错误 Assist、零 Hash 与非法时间；Integration 已在后续 master 合入消费接线。 |
| `M11-ENV-002` 【代码与自动门已修复，待用户可见 PIE 复验】失败飞行停止时天空立即变暗，鸟体明暗轮廓看起来像突然转向；预期应在可见失败飞行和淡黑期间保持原飞行环境，全黑后再恢复地表。 | `BeginAttemptFailure()` 在可读停留开始就把交互态切为 `Failed`，旧只读解析器又把 `Failed` 直接映射成 `Recovering`；Integration 因而在鸟和背景仍可见时撤销 `FinaleSpace`。真正的世界恢复本来要等 `ReadableHold + FadeToBlack` 到达全黑，交互态才切到 `Recovering`，环境契约比恢复事务早了整段可见时间。Profile 不写鸟 Transform，“转向”首先是天空/光照切换造成的视觉假象。 | `Failed` 与 `Launched` 共用冻结 Released Result 的阶段解析：已过 `AssistEnter(1)` 保持 `DeepSpace`，较早失败保持 `AtmosphereTransition` 供 Integration 继续按高度决定；只有失败时间线全黑恢复帧把交互态改为 `Recovering` 后才发布恢复环境。轨迹、候选/认证 Hash、鸟 Transform、失败时长与 Integration 渲染代码均不改。 | UE 5.8 ForceUnity 完整链接成功；fresh NullRHI 环境专项 1/1、完整 `ABTS.M11C.Unit` 12/12、Integration 路由专项 1/1（`M11-FailureEnvironmentHold-ForceUnity-20260813.log`、`M11-FailureEnvironmentHold-Contract-20260813.log`、`M11-FailureEnvironmentHold-M11CUnit-20260813.log`、`M11-FailureEnvironmentHold-IntegrationRouting-20260813.log`）。专项覆盖 `Hold/FadeToBlack → Failed/DeepSpace`、全黑 restore pulse 与 `Recovering` 同帧边界，以及早期失败保持高度过渡。若用户 PIE 后 Visual Rotation 数值仍跳变，再拆分独立姿态故障，不把光照观感误判成 Transform 修改。 |
| `M11-FAIL-001` 【代码与自动门已修复，待用户 PIE】正常深空失败一进入 `Failed`，鸟、四鸟编队、尾迹和相机就停在失败显示终点，随后静止画面才进入淡黑；首版续航虽移动了鸟，却让相机等量平移，星空相对视角仍不动。 | `Tick()` 在 `Failed` 状态只推进 `FailureTimeline`，不再调用完整播放与导演。首版计划末端切线又把四鸟和相机施加相同世界位移，保持了相机相对鸟与星空的视向，因而 `ContinueFlight=1` 只是数值运动，不构成可见运动。 | Release 时用错误轨迹的 Presentation End、播放倍率和 `ReadableHold + FadeToBlack` 反算更早的失败起点；进入 `Failed` 后，全黑前继续调用原 `UpdatePlayback()`，让冻结轨迹、四鸟、尾迹、姿态和完整导演共同推进。全黑恢复脉冲精确对齐已有轨迹末端；短轨迹只按比例压缩可读停留与淡黑，不做末端切线、不等量平移相机。Playback Points、事件、Plan/Result Hash 与认证身份不变，依赖故障仍 fail closed。 | UE 5.8 Development Editor 最终增量 4/4 编译成功；fresh NullRHI `ABTS.M11C.Unit` 12/12、`ABTS.M11C.Runtime` 2/2、Integration 环境路由 1/1（`M11-FailurePlaybackAlignment-20260813-Build.log`、`M11-FailurePlaybackAlignment-20260813-M11CUnit.log`、`M11-FailurePlaybackAlignment-20260813-M11CRuntime.log`、`M11-FailurePlaybackAlignment-20260813-IntegrationRouting.log`）。自动化覆盖正常窗口、短轨迹压缩、全黑与 Playback End 对齐和零时长 fail closed；PIE 仍需确认淡黑提前开始、星空随导演继续运动，且 `RestoredAtBlack` 的 `Playback` 与 `PlaybackEnd` 对齐。 |

## 9. 集成工作树摘录清单

集成工作树每次整理本文时，至少检查：

- 本轮新增条目是否已经有稳定根因，而不是只有参数猜测；
- 已修复项是否包含 commit、自动化或可见 PIE 中至少一种可重放证据；
- Candidate、Certified 和 Production Binding 是否被明确区分；
- `NullRHI` 的结论是否被误写成 SceneCapture/光照像素验收；
- 是否涉及共享地图、天空/雾云、默认 Blueprint 或稳定契约；若涉及，只摘录结论，实际修改仍留在集成工作树；
- 摘录完成后不删除本文历史，后续复发继续在原条目追加日期和新证据。
