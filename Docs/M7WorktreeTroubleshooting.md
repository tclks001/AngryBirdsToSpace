# M7 功能工作树排错记录

> 编码：UTF-8，简体中文。
>
> 文档性质：`feature/m7-buildings` 的增量排错台账，不是共享总文档。
>
> 记录范围：从 2026-07-28 22:59（Asia/Hong_Kong）本工作树会话建立职责约定起，至当前 M7.3 Beam-C3 稳定芯体收口。
>
> 上收关系：集成工作树在合并 M7 阶段提交时，从本文提炼已确认条目到
> [开发排错总文档](DevelopmentTroubleshooting.md)。总文档由集成工作树所有；M7 功能工作树不得直接修改。

## 1. 使用与维护规则

- 本文只记录 M7 工作树新遇到、且尚未完整进入共享总文档的问题。
- 每条至少保留“现象—根因—修复—防回归验证”，不能只写最后一次调参结果。
- 同一问题经历多次错误修复时，应保留错误假设为什么不成立，以及最终采用的权威规则。
- 编辑器截图只能证明视觉现象；编译、NullRHI 自动化、实时 PIE/Chaos 各自只能证明对应层级，不能互相替代。
- 自动化结果必须来自当前工作树、当前二进制和唯一日志。使用固定时间步的 `-benchmark` 不能代替实时 PIE 稳定性证据。
- 每次新增条目后更新“待集成提炼”清单。集成工作树完成上收后，将条目标为“已上收”，不删除历史。
- 本文属于 M7 专属文档。不得在此修改共享契约、M3/M11 行为或集成工作树的构建默认值。

## 2. 已由总文档覆盖、本文不重复展开的问题

以下问题虽然也出现在本会话早期，但当前
[开发排错总文档](DevelopmentTroubleshooting.md) 已有完整条目，因此本文只保留索引：

- DAG2.3 编辑器预览存在、PIE 约 6 秒后因 `IdleValidation` 被事务删除。
- TaskGraph 仍走 Legacy `Algorithm=0`、M6 与 M7.3 同时冻结刚体、建筑合同可能把零栋误判为空关。
- B2/Furnace 的 Tripod 支撑质心、真实接触比例和固定时间步假绿灯。
- DAG 新增源文件改变 Unity 分桶后，匿名命名空间同名函数发生重定义。
- DAG 预算前置终止、真实 Contact DAG、联合支撑凸包、非地基板逐层物理连续性等 DAG1～2.3 问题。

后续集成时，应优先提炼本文第 3～8 节，避免把上述旧条目重复复制到总文档。

## 3. 多工作树、构建与引擎基线

| ID | 现象 | 根因 | 修复 | 防回归验证 |
| --- | --- | --- | --- | --- |
| M7-WT-001 | 合并集成 `master` 后，M7 自身未改 M11，却在 ForceUnity 编译中出现 M11 `IsFiniteVector` 调用歧义 | Unity Build 会把原本位于不同 `.cpp` 的同名内部函数放入同一翻译单元；共享基线中的 M11 源码不满足 Unity 兼容性 | 不在 M7 越权修 M11；由 M11/集成工作树形成共享修复，M7 合并指定 `master` 后用 `-ForceUnity -DisableAdaptiveUnity` 重新全链接 | `git merge-base --is-ancestor <shared-sha> HEAD` 为真；ForceUnity 全链接成功；相关世界合同、M3 首周与 M7 门禁均在 fresh 进程通过 |
| M7-WT-002 | 打开 `.uproject` 提示 `AngryBirdsToSpace`、`ABTSRuntime` 缺失或由不同引擎版本构建 | 曾用 `C:\workspace\UnrealEngine-5.8.0-release` 源码版编译，而项目绑定 `C:\Program Files\Epic Games\UE_5.8` 安装版；两个 Editor BuildId 不同 | 删除“源码版也算 UE 5.8”的错误假设；本工作树固定使用安装版 `Build.bat` 全链接，不复制其他工作树 DLL，不使用 Hot Reload | 安装版 `-ForceUnity -DisableAdaptiveUnity` 全链接成功；fresh Editor 能加载两个模块；后续构建命令必须显式使用安装版绝对路径 |
| M7-WT-003 | 当前工作树未开启 Live Coding，构建仍可能被另一个工作树的 Editor 拦截 | UE 5.8 的 Live Coding 互斥检查按共享 `UnrealEditor.exe` 路径判断，范围大于单个项目 | 先读取活动 Editor 命令行确认归属；只有所有活动 Editor 都未加载当前 M7 工程/DLL 时，才允许对本次构建追加 `-NoHotReloadFromIDE`；不得结束其他工作树进程 | 构建交接记录是否使用该例外；M7 自己的 Editor 运行时禁止绕过，必须正常关闭 |
| M7-WT-004 | M7 实现 DAG4 时修改了共享 `AngryBirdsToSpaceGameDesign.md`，导致集成拒绝接收 | 把共享设计索引误当成 M7 阶段父文档；违反功能工作树所有权 | 恢复为集成 `master` 的精确 blob，单独提交 `M7: remove integration-owned design index change`；后续只链接/修改 `Docs/M7*.md`、`Docs/M73*.md` | 提交前执行所有权审计；交接中 `Shared files changed` 必须为 `none`；共享索引更新由集成工作树重放 |
| M7-WT-005 | 用工作树目录数字 ID 判断职责，或在文档/命令中硬编码托管路径 | Codex 托管 worktree ID 是不稳定实现细节，不代表 M3/M7/M11 身份 | 身份只取 `git rev-parse --show-toplevel`、`git branch --show-current`；运行时项目路径从 Git 根解析 | 每次会话启动记录 root/branch/status/worktree list；代码、配置和交接契约中不得写入 opaque ID |

## 4. DAG3、DAG4：开关、预算、预览与真实运行

| ID | 现象 | 根因 | 修复 | 防回归验证 |
| --- | --- | --- | --- | --- |
| M7-DAG3-001 | 物理测试场三栋建筑选择了 Single/Dual/Seam，但编辑器看起来完全一样，PIE 又全部失败 | 只修改了 Pattern 枚举，`bEnableAnalysis` 与 `bEnableGeometryRewrite` 仍关闭；三栋实际都是同一个普通 DAG2.3 Arch。编辑器显示无碰撞 HISM，PIE 创建真实 Chaos 模块，二者不是同一验证层 | 验收夹具显式开启 DAG3-A/B，并在 Summary 中显示 `DAG3Enabled/DAG3BEnabled/DAG3BApplied/Hash`；将普通 Arch 动态稳定缺口与 DAG3 视觉缺口分开修 | 三模式必须有不同 Realized Hash 与 W/P/受影响体；PIE 三栋 `IdleValidation Accepted=1`；不能因编辑器预览存在就断言动态成功 |
| M7-DAG3-002 | 普通 Arch/DAG2.3 在 30 Hz Chaos 中整体侧滑，静态图与固定时间步测试却通过 | 默认 8/2 求解迭代不足以稳定求解约 33 个刚体的多层接触；这不是几何 COM 倾覆 | 对 M7.3 生成刚体应用可调 32/8 求解迭代；不放宽位移、旋转或速度门槛 | 真实 30 Hz Actor/Chaos 回归通过；几何 Hash 不变；固定时间步结果只作算法回归 |
| M7-DAG3-003 | 提高递归层数后报 `DAG3BNoAcceptedPattern:DAG3BBrickBudgetExceeded`，整栋消失 | DAG3-B 第二遍事务重求解后的真实 Brick 超过夹具 `MaxBrickCount`；fail-closed 不会回退普通建筑 | 保持认证夹具的深度/预算配对；实验深递归时同时提高抽象节点、估算 Brick、编译 Brick 和搜索预算 | 错误必须带实际数/上限；预算充分时同一候选可生成；预算不足时不发布半成品 |
| M7-DAG3-004 | DAG3-C 增加细分后，整体仍像同一栋上下两块建筑 | DAG3-C 认证攻击可达、Drop/Tip/Slide 净空和弱点绑定，不改变宏观轮廓；递归只增加 Macro 内部层级 | 将该现象定义为阶段边界，不用 DAG3-C 参数伪造视觉多样性；轮廓多样性交给 DAG5-B/Beam | DAG3-C 验收 `Playable=1`、攻击净空、弱点数和预算；六栋读形不作为本阶段门槛 |
| M7-DAG4-001 | PIE 中 W/P、Affected、方向箭头和标签留在建筑初始位置，像固定在屏幕上 | 诊断 HISM/TextRender 挂在生成器 Root，设置为 `HiddenInGame=false`，且运行时模块生成后重新创建；它们不是物理砖 | 所有弱点诊断组件设为 Editor-only/游戏中隐藏；PIE 不再创建或显示 | Game World 自动化检查诊断组件不可见；Editor 仍可查看；运行态坍塌不残留覆盖层 |
| M7-DAG4-002 | 三种弱点模式在结构与失效表现上差异很小 | DAG4 使用同一旧主体骨架，只替换内部 Failure Frontier；弱点多样性受上游骨架多样性上限约束 | DAG4 先以静态/Chaos 反事实闭环完成；建筑与弱点联合多样性明确延后到 DAG5/WFC/Beam 后重做 | 文档标记 DAG4 完成但多样性延期；新骨架进入生产前必须逐 Profile 重跑 DAG4 |

## 5. DAG5-A/B：候选搜索、预算与语义轮廓

| ID | 现象 | 根因 | 修复 | 防回归验证 |
| --- | --- | --- | --- | --- |
| M7-DAG5-001 | 深递归以前也能生成，看不出 DAG5-A 新增了什么 | DAG5-A 不负责增加递归深度，而是把“一次随机推导”升级为确定性、有界、多候选完整可行性搜索 | 以逻辑 Seed 派生固定候选序列；每个候选通过预检、DAG2.3、已启用 DAG3、真实 Brick 预算后才发布 | 深递归扫描中 K=8 的成功率显著高于 K=1；同输入候选序列与 SearchHash 可复现；数学无解配置仍前置拒绝 |
| M7-DAG5-002 | 按深递归参数配置后，所有 Building Seed 都报 `DAG5ANoFeasibleCandidate:...DAG3GeneralizedCutFlowBudgetExceeded` | 实例仍开启 DAG3-A 广义小割、B 重写和 C 路由；深图让固定 `8192` 流操作预算耗尽。换 Seed 不能改变同一硬预算瓶颈 | 单独验收 DAG5-A 时关闭 DAG3 A/B/C；联合验收时按图规模提高 Flow/Candidate/Brick 预算，并明确这是另一组夹具 | 单独 DAG5-A 深递归成功；联合模式失败日志指出具体下游门；不得把 `MaxCandidateAttempts` 当成无限重抽 |
| M7-DAG5-003 | `DAG5B_ThroughOpeningWall` 固定报 `DAG5BEstimatedBrickBudgetExceeded` | `MaxEstimatedBrickCount=30`，而 8 Macro + 7 Support Edge 的保守估算为 36；有效上限取估算预算与真实预算较小值 | 提高估算预算到覆盖该轮廓的值，不需要提高已经足够的 `MaxBrickCount=100` | Details 与日志同时显示两个预算；预估 36 的夹具不再在 30 门槛必败 |
| M7-DAG5-004 | `OneSideHighTower` 接入 DAG3 后报 `DAG3CNoPlayablePattern`，误以为是“没有弱点/所有层三柱” | DAG3-A 有合法 Frontier，但 DAG3-B 改写后初始支撑裕量为负，完整建筑未受击就不稳定；C 只是包装上游失败 | 保留 B 的完整态静态门槛；不因想要弱点而接受负支撑裕量。后续在轮廓/支撑共同搜索中选可行候选 | 分阶段诊断 A 成功、B 明确 `InitialSupportMarginTooSmall`、C 不发布假弱点 |
| M7-DAG5-005 | DAG5-B v1 有 Shape/WFC、DAG 和 Brick，但与“轮廓→Seed DAG→Expansion→拟合”设想不契合 | v1 把固定 5～8 个 Macro 直接当最终 DAG，开启后绕过递归 Expander；没有细分 DAG 再拟合包络的中间层 | 保留 v1 为已实现参考；另写轮廓约束递归 DAG 演进设计，并用 DAG5-B v2 + Beam 路线逐步替代整板几何 | 文档明确“已实现、默认关闭/部分延期”，不得用 v1 结果宣称已完成轮廓约束 Expansion |
| M7-DAG5-006 | WFC 生成棱柱上再放棱锥，预览看似丰富但物理上只有线/点接触 | 旧垂直相容规则允许非平顶屋顶原语承载独立上层体量 | 棱柱、棱锥均改为终端屋顶；上方仍有体量时 Domain 强制 Box；需要复合屋顶时必须有平顶过渡层或合并为一个原语 | 多 Archetype/Seed 测试保证 Roof Primitive Terminal；编辑器不再出现尖顶承载独立体量 |
| M7-DAG5-007 | 提高 `GrammarDepth` 后主体轮廓变化不明显，变化集中在屋顶 | 初始 Archetype 已锁定大包络；Stack/Split 多在包络内切分，只有 Setback/Gap/Bridge 明显改变剪影；深度是上限而非每支必达 | 将轮廓复杂度指标从单一 Depth 改为量体数、退台、分支、非对称、桥/空洞等组合；后续由 Profile Resolver 强制里程碑 | 同 Seed 提深度时允许结构复杂度增加但不承诺剪影突变；Profile×Tier 必须用显式视觉里程碑验收 |

## 6. Beam-A：从脚手架中心线到堆放积木

| ID | 现象 | 根因 | 修复 | 防回归验证 |
| --- | --- | --- | --- | --- |
| M7-BA-001 | 初版 Beam-A 像脚手架和方框，只有 XYZ 端点连接，没有 X 梁搭在 Y 梁上的积木感 | IR 把建筑建模为中心线端点 Joint 图，缺少有面积、带上下顺序的 Bearing Contact | Beam-A v2 改为固定截面、长度可变的 XYZ 长条；X/Y 分层交叉，柱落梁、梁落柱、同向叠放均显式记录 Bearing Contact；屋顶用水平层逐级收分 | `DiagonalMemberCount=0`、`BearingContactCount>0`；编辑器能读出上下堆放，而非同高线框交叉 |
| M7-BA-002 | 某些平行梁看起来沿一轴变粗 | 不是截面变粗，而是同一 course 或相邻 Bay 的两根同色 Member 零间隙面贴面，视觉合并成一根双宽梁 | course 内引入最大数量、最小净空和等距排布；空间不足自动降根数；跨 Bay 另做公共边界协调 | 每根 Member 截面仍等于统一参数；同层平行 Member 默认有可见净空；只有显式复合梁允许侧贴 |
| M7-BA-003 | `MaxParallelBlocksPerCourse=2` 后，两个相邻 Bay 仍看见“三根”，中央像一根加粗梁 | 左右 Bay 拓扑各有两根，但公共边界上的两根面贴面；单个 course 的 Gap 求解不知道邻居 | 不合并两个 Bay 的语义 course；公共边界两侧分别内缩半个已求得的实际 Gap，仍保留 2+2 根 | 专项夹具应有 4 个 Member；公共边界净空与组内实际净空相等 |
| M7-BA-004 | 第一版公共边界内缩后，中央净空与组内净空不一致 | 错把 `MinimumParallelBlockGapCM` 当成最终实际 Gap；当可用跨度产生更大等距余量时，边界仍只缩最小值 | 先由数量和跨度求 `ActualGapCM`，再让两侧各内缩 `ActualGapCM/2` | 左/右组内 Gap 与公共边界 Gap 数值一致；只对高度实际相交的相邻 Bay 生效 |
| M7-BA-005 | Z 柱没有跟随已偏移的水平梁，柱高恒定，且只支持固定 X-Y 角点模板 | Z 柱位置从原始 Bay Bounds 重新推导，未消费最终水平 Member 站位，也未覆盖 X-X/Y-Y 同向承托 | X-Y 连接直接消费已生成水平梁交点；对齐的 X-X/Y-Y 在重叠区复用平行梁数量/间隙求解；不对齐则不强行补柱；柱长由上下真实层面决定 | 公共边界偏移后柱梁仍对齐；不同层高产生不同柱长；无对齐站位时稳定拒绝而非斜接 |
| M7-BA-006 | 两根平行梁只因不满足普通最小净空就过早合并成一根 | “允许最多几根”的净空阈值与“两根已近到应合并”的阈值共用一个参数 | 保留 `MinimumParallelBlockGapCM` 管多根排布，新增更小的 `TwoBlockMergeGapCM` 专管 2→1 合并 | 净空位于两阈值之间时保留两根；仅低于 Merge Gap 时合并为居中单根 |
| M7-BA-007 | Beam-A v2 仍出现跨 Volume 悬空、横穿和大块重叠 | 每个 Volume/Bay 独立生成；去重只处理完全同端点 Member，未做全建筑装配 | 增加全局收口：同向合并/裁切、X/Y 错层、Z 柱在水平层切分、短缝补承托、悬空岛补最近承重层；无进展时裁剪孤立冗余 | `RemainingPenetrationCount=0`、`UnsupportedMemberCount=0`；正式 Brick 不得在下游重新排布已验收中心线 |

## 7. Beam-B：Motif、语义屋顶和桥接闭合

| ID | 现象 | 根因 | 修复 | 防回归验证 |
| --- | --- | --- | --- | --- |
| M7-BB-001 | Beam-B 首版出现大量悬空和重叠，虽然 Port/WFC 验证通过 | B 只消费 Beam-A 的 Bay，清空已闭合 Members/BearingContacts；各 Motif 在原始 Bounds 内独立重建。Port 只表示语义兼容，不保证几何接触 | 将 Motif 输出编译回 Beam-A Joint/Member/Assembly IR，并复用同一全局装配收口；预览显示收口后的最终 Member | 无穿透、全 Member Ground 可达、非法短构件为 0；Port 通过不能单独作为结构通过 |
| M7-BB-002 | 蓝色斜撑与柱梁重叠，且可能依赖看不见的铰链/焊接稳定 | 把完整方截面长方体旋转后直接插入节点中心，既无斜切榫头也无可见托座；Chaos 接触会卡接或滑落 | `BracedBay` 保留兼容枚举但退出 WFC 生成域；Beam-B 当前只使用 XYZ 正交积木。未来斜撑必须有可见 BraceSeat、斜切几何和可断连接 | `DiagonalMemberCount=0`；任何重新启用斜撑的阶段需单独做静置、滑落和破坏认证 |
| M7-BB-003 | B 比 A 丢失 Shape Grammar 棱柱/棱锥语义，全部变成平顶矩形木垛 | B 只用 Primitive 限制 Motif 候选，却用 Bay AABB 生成等宽结构，并清空 A 的分层屋顶 | Box Bay 使用 Motif WFC；Prism/Pyramid Bay 复用 Beam-A 逐层收分屋顶，再统一进入 B 装配闭合 | `SemanticEnvelopeViolationCount=0`；棱柱沿短轴收分、棱锥双轴收分；Unsupported/Penetration 为 0 |
| M7-BB-004 | Shape Grammar 的高空 Bridge 被闭合器用超长落地柱“救活”，堵住门洞 | Bridge 只表达悬空体量，WFC 不证明双端承托；通用 GroundReachability 修复不知道门洞语义 | 禁止单边 Cantilever 进入生成域；新增 `SupportedSpan`，只在两端连接不同主体且下方完整 MustVoid 时保留；跨中禁止落地长柱 | BridgedArcology 下方门洞完整；桥体必须经两端指定模块承托，不能靠地面柱 |
| M7-BB-005 | 桥两端仍留可见缝隙或只部分纵梁落在桥托上 | Bridge Bay 与塔体都向各自 Bounds 内缩半截面；旧审计只要端点区域存在任意一条接触就把整端记为通过 | 主桥延伸至权威门洞边界；两端生成模块归属的 BridgeSeat/托块；逐根、逐端审计所有 BridgeRail | 每根纵梁左右端均有局部 Bearing；跨中无地柱；端点错位稳定 fail closed |
| M7-BB-006 | 只给一根悬空横梁补了 Z 柱，其余同层横梁仍悬空 | 支撑搜索每组只选一个 `BestUpper`，没有遍历全部独立上层横梁 | 逐根审计 BridgeRail/Seat 上方最近层全部水平 Member，为每根真正悬空梁在重叠区补局部 `BridgePost` | 默认夹具记录 `Targets=Supported`、`Violations=0`；已有直接承托的梁不重复补柱 |
| M7-BB-007 | 相邻 Bay 的横梁端部差半格未贴合 | Motif 的 `U0/U1` 从 Bounds 两侧各内缩半个截面；Port 相容与 Ground 可达都不要求端部完整覆盖 | 完整横梁延伸到 Bay 实体边界；邻接 Bay 在公共边界精确相接；只有局部短梁保留非连接端内缩 | 固定种子端部覆盖测试；不得以“有任意接触”替代完整接口覆盖 |
| M7-BB-008 | `GrammarDepthAddsTopology` 的深层语法稳定产生 `BridgeSeat` 与 `Post` 约 7.3 cm 穿透；在 corbel 前额外闭合后失败签名不变 | Beam-A 先按水平 course 切 Z 柱，随后共线合并又把同 lane 片段跨 course 并回完整区间；问题属于最终装配规范化顺序，不是 corbel 局部补闭合 | 保留第一次 split/merge 去重流程，并在最后一次共线合并后再执行一次物理 course 分段；同时让穿透日志输出双方 Role，测试失败时立即返回真实错误，避免后续断言噪声 | fresh `GrammarDepthAddsTopology` 1/1、`ABTS.M73DAG.BeamB` 13/13；`BeamAMemberPenetration=0`。不得通过缩小 BridgeSeat、放宽穿透容差或重复局部闭合掩盖规范化顺序错误 |

## 8. Beam-C、D0、D1/D1.5：荷载、真实接触、难度与屋顶

| ID | 现象 | 根因 | 修复 | 防回归验证 |
| --- | --- | --- | --- | --- |
| M7-BC-001 | 屋顶最上层颜色比下层更“高负载” | 颜色表示 `max(跨度利用率, 悬臂比, 柱长细比)`，不是累计重量；单支点长梁的跨度平方项可高于下层多支点重梁 | 将 UI/文档命名为结构利用率；调试时同时看累计荷载、支点数和 EffectiveSpan | Ground 深灰；蓝/青/黄/橙/红按阈值利用率；不得只凭颜色认定弱点或真实应力 |
| M7-BC-002 | Beam-C Load DAG 通过，但最终 Brick 看起来不接触或意外旁路 | C 最初直接消费上游声明的 BearingContact，没有从最终 Brick AABB 重建真实上下接触 | Beam-C2 从最终 Brick 几何重建接触，比较声明/真实边，计算合力落点、接触覆盖和支撑展宽；闭合后重新跑接触与 Load DAG | `RealContactMismatchCount=0`、阻断型支撑违规为 0；声明图不再是最终物理权威 |
| M7-BC-003 | `SeamRelease Tier 0` 出现宽大上部结构只靠单根细柱，编辑器却判为承重通过 | 旧 C 只证明存在 Ground 可达路径并按面积分荷载，没有验证局部合力是否落在支撑面/凸包，也没有局部抗倾覆门槛 | 对每个承重层计算真实支撑区域与累计合力；不满足时确定性补 Z 柱，并保护门洞/桥洞 ReservedSupportVoids | 真实接触与支撑违规均为 0；`AddedStructuralSupportPostCount` 可大于 0；D1 只编译闭合后的最终 Member |
| M7-BC-004 | D1 建筑不受击也会在 Chaos 中倾倒；承重 DAG、真实接触和静态代理却全部通过 | “存在向下荷载路径”只证明竖向静力可传递；大量贯通楼层的细长自由 Z 柱在无隐式连接的摩擦模型中会因微小偏心先倾倒，再被高重心上部结构放大为连锁坍塌 | 在 Beam-B 与 C2 之间加入 C3 四柱闭合井干芯体：每 Belt 两 X + 两 Y 实体 course、四角 Z 柱分段、Beam-A 重闭合和统一 720cm 全部 Z 站位柱跨；Core Member 不作为弱点候选 | 固定 5 Profile × Tier 0/1 静态矩阵必须全部通过；最终防回归必须追加实时 Chaos 静置 PIE，NullRHI 不能替代 |
| M7-BC-005 | Tier 0 加芯体后超过 49 Brick；事后删屋顶平行梁会破坏屋顶读形，C2 还可能把缺失承托补成更多长柱 | 芯体被当成附加装饰而非原框架替换；在已闭合承重结构后偷删 donor 会改变真实接触与合力，C2 正确修复后又超预算 | D0 先减少重复普通 Bay；C3 在 Host 内优先以普通框架 Assembly 交换四柱芯体预算。保护桥、Void、主屋顶 Crown、檐口与屋脊；单/双 lane 屋顶不可删除，任何内部冗余 lane 回退都要重闭合并保持屋顶指纹 | Tier 0 最终 Brick ≤49 且仍有单主屋顶；四柱闭环、真实接触、屋顶 course/指纹和 C2 后最终预算同时通过，不能只看 C3 改写后的中间数量 |
| M7-BC-006 | 三柱 L 形“芯体”已有 X/Y course，静态日志却仍会出现 `MissingPostBearing`，也不能形成抗双向侧倾的闭合围合 | 只有一个 X/Y 交点的三柱拓扑不是闭环；某一角/边缺失时，重闭合可能保留 course 数却没有四角真实堆放接触 | Catalog v8 升级为矩形四站位；每 Belt 明确生成/复用 X0、X1、Y0、Y1，并逐 Host 校验四角 course-course 与 post-course Bearing。缺任一角稳定拒绝 | `StationPositions.Num()==4`；每 Belt 至少四条 CoreCourse、四角 Bearing 完整；`BeamC3CoreTopologyIncomplete` 夹具 fail closed；5×2 不得降级三柱 |
| M7-BC-007 | C3 预留了 `BeamC2MemberReserve`，但 C2 补柱后最终 Brick 仍可能超过 Tier 上限 | Reserve 只是先验估算；候选需要多少 C2 修复只能从实际接触得知。若只检查“新增柱数量”或 C3 中间数量，就没有约束最终 Assembly | 把 `MaximumFinalMemberCount` 传入 C2；初始、每次补柱、每次重闭合与成功返回均检查实际 `Members.Num()`，容量不足以 `BeamCFinalMemberBudgetExceeded` 失败；C3 最终再防御性复核 | Tier 0/1 分别不超过 49/199；构造剩余容量不足的修复夹具必须在 C2 内失败，而不是等 D1 少编译或事后截断 |
| M7-BC-008 | 旧 v7 日志显示低 Tier 与全量矩阵通过，于是容易误判 Catalog v8 四柱实现已完成 | Catalog 版本、拓扑语义和 Hash 已改变；旧三柱夹具不检查四角接触、全部 Z 站位、严格根系普通楼层网或 C2 最终硬预算，证据不可继承 | v8 重新固定 5 个 Profile Seed × Tier 0/1 正式门槛；记录 Catalog/Resolved/Core/RootedEvidence/Brick Hash，并在最终代码 fresh 编译后用唯一日志重跑；失败项保留明确原因 | 安装版 UE 5.8 的 `BeamC3-Full-ContactFaces-Final.log` 登记了当时的 `ABTS.M73DAG.BeamC3` 14/14 与低 Tier 10/10；`DropTrigger/Tier4/Seed669740` 以 2297 Brick、Rooted=219、最大柱跨 713.04 cm 通过。Column v9 静态收敛见 M7-BC-017，仍须完整 D1、D1.5、5×6 视觉回归与真实 Chaos PIE |
| M7-BC-009 | NullRHI 的四柱拓扑、接触与预算全部通过后，仍可能在实际摩擦、质量和求解器条件下静置倾倒 | 静态几何/Load DAG 不包含 Chaos 的接触迭代、摩擦滑移、微小偏心和连锁碰撞；自动化证据层级不同 | 把可见 PIE 作为后续独立门槛：5 Profile × Tier 0/1 无攻击跑完整 IdleValidation，再做一次受控普通底部支撑击打；记录身份 Hash、首动 Member、位移/转角与坍塌范围 | `Accepted=1` 且无静置连锁坍塌才证明 C3 动态基线；受击局部性与弱点差异仍交给 D2，不得靠隐式锁定或放宽 IdleValidation |
| M7-BC-010 | C3 初次通过、C2 收口后只多出一处 720 cm 柱跨违规；修复时要么找不到合适高度，要么为缺 1 根预算删掉整组框架，触发更多补柱和更长裸 Z 柱 | 旧修复没有继续已有 Host/Tie Plan；高度搜索跳过了违规区间内的可用接触面；C3/C2 两次调用各自看局部预算，且预算供体先删整组普通框架 | final-only 修复继续已认证 Plan，重建 Host 签名和既有 Tie 计数；只在违规区间内按单截面步长搜索；定向 Tie 必须锚到已有 Host 并有两端真实 Z Bearing；C2/C3 使用累计账本。低 Tier 优先安全屋顶内部 lane，Tier 1 净增额度按三 Member 拉结原子校准为 33 | Catalog v8 历史证据：`ColumnBreak/Tier1/Seed710000` 为 163 Brick、Host=3、Tie=3、Rooted=19，最大柱跨 1223→648.00 cm；普通横梁、脱离锚点和重复局部预算均有 fail-closed 自动化。Catalog v9 当前值为 133 Brick、Host=3、Tie=0、Rooted=10、MaxAllZ=612.29，Tie 形态不是固定合同 |
| M7-BC-011 | `TipOver/Tier1` 的 C3/C2/C3 认证和预算均通过，D1 却以 `BeamD1BrickPenetration` 淘汰候选；顶层只显示最后一次 `AllZSpanExceeded`，掩盖了更接近成功的候选 | post-C2 修复为缺失承接面补一个截面高的短 Z 柱时，没有检查该体积是否已被普通水平梁占用，因而形成完整 `36×36×36 cm` 穿透；候选循环只保留最后失败原因 | 在所有承接面补柱前按真实 Member AABB 检查非 Z 占用；被阻挡时回滚当前 Host/Belt 并改选高度或 Host，不放宽 D1 穿透门；为后续门禁增加按 Attempt 的 `CandidateRejected` 诊断 | `TipOver/Tier1/Seed730000` 在 Attempt 0 确定性通过，134 Brick、Rooted=16、最大全部 Z 柱跨 679.02 cm、`StrictPenetrationCount=0`；生产确定性专项通过 |
| M7-BC-012 | 把与芯体“连通”的普通楼层梁全部算作约束后，单个偶然接触便可能给整片塔楼截断 all-Z 柱跨；单锚悬臂也会成为假稳定证据 | 宽泛组件 BFS 可穿过普通 Z 柱、跨楼层和跨语义量体传播；组件只要某处含 X/Y 且碰到一个芯体，就无法证明每条普通 course 位于两个独立根锚之间 | 最终几何上重新派生严格根系网：根链必须由精确 Host course 经真实 Bearing 到同源 `CorePost`；普通网只沿水平—水平 Bearing、同一 `SourceVolumeId` 和 `Section*2.5+Tolerance` 楼层带传播；剪除无锚叶枝，并要求剩余骨干含 X/Y 且连接至少两个不同根锚。Disconnected course 不参与柱跨截断 | `RootedExistingFloorNetworkBracesPeripheralPost` 正例与 `SingleAnchorCantileverNetworkRejected` 反例均通过；Summary 输出 `StabilityRootedExistingCourseCount` 和按轴/中心/长度/Source 排序的 `StabilityRootedEvidenceHash`，完整 C3 为 14/14 |
| M7-BC-013 | C3 排除一个失败 Host 后再次选址时可能无限重试同一矩形，表现为候选搜索不结束 | `SelectCoreHost` 的 `FCoreHost& OutHost` 保留上轮的高 Score 和有效索引；剩余低分候选无法覆盖旧值，而调用方又把旧索引误判为本轮成功 | 每次进入 `SelectCoreHost` 立即以默认 `FCoreHost()` 清空输出，再在当前排除集上独立选优；不得把输出引用同时当作跨轮缓存 | 排除 Host 后要么选到新的签名，要么有界返回 `BeamC3NoClosedCoreHost`；完整 C3 14/14 在固定时限内结束，不再出现 stale OutHost 自旋 |
| M7-BC-014 | 稀疏夹具日志显示已经生成 65 个确定性备用 Belt 高度，实际却没有任何高度进入评分/尝试 | 评分数组在备用高度循环之前构造；随后加入 `CandidateMidZs` 的高度没有同步进入 `ScoredCandidateMidZs`，形成“候选计数存在、求值集合为空”的时序错误 | 先收集已有楼层高度与全部确定性备用高度，再统一计算可复用 course 数并排序；优先复用最多，其次按距目标高度与高度本身稳定排序 | `ExistingPlanRestoresMissingCourse`、`ExistingPlanRepairsPostC2HighZStation` 与完整 C3 14/14 通过；日志中的候选计数必须等于真正进入求值的集合大小 |
| M7-BC-015 | 高 Tier Column 在 C2 重闭合后仍可丢失 Belt 一侧的短 Z 承接面，随后出现 `MissingPostBearing`、`BeamAGlobalAssemblyRebuildFailed` 或剩余 all-Z 超限 | C2/Beam-A 会重新分割、合并计划 Z 链；宽连续区间仍存在并不保证 Belt 上下各有一个真实短 Z 面。补入一截面高残段后，后续全局重闭合又可能消费、重分段或与密集楼层梁冲突 | 保守处理为：按 Host 站位重新标记实际 Z 段、重建 Bearing，只为确实缺失的一侧补一截面短面，并在补入前检查非 Z AABB 占用；不能用“有连续区间”替代真实接触 | `BeamC3-ColumnHighTier-NoShortFaces.log` 保留当时 E5/E6 失败；当前实体闭合与 v9 高 Tier 静态收敛由 M7-BC-017 接续证明。真实 Chaos PIE 仍不得宣称完成 |
| M7-BC-016 | 高 Tier 定向拉结在局部候选中看似可用，最终认证却仍报告 all-Z 超限；或者同一语义量体被 Bay 切分后只能反复增设 Host | 旧链路把 `BayId` 同时当作装配归属和物理断开边界，且局部枚举、插入闭合与最终审计使用了不同的 Source/Bay 判定；精确 Host 四角之外已经严格根系化的 XY 楼层横隔也未被统一视为双向约束端点 | 冻结 source-aware Portal 合同：同 `SourceVolumeId` 可跨 Bay，不同 Source 继续 fail closed；局部与全局使用同一判定；严格 rooted XY floor diaphragm 可作为端点；Portal 必须是两端均有最终真实 Bearing 的水平 Brick。原始 Z Member 的 720 cm 硬门不放宽，必须以实体分段消除超长柱 | 增加同 Source 跨 Bay 正例、跨 Source 反例、rooted diaphragm 正例、单锚/装饰横隔反例、缺任一端 Bearing 反例和局部/全局一致性夹具；Tier 0/1 仍须满足 49/199 与净增 12/33。合同与自动化完成后仍必须人工执行实时 Chaos PIE，NullRHI 不得替代 |
| M7-BC-017 | `ColumnBreak / Tier 4` 的最终 Node 606 会反复返回同一失败 DAG；日志显示新增两根 Z 柱，但权威 Beam-A 闭合后 Member/Bearing/DAG 身份不变，继续重试会耗尽闭合账本，提前批量补 cap 又会给没有根系证据的节点制造假承托 | 两根 25/75 Z lane 会被现有分段柱 merge/split 吞并；“尝试 Added”不是实体 Bearing。旧循环既没有保存已证明的精确 lane 几何，也没有按失败 DAG 身份限制事务；随后审阅还发现 `BestVoid` 首次实际添加 grillage 时没有登记 token，因此同一 Hash 仍可能重复实体事务 | Catalog v9 的唯一配置参数变化是把 E5 `TargetBaySpanCM` 调为 473 cm，版本升级仍改变全局身份 Hash。Beam-C 先证明两条精确 25/75 根系 lane，证据只跨一次权威重闭合；只有下一轮相同几何才能在上梁下一截面生成平行 cap。普通 proposal 被归一化时不提前消费 token；`H1→H2→H1` 非相邻循环 fail closed。修复函数显式输出“本轮实际添加 `BridgeSeat + 2 BridgePost`”，`BestVoid` 与平行 cap 都在重闭合前登记失败 DAG，下一轮修改前即拒绝；短 course 检查真实 lane 间距；C3 屋顶 donor 失败则事务回滚并继续 frame donor | `BeamC3-ColumnHighTier-V9-PhysicalCommitFinal-20260805-1822.log`：E5/E6 均双次重放一致；E5 Attempt 5，1499 Brick、Rooted=159、MaxAllZ=712.19、6/33；E6 Attempt 5，2325 Brick、Rooted=220、MaxAllZ=667.03、1/15。`BeamC3-Full17-V9-PhysicalCommitFinal-20260805-1821.log` 为 17/17，`BeamC3-BeamC13-V9-PhysicalCommitFinal-20260805-1820.log` 为 13/13，`BeamC3-D0-6-V9-PhysicalCommitFinal-20260805-1825.log` 为 D0 6/6；完整 D1、D1.5、5×6 与真实 Chaos PIE 仍未完成 |
| M7-D0-001 | 调节 DifficultyTier 时相邻档视觉变化很小 | 旧 Tier 主要以 3% 尺寸和连续权重微调，且许多解题指标尚未由 D2 消费 | D1.5 同时使用强制语义里程碑与互不重叠 Brick 数量窗，有限候选必须同时满足二者 | 五 Profile × 六 Tier 的 Brick 数逐级落入独立窗口；E1～E6 每一档有明确量体/退台/屋顶/桥接差异 |
| M7-D1-001 | E1/E2 只有架子，E3 才突然出现完整楼体和屋顶 | D0 曾把 Tier 0/1 强制为 Box-only，Prism/Pyramid 权重为 0 | 改为“低分辨率完整建筑”：E1 一个主体+唯一主屋顶，E2 增加第二量体/侧翼且仍保留主屋顶；不靠额外语义量体抬 Brick 数 | 固定 Profile/Seed 逐档观察；E1/E2 已有主体—屋顶层次，E3 仍有清晰复杂度跃升 |
| M7-D1-002 | E1/E2 屋顶只有两三层，仍像方盒，和 E3 差距过大 | 低 Tier 屋顶 course 被 Brick 预算压得过少，短边高度没有转化为足够收分层数 | E1/E2 保留一个主屋顶并提高最低 course；预算紧张时优先降低主体密度/包络，不牺牲屋顶立体感 | 屋顶诊断输出 `RoofBricks`；低 Tier 屋顶有明确收分高度，且 Brick 总数仍落在本 Tier 窗口 |
| M7-D1-003 | `DropTrigger Tier 0 Seed 669740` 屋顶层之间够不到，闭合器在内部补长柱 | 每层梁长度直接取当前层收缩后的长宽；低 course 数导致相邻层偏移过大，上层梁没有跨到下层承托梁 | 上层屋顶梁沿自身长轴延伸到紧邻下层的完整宽度；棱锥逐层应用，棱柱按 X/Y 交替每两层应用 | 固定种子 `Roof Brick=8`、新增补救柱 0、接触/承重违规 0；屋顶层层直接 Bearing |
| M7-D1-004 | Shape Grammar 的 X/Y 密度不平衡，出现某轴极密、另一轴极疏的高塔和勉强贴合层 | Shape 递归方向权重有偏；Beam-A 按局部长轴切 Bay 并各自求站位；WFC 只有局部 Port 相容，不做全局二维柱网平衡 | 平衡 Shape 的 X/Y 切分与尺度，Beam-A 建立/复用双轴结构站位，D1 增加各向密度与接触覆盖门槛 | X/Y 单位长度站位密度比受控；Beam-C2 补柱比例不异常；不再出现近零覆盖的两层承托 |
| M7-D1-005 | 平衡 X/Y 后，一个大屋顶裂成四个小棱锥，失去整体立体感 | 平衡/切分产生多个相邻顶层终端；WFC 对每个叶终端独立分配屋顶原语 | 在 WFC 分配前聚合同标高、水平相邻且覆盖率足够的屋顶终端；聚合 Crown 的长宽比决定 Pyramid/Prism 权重 | 相邻屋顶恢复为大 Crown；不跨大空洞误合并；同输入聚合 Hash 确定 |
| M7-D1-006 | 棱柱屋脊方向和屋顶高度不能稳定表达原终端比例 | 旧原语选择与 terminal 长宽比弱相关；course 数是固定/低阶 Tier 值；屋脊可能被切成多个 Bay | 短边决定屋顶高度，近方形偏 Pyramid、长宽差大偏 Prism；Prism 屋脊沿长轴，顶层强制为一根长轴梁，下层均匀展开 | E1/E2 也使用同一几何规则；屋顶高度约随短边增长；Prism 最顶层只有一根连续长轴屋脊 |
| M7-D1-007 | 提高语义屋顶后，Beam-B 全局闭合循环反复抬升间隙，运行很久或触及轮次上限 | 某些 gap-lift 修复没有减少违规集合；闭合循环缺少 no-progress 判定 | 记录每轮违规签名；无进展时转入明确补支撑/裁剪或 fail closed，不继续重复同一变换 | 闭合轮数有界；固定低 Tier/高屋顶种子不再出现重复相同违规；最终 Penetration/Unsupported 为 0 |

| M7-D1-008 | `ABTSM73BeamD1PreviewActor` 开启 `Spawn Runtime Modules in PIE` 后建筑消失，既没有编辑器预览，也没有真实 Module | PreviewActor 是关卡放置 Actor，其 `BeginPlay` 早于 `AABTSM7GameMode` 动态创建 `AABTSM7BuildingMaterialSystem`；旧逻辑只搜索一次，找不到便静默返回，而 HISM 预览在游戏中默认隐藏 | 将一次性搜索改为有界定时重试：每 0.1 秒查找一次，最多 40 次；找到后生成全部真实 Module、清除定时器并输出成功日志，超时或编译拒绝输出明确诊断；`EndPlay` 清理定时器 | 自动化先在没有 MaterialSystem 时执行同一重试入口，再延迟创建系统并确认生成数等于 Brick 数；日志必须出现 `RuntimeModulesSpawned`，完整 `ABTS.M73DAG.BeamD1` 10/10 通过 |

| M7-D1-009 | 物理测试场点击弹弓后报 `Accepted=7 Rejected=2 Contract=0`，预览夹具阻塞发射 | `AABTSM73StableBuildingActor` 过去没有预览/运行时边界：所有关卡实例都会在 PIE 生成真实 Module；测试场又未开启生产 Building Contract，因此 M6 兼容路径扫描世界中全部 StableBuildingActor。一个深递归夹具有 76cm 内部穿透，另一个 DAG5B+DAG3 夹具无可行候选，二者共同拒绝 | 新增互相独立的 `Participate in PIE Runtime` 与 `Participate in Slingshot Validation Gate`。前者关闭时不生成 Module、不运行 Chaos，并标记 `NotRequired`；后者关闭时保留真实 PIE 物理和内部诊断，但对弹弓门禁公开为 `NotRequired` | `ABTS.M73A.StableBuildingParticipation` 验证两种开关组合；M73A 2/2、Beam-D1 10/10 通过。测试地图中的失败夹具应按验收目的选择“纯预览”或“物理但不阻塞”，不得依靠放宽全局门禁 |
| M7-D1-010 | D1.5 视觉矩阵以一个测试串行执行 30 个 Profile/Tier，每格又做 6–12 次完整候选流水线；集成失败约 20 分钟后才只报告末项错误 | 测试粒度与故障域不一致，且候选日志没有阶段耗时；实测 `ColumnBreak.E4` 单格约 43 秒，其中一次旧 C3 达 18.7 秒。通用预算错误又不能区分 Member、Joint、Bearing 或配置 Support 配额 | 保留同一 5×6 合同，但改为 30 个可独立过滤的复杂自动化叶子；每个 Attempt 输出 Profile/Shape/BeamA/BeamB/C3/BeamC/Compile/Total 计时；Beam-A/B/C 容量错误输出实际值、上限和所需量，不调大预算 | `ColumnBreak.E1` 叶子 1/1；旧 C3 红样本仍 fail closed，但可单独运行。`ColumnBreak.E4` 明确为 `Required=2`、`MemberRemaining=1`，`SeamRelease.E6` 明确为 `Joints=8192/8192` 而非 Member；最终 30/30 仍是新版 C3 的完整门，不得删除、放宽或用部分叶子代替 |

## 9. Beam-C3 第一版约 20 小时复盘：效率与停止合同

第一版耗时过长不是某一次自动化单独运行了 20 小时，而是多个短循环串行累积：物理前提没有先用最小
Chaos 夹具证伪，静态代理和生产实现先被做深；候选搜索、donor、Portal、容差与 Catalog 参数连续变化，
却没有始终对应一个可证伪假设；同一权威失败 DAG 被重复提交；局部修改后过早重跑较宽回归；预算不可行性
没有先做下界证明；最后又缺少强制停止点。结果是“代码、日志和尝试次数都在增长”，但可排除的根因增长很慢。

### 9.1 可交接的复盘条目

| ID | 现象 | 根因 | 后续强制处理 | 防回归/停止证据 |
| --- | --- | --- | --- | --- |
| M7-BC-018 | 在尚未证明芯体自身能依靠真实碰撞与摩擦静置时，先投入大量时间完善四柱闭环、根系证据、预算与生产矩阵 | 把“闭合矩形、存在 X/Y course、静态 DAG 可达”误当成抗侧移稳定；无斜撑直立矩形仍可剪切，静态合同不能模拟 Chaos 接触迭代、摩擦滑移、微小偏心和高重心放大 | 对任何以真实物理稳定为目标的新拓扑，先制作最小独立物理夹具，只保留合同允许的 Brick、碰撞、质量和摩擦；先验证芯体自身，再接入整栋建筑。没有 GUI 授权时必须明确标记“待动态证伪”，不得以 NullRHI 继续加深实现 | 最小芯体必须在 fresh 可见 PIE/Chaos 的规定静置窗口内无持续位移、转角和连锁坍塌；若失败，立即回到拓扑设计，不运行生产矩阵，也不靠扩大静态证据声明稳定 |
| M7-BC-019 | 日志反复显示“Added”或进入新修复分支，但权威 Beam-A 重闭合后的 Member、Bearing 与失败 DAG Hash 不变 | 把“执行过修改”当成“最终物理状态已改变”；新增 lane 可能被 merge/split 归一化吞掉，或者事务 token 没按最终 DAG 身份记账 | 每次修复必须比较重闭合前后权威 `Assembly/Brick/Bearing/FailureDAG` 身份；只有最终实体和失败签名发生预期变化才算一次有效实验。相同输入、相同最终身份不得再次提交同一事务 | 同一 `FailureSignature + FailureDAGHash + AssemblyHash` 连续出现两次即 no-progress，禁止第三次同路线重试；先输出逐 Member/接触差异或停止并建立检查点 |
| M7-BC-020 | E5 已显示 `1803>1499`，仍继续靠 donor、Host、Portal、容差和 Attempt 组合寻找偶然过窗 | 没有先证明至少 304 个 Member 可在保持 Bearing、load cone、720 cm 和屋顶合同下安全回收；把结构/预算不可行问题误当成搜索深度问题 | 在扩展启发式前先做预算下界和 donor 依赖闭包审计；若安全可回收量小于缺口，结论必须是当前 Catalog/轮廓不可行，返回 Profile 或结构设计层 | 可行性报告必须给出 `RequiredSaving`、`ProvablySafeDonors`、删除后最终跨度/承接和保留合同；下界不成立时禁止增加 Seed、Attempt 或特殊分支 |
| M7-BC-021 | 每次局部修改后都可能重跑宽测试，构建和全量回归占用大量墙钟时间，但失败仍停留在同一 E5 节点 | 验证顺序倒置；尚未用最小夹具区分假设，就把昂贵门槛当调试器使用 | 固定“静态可行性/单元夹具 → 单一失败 Profile/Tier/Seed → 同输入双次重放 → 受影响小套件 → Beam-C3 全套 → D1/D1.5/5×6 → 可见 Chaos”阶梯；前一层未过不得晋级 | 每轮记录实际 Filter、预期测试数、唯一日志和耗时；宽套件只在失败签名已改变且专项通过后运行，零匹配、旧日志或仅进程返回 0 均不算证据 |
| M7-BC-022 | 顶层只保留最后一次候选错误，掩盖了更接近成功的候选；候选计数存在但求值集合可能为空 | 诊断没有保留 first/best/last 三类证据，也没有证明候选真的进入评分、修改、权威重闭合和最终认证 | 每个 Attempt 输出输入身份、候选数、实际求值数、最佳可行距离、首次阻断门、最终阻断门及重闭合后 Hash；以“最接近同时满足全部硬门”的候选指导下一假设，不以最后一条日志猜测 | `Enumerated == Evaluated + PrecheckRejected`；每个实际修改均有 pre/post 身份；错误摘要同时保留 `FirstFailure/BestFailure/LastFailure`，不能相互覆盖 |
| M7-BC-023 | 同一技术路线可以在没有新证据的情况下持续一整天，直到人工要求暂停才建立检查点 | 缺少单假设尝试上限、同 Hash 停止条件、墙钟时间盒和设计升级门；“还可再调一个参数”替代了明确的继续成本判断 | 开始编码前写实验卡：唯一假设、单一主变量、预计改变的权威签名、最小 Filter、时间预算、成功/失败判据和回滚点。单假设最多两个实现—专项循环；同一路线累计 4 小时仍未越过最小门槛，必须暂停并做设计评审，不得无人值守继续枚举 | 每 60 分钟更新排错账本或 checkpoint；连续 60 分钟没有新增可排除结论，或连续两轮权威失败身份不变，以先到者触发停止。构建互斥的纯排队时间单列，不得用于掩盖无证据迭代 |
| M7-BC-024 | 新版若继续用全局 `36 cm` 截面的“两 rail/course + 完整 XY pair + 屋顶另计”芯体，E1 会在候选搜索前就注定超出 49 Brick，却仍可能被误当成 Seed/Attempt 问题 | 该精确合同每升高 `72 cm` 至少消费 4 Brick；五个 E1 身体高度对应芯体下界 56/52/64/48/48，再加最低 8 个屋顶 Brick 全部超过 49 | 在写 generator 前先执行符号/整数预算下界。按最坏 TipOver 和至少 12 Brick 外壳反推 `t>=81.2 cm`；按现有 36 cm 量化，108 cm 是首个未被 Brick 数下界排除的截面，但完整 pair 会让五项身体分别超高 118/168/159/60/27 cm，不能称为生产可行。逐 Member 尺寸、屋顶起点、轮廓 placement 与总高 seam 必须另证，不得只在 D1 视觉放大 | `ABTS.M73DAG.BeamC3V2.MassiveXYCrib.Tier0BudgetProof`；永久停止的是上述精确 36 cm 合同，不外推到共享 shell/roof 或其它尚未证明的拓扑。任何新 rail/Z 候选先给出 `core + roof + shell <= window` 和无穿透空间布局，两者任一不成立时不生成生产候选 |
| M7-BC-025 | 把每层增加到 3/4/5 根 XY，或在空隙加入 Z 柱后，容易仅凭接触点/Brick 数增加就宣称“结构更强” | 完整受压交叉形成的 `N²` 理论承压区主要改善竖向承压、载荷分配和冗余，不等于 Chaos manifold 数；同 μ、同总法向力且无开缝/冲击/咬合时，整体 Coulomb 滑移上限不自动增加，中央 rail 也不扩大抗倾覆力臂。无胶 Z 柱不能像钢筋跨缝传拉力，且会重新引入屈曲；只有底顶承压路径持续、外笼在预登记 clearance 内提供围束并由 cap 送载时才可能作为压杆有益 | 候选固定为 A2、A3/A4/A5、受围束全长 Z、错位短 Z 四类，逐类记录外轨宽度、overhang、三种 H/W 定义、接缝张开、相对滑移、COM margin、Z 底/顶承压、侧接触、位移/倾角与每新增 Brick 收益；不得同时改 rail、外宽、截面、材料和摩擦。被更少 Brick 方案 Pareto 支配即淘汰 | 依据 USBM RI 9341/9168/9494 与 FEMA crib 指南建立有限候选树；同一拓扑、Geometry/Fixture CRC 与 binary identity 两次失败即停止。多 rail 先用于预算充足高 Tier，E1 的 108 cm 三轨在最简外壳下并非 49 窗统一解 |
| M7-BC-026 | 初版独立 Chaos screen 在首次 quiet 后 1.70 s 就退出、只 gate final，并对受压 rail 向内施加等于 quiet 上限的 4 cm/s；睡眠刚体的 immediate velocity readback 还是零，旧正结果可能漏掉延迟倾倒、峰值越门或扰动 no-op | NullRHI 能创建真实 Physics Scene，但证据必须闭环证明自定义重力、实际碰撞尺寸、UE 自动质量、solver、有效物性、扰动响应及完整观察窗。Stage-0 A2 的 `H/W` 依定义为 3.00（整塔 AABB）、3.27（首层承压外缘）、4.50（外轨中心距）；FEMA 2W/3W 只作保守参照，18 cm overhang 也没有矿井压缩木材互锁，不能据一次筛查外推生产 | 加独立自定义重力 probe；强制无 Attach/Constraint/repair/Mass Override/Freeze；回读 collision bounds、`RaiseMassToPower=0.75` 的 `417.871 kg` UE 生成质量与 32/8 solver。每阶段固定 6 s，要求曾达到且结束保持 quiet，同时 gate peak/final；顶层自由 rail 向外请求 8 cm/s，首物理帧必须有 >0.05 cm、>1 cm/s 的向外响应。用 `GeometryCrc32 + FixtureCrc32 + commit/binary identity` 防误配；DamageGrace 覆盖屏幕，明确只是 Wood-on-Wood no-damage screen | Geometry CRC32 `3576735518`；Idle Fixture `3056556652`，最终漂移/沉降/转角 `0.864/1.544/0.061`，峰值 `1.379/1.730/0.083`；Perturb Fixture `1572860114`，首帧响应 `0.093 cm/2.078 cm/s`，最终 `0.059/0.006/0.004`，峰值 `0.179/0.026/0.031`。严格 4-leaf 小套件两个 fresh 进程均 4/4；这只允许进入有限候选/production seam，不能替代 palette、完整荷载、实时 PIE 或攻击门；地图未修改 |
| M7-BC-027 | V2 四面外框接入后，E1 在旧 49 上限内无论换 Seed 都不可能通过，容易再次被误诊成候选搜索不充分 | 单叶下界表明最少需要 55 Brick；临时 59 仍失败，高容量诊断才观测到 63～84 的可行范围。旧数量窗与新结构合同不相容，不是 C3 内部隐藏 cap | 先做结构下界，再由 D0/D1.5 明示重新分区为 E1 `20–99`、E2 `100–299`，保持 E3 从 300 起、六档不重叠；不改 36 cm 截面，不放宽 DAG/跨度/穿透门 | `TipOver.E1 / 730000` 首候选 84 Brick、Plan `2768066177`、DAG `677265849` 通过；这只证明一个 E1 叶子，5×2/5×6 仍必须另跑，禁止把窗口调整冒充矩阵通过 |
| M7-BC-028 | 仅按某个接地 Bay 自身高度选根时，E6 报没有足够高的 grounded cell；换成“最高 Bay”又可能跨断层或借用不相连楼层 | Bay 的包围盒高度不是竖向承重权威；根体可能由多个相邻 Bay 向上连续组成，也可能在视觉上接近但没有上下表面接触或正 XY 重叠 | 在 Beam-A `AdjacentBayIds` 上建局部竖向权威链：根 Bay 接地且 XY 可放；每跳同时要求上下表面接触和正 XY 重叠；`HighestReachZ >= RequiredZ` 才可候选。链可穿过真实相接的 stacked semantic volume，全部 Bay/Source witness 生成 `RootAuthorityCrc32`；`ReservedSupportVoid` 仍作全局几何审计 | G0、E1、E3 在新权威下通过；若后续失败，日志必须区分 Grounded/XYFit/HeightAuthorized/GlobalVoid，不能退回按单 Bay 高度、全局最高点、跨断层或借用 witness 外的无关 Source |
| M7-BC-029 | E6 正式 4999 上限下最接近候选只差一个 Beam-C 支撑，容易继续把容量当作唯一根因并抬高预算 | 临时把上限提高到 8191 后，12 个候选仍全部失败，最佳候选转为 `ClosureStalled Resultant=5 Spread=5`；预算不足只是部分候选的表象，无法解释全部闭合失败 | 容量上调只能作一次有界诊断，必须立即恢复正式值。只有在给出缺失承重路径及每新增 Member 的确定性收益后才可讨论预算；否则回到几何/语义合同 | 正式单 cell E6 约 92.98 s 失败；8191 诊断约 86.09 s 仍失败并已撤回。禁止再次调 4999、Support/Joint/Member cap，或用更宽窗口掩盖 `ClosureStalled` |
| M7-BC-030 | 看到高 Tier 的 `MaximumCellCount=2/3/4` 后自动把 cell 填满，E3 反而从通过退化为全候选失败 | `MaximumCellCount` 是轮廓、语义和预算允许时的上限，不是 quota；无结构原因的额外 cell 消耗 Member 与闭合预留，却不一定承接任何桥端或失败 DAG | 首 cell 为唯一强制 anchor；可选 cell 必须绑定明确语义（当前仅合法 `SupportedSpan` 双端）、精确几何不冲突且通过 `Existing-RemovedUnion+Planned <= HardFinal-ClosureReserve`。一次规划、一次接纳，不枚举 1..N 组合 | 填满路线的 E3 为 6/6 失败，最接近 778/799、Required 22/Capacity 21；撤回后同一 E3 回到 711 Brick 首候选通过。禁止按 Tier 数值自动增 cell 或尝试 3/4 cell 网格 |
| M7-BC-031 | 为 E6 增加“桥端双 cell”后，日志看似已有 2 endpoints 和 2 eligible roots，容易继续重跑昂贵 E6 猜测哪一项差一点 | 8 个通过 Beam-B 的候选全部为 `EndpointPairUnavailable`；现有摘要只证明端点和根体数量，不说明 root witness、端点边界对齐或垂直包络的具体拒绝项，双 cell 实际从未进入最终几何 | 停止生产 E6；先建毫秒级纯数据端点派生 fixture，对每个 endpoint/root 输出 WitnessMatch、BoundaryFit、RailCoverage、DistinctSource、NonOverlap 的精确结果。只有 fixture 产生双 cell、Plan/Geometry 身份改变后才允许一次 E6 | 精确 E6 12 attempts/71.27085 s 失败，最后 `ClosureStalled Resultant=6 Spread=4 Members=4849`；该继续条件随后已完成，双 cell 确实进入几何，新的最终预算阻断见 `M7-BC-033`。同名非 Retry 日志零测试，排除为证据 |
| M7-BC-032 | Catalog 把 E1 从 49 改为 99 后，旧 `MassiveXYCrib.Tier0BudgetProof` 仍 Resolve 当前 Profile 并硬断言 49，导致宽回归会因历史数学结论而必然变红 | 历史夹具同时依赖旧 body/roof/window 和当前 Catalog，证据时代没有隔离；旧路线的永久取证被误当成现行配置门 | 历史夹具冻结自己的旧 49 窗、8-course roof 和五个 body 高度，不再 Resolve 当前 Catalog；当前窗口、Resolved Hash 和生产可生成性分别由 D0、D1.5、V2 G0/G1/G2 测试负责 | `M73-BeamC3V2-HistoricalTier0Budget-Compliant-20260806.log` 1/1 Pass；G0 3/3、E3、E1 在同一新二进制上合规重放通过。禁止通过删除历史夹具或把当前 99 回填进旧布尔推导制造假绿 |
| M7-BC-033 | E6 双端 cell 已经用两条连续共享 course 桥接且专项 G0 通过，容易把“少了重复桥梁”当成足以越过最终预算，或继续扫 R3/R2、Seed、cap、容差和额外内轨 | Beam-B 权威外轨只有两条，不能从 R4 core rail 数虚构桥轨；Beam-A Bearing 使用 0.5 cm 精确 face bucket，Beam-B/C3 中点相位不会形成 sandwich 接触；把交叉 course 延到整个 cell 边界还会穿透 facade Z 柱。修正三项后真实 E6 已形成双 cell/共享几何，但主导成本仍在 Beam-C 支撑与全局 reclose：旧 `4706+235+464-181=5224`，新 `4671+241+491-186=5217`，仅减少 7；235/241 是按闭合恒等式反推量而非日志单列 proposal，反推量增 6、净 split 成本增 22 | E6-only 原子合同只消费两条 Beam-B authority station；采用真实 C3 lattice Z；sandwich 只延到 station；受保护 `BridgeRail` 仅在同轴/同 station/Z 相位相容且被共享计划完整覆盖时才允许替换。把共享 course 记录为已证伪的充分解，不推广一般多 cell，也不靠参数排列重复。当前证书尚未逐 station 证明实际替换或正确 span ownership，后续必须先补该语义合同 | 当前二进制专项 G0 `M73-BeamC3V2-SeamRelease-E6-BridgeShared-G0-R2-20260806-1949.log` Pass；首次全边延伸穿透日志 `...Production-20260806-1947.log`；修正后生产日志 `...Production-R2-20260806-1950.log` 以 `BeamCFinalMemberBudgetExceeded:5217>4999` fail closed。该历史停止点要求先证明 218 个 Member 的结构性节省；M7-BC-034 的 40 cm 减密随后满足预算前置，但不改变本条“共享 course 不是充分解”的结论 |
| M7-BC-034 | 无芯体主体已接近 4999，上线双分层芯体后 5217 越界，容易继续删芯体或把数字当成固定 Seed 的偶然值；改变平行净空后若 Result Hash 不变，还会把新几何误认成旧候选 | `MinimumParallelBlockGapCM` 决定三根以上平行 course 的 lane 数，`TwoBlockMergeGapCM` 只控制 2→1，二者不可混调。12 cm 基线 5217；72 cm 为 3625 但四个 DAG 节点停滞且三轨退成两轨；40 cm 为 4552、仍四节点停滞但保留三轨。说明预算缺口来自确定性密度，不是估算误差；闭合缺口则是另一项拓扑问题。原 Resolved Hash 还漏掉截面/min gap/merge gap | Catalog v11 固定 min gap 40 cm、merge gap 4 cm，把三项几何硬门纳入 Resolved Hash，并在 5×6 D0 全矩阵断言；只允许 72/40 两个离散原型，40 已有 447 Member 余量后立即停止 gap 假设，不测试第三值、不动截面/4999/DAG 阈值 | gap72 D0 6/6、Beam-A 11/11、E6 3625 fail；gap40 D0 6/6、E6 4552 fail。预算已解决，后续任何 `ClosureStalled` 不得再以加大 gap 或 cap 解释；日志为 `M73-BeamC3V2-ParallelGap72-*`、`...Gap72-Production-*`、`...ParallelGap40-BeamD0-*`、`...Gap40-Production-*` |
| M7-BC-035 | V2 接入后仍调用 Beam-C closure 的 `bAllowDeferredCoreBracing=false`，显式 transfer seat 路径不可达；启用后又容易把“提交 BridgeSeat+双柱”当成最终 Bearing 已闭合，并继续递归给失败 seat 加 seat | 旧 parallel 25/75 cap 与 Upper 同轴，放在交替正交层时会被 Beam-A course separation 抬高并脱离根柱；另一 cap 也未覆盖真实 resultant。改成与 Upper 正交、以 LoadResultant 为中心的三截面 cross-bearing 后，6 个待修节点消除 5 个，最终 4358/4999，只剩 Member 1879。该 Y-seat 已提交、Shift=0，但 reclose 后未进入 Upper 的 Bearing candidate；`Added` 仍不等于最终实体接触 | deferred 路径只能由随后立即运行 V2 几何、完整 Beam-C DAG 和所有最终 Z 段审计的 orchestrator 开启；756 cm Z 夹具必须 fail closed。单失败 DAG 先一次 quarter-root、再一次 resultant cross-bearing，事务按失败 Hash 限制；两轮用满即停止。BridgeSeat 不得递归生成第二层 seat。恢复前先用毫秒级 1879 fixture 输出 pre/post Member/Role/AABB/owner/Bearing 差异，不以第三 gap、比例、margin、pass、Seed 或预算猜测 | final-all-Z 1/1 Pass；parallel cap 日志最终 4363、剩 2 节点；resultant cross-bearing 日志最终 4358、剩 1 节点并以 `BeamCStructuralClosureNoProgress` fail closed。G3/G4、其它 Profile/Tier、视觉和 Chaos 均未运行；精确日志 `M73-BeamC3V2-FinalAllZSpan-20260806-211749.log`、`...TransferGrillage-20260806-211841.log`、`...ResultantCrossBearing-20260806-2129.log` |
| M7-BC-036 | 已有 Beam-A/Beam-B 主体看似“先有结构”，C3 后插芯体后却仍出现 DAG 不闭合、预算被 closure 吃光和最后一个 seat 长期不收敛 | 旧主体先消费空间、Member/Bearing 容量和所有权；后插芯体再触发 merge/split/shift/reclose，计划 `Added` 与最终接触不是同一状态，Beam-C 同时被当生成器和审计器 | V3 改为 skeleton-first：WFC 只给语义包络，先计划 grounded component、cell、seat、span、roof，再一次发射；Beam-C 只调用只读 `Generate`，禁止 structural closure、rescue post 和 post-C2 repair | 本条最初记录的 V3 Stage-1 49/49、5×6 30/30、BeamD15 32/32 已被 M7-BC-042 降级为缺少显式 core 合同的历史假绿；架构方向保留，现行接地重写证据见 M7-BC-043/044。所有现行生产叶仍为 `StructuralAttempts=1`、`ClosurePasses=0`、`ClosureAdded=0` |
| M7-BC-037 | 栅格 cell 中心落在 WFC 内，或 post 属于同一 component，就容易把整根实体判为合法；Body/Crown 接缝又可能被“必须由单一 volume 覆盖”误拒绝 | 点采样不能证明 `36 cm` 实体；component union 会掩盖 foreign/wrong-source；0.5 cm cut 去重还可让 `0/0.4/0.8` 链式吞掉正宽漏洞 | 对四条实际发射边做 full-solid `Body∪Crown` 并集覆盖；cuts 只按机器 epsilon 去重。CorePost 绑定声明 source 投影；exterior fallback 还要求同 root、当前 slice、半砖 halo。保留 chained-sliver、foreign source、same-root wrong-source 反例 | 原 `Stage1.G0` 15/15 属 M7-BC-042 前历史结果；相同 full-solid/source 反例保留在接地重写后的 G0 16/16 中，任何 uncovered solid、owner/source 错配或 ProtectedVoid 相交仍在 emission/Beam-C 前 fail closed |
| M7-BC-038 | planned/actual Bearing 数量相等且四面 mask 为 `0x0f`，仍可能丢一个接触又多一个意外接触，或完全由水平周边梁冒充四面外柱 | 数量不是接触身份；required-seat 只是预测集合的子集。FaceMask OR 也不区分水平梁与 Z 柱、同一站位的多段柱 | `RebuildBearingContacts` 后比较完整 canonical pair set，拒绝重复、非法和等数量替换；另按 component/face 统计 seat-DAG grounded 的 Z-post XY distinct station，每面至少 2，同站位多段只计一次 | 原 `MinFacePosts=2..5/SeamRelease.E6=3` 属顶部假芯体版本；接地重写后的最终 30 格均为 `MinFacePosts=4`，SeamRelease.E6 也是 4，planned/emitted/contact 集合继续在 Beam-C 前冻结 |
| M7-BC-039 | 5×6 从 21/30 改到 23/30、27/30、29/30 时，容易继续把水平/垂直 cell units 当连续参数或按 Seed 邻域搜索，重新形成路径依赖 | Brick 数由确定 WFC 包络与离散 subdivision 精确决定；同一候选内搜索密度会把预算窗变成偶然验收，也无法解释哪个结构合同改变 | 只允许 Profile/Tier 固定 Recipe；每轮针对一个已定位的窗口/屋顶/包络假设，专项通过才扩矩阵。最终冻结基础 `18/18,12/14,11/10,8/8,9/7,8/5` 与五个显式覆盖，禁止 Seed/Attempt/邻域扫描 | 五次矩阵日志分别为 21/30、23/30、27/30、29/30、30/30；终审收紧后同一固定 Recipe 再次 30/30。Recipe 变化必须新建实验卡和身份版本 |
| M7-BC-040 | 第一版 D1.5 自动化约 20 分钟，容易把“高 Tier 本来就慢”当成必然并继续用宽套件调试 | 旧路径对每个候选运行 Beam-A、Beam-B、C3 后插、全局 reclose、Beam-C 多轮修补和候选 Attempt；宽门把搜索/修复成本重复 30 次 | V3 在语义有效候选后只做一次 plan/emission/Bearing/Beam-C/D1；G0/G1 先阻断合同错误，30 格只作里程碑。每阶段记录唯一日志和期望测试数 | 旧假绿构型的 V3 套件曾约 30 秒；显式接地芯体重写后，fresh G0 约 141 秒、G1 约 42 秒、5×6 约 117 秒、BeamD15 约 133 秒，仍是约 2 分钟级而非 20 分钟。时间增长来自真实 core/shell 几何与完整 anti-fake 审计，不应通过恢复 closure/候选搜索解释；零匹配、旧二进制和仅进程退出 0 仍不算通过 |
| M7-BC-041 | 5×6 DAG、预算、外柱和 BeamD15 全绿后，容易直接宣称“新芯体稳定”或马上把物理调优绑定到未批准外观 | Stage-1 证明的是构造出来的静态图、接触几何和代理门，不包含 Chaos 摩擦、质量、求解器、接缝开裂或高重心动态响应；人工视觉也不是自动化布尔值 | 证据顺序冻结为静态 5×6 → 用户视觉批准 → 另立物理设计/最小夹具 → 完整实时 Chaos。视觉拒绝时只改形体/Recipe并重跑静态门，不提前调摩擦或 solver | 所有 V3 日志显式 `Physical=NotEvaluated`；本轮未启动图形化 Editor、可见 PIE 或 Chaos。视觉批准前不得把 30/30 写成自稳定结论 |
| M7-BC-042 | V3 的 49/49、5×6 30/30 和 BeamD15 32/32 全绿，但编辑器中“芯体”出现在建筑顶部，主体内部没有从地面起的分层芯体 | 生成器把整栋 Body 的 occupied raster cell 数统计成 `CoreCellCount`，内部 Floor 误用 `CoreCourse` 角色，真正 `CoreCell` 只有稀疏 Z post；Crown 又以固定共同 XY footprint 重复交替 `RoofCourse`，形成顶部井干塔。旧测试只证明任意 Member 经全局 seat DAG 到地、四面外柱和非零 Hash，无法表达“显式 grounded core → shared requirement/course → core-derived shell → tapered roof” | 在 IR 中加入独立 `FCoreCellPlan`、member kind、core origin/inward dependency 和 shared endpoint core；先冻结 shared 相位、opening 与 endpoint required top，再从 Body/ground 连续生成纯 XY core并向外派生四面外框、Floor/Infill、收分屋顶；两端 core 可引用后绑定 shared member，完整计划一次发射。生产 validator 固定 core 抬高、shell 丢 origin、shared 换非-core seat、非法 Crown core 反例；`CorePlanHash` 只覆盖显式 core | 旧三组全绿日志永久降级为假绿反例。现行门已重新满足 `GroundedCore==ExplicitCore`、`SuspendedCore=0`、`CoreDerivedShell==Shell`、`SharedNonCoreEndpoint=0`，并取得 G0 16/16、G1 4/4、5×6 30/30、BeamD15 32/32；具体相位、连续上延、屋顶和预算证据见 M7-BC-043/044。视觉批准前仍为 `Physical=NotEvaluated`，不得运行 Chaos |
| M7-BC-043 | 显式双芯体已经存在，E5/E6 仍报 `EndpointSeatUnavailable`，容易误判为“shared course 方案本身无效”或继续增加 rail | shared bridge 的轴向与绝对 course 奇偶相位未绑定，桥下 CoreCourse 可能与桥同轴，不能形成交叉承压座；修正相位后 E6 又暴露较矮 endpoint core 只生成到 Body 顶部，而合法桥位在 Crown 内 | X bridge 固定在绝对偶数 course、Y bridge 固定在绝对奇数 course；两端 `c-1/c+1` 必须为正交 CoreCourse。默认芯体止于 Body 顶部；仅为真实 shared endpoint 从同一接地芯体连续延到 `c+1`，每段须由 `Body∪Crown` 覆盖，validator 反推精确顶层并拒绝多层、断层和悬空 Crown core | `ColumnBreak.E5/E6=1951/2515`；Column E6 为 4 个显式/接地芯体、0 悬空、2 shared course，最终矩阵 `SeamRelease.E6=4907` 且为 6/6 个显式/接地芯体、2 shared。`BeamC3V3-ColumnBreak-E6-GroundedExtension-20260807-113711.log` 与 anti-fake `...GroundedTopology-CrownExtension-20260807-113926.log` 通过。该上延是接地芯体连续段，不是恢复顶部假芯体 |
| M7-BC-044 | 接地重写后的首次 5×6 只有 20/30；`DropTrigger.E4` 先报屋顶 envelope，再报 2 cm roof/shell 穿透，其余多数失败落在旧 Brick 窗外，容易在宽矩阵中调 Seed/密度或用全局容差掩盖 | 屋顶拓扑允许沿承托轴延到 lower seat，但 envelope 仍只接受原 Crown；横向固定站位也未避开同 slab 的实际 shell 障碍。另一方面，旧窗口统计的是不含真实芯体的结构，不能容纳确定性新增 core/shell 成本 | envelope 只精确增加声明 lower-seat 轴向范围；横向只枚举包络边界和障碍物非穿透边界组成的有限集合，选最外侧两个可行站位。只做一次 5×6 实际计数调查并一次性冻结通用 `20–149/150–349/350–799/800–2099/2100–3399/3400–5499` 与 Column E4–E6 `800–1599/1600–2199/2200–3499`；不改截面、不扫 Seed/Attempt/连续间距 | `DropTrigger.E4=1800` 后通过；fresh G0 16/16、G1 4/4、5×6 30/30、BeamD15 32/32。H8/H9/H10 为 1472/1348/1348，平台后停止 H11/H12。全部 `Physical=NotEvaluated`；窗口调整证明预算可表达，不证明视觉或 Chaos 稳定 |
| M7-BC-045 | grounded rewrite 的 `SeamRelease.E6` 静态门显示两个 shared course，但编辑器中实际只是两根独立桥 rail 用很短端部搭到两芯；两端芯体原 course 仍各自存在，桥洞中也没有其它 course 构件 | validator 只检查 span member 记录两个 endpoint core 和两个端座，没有证明同一个 Member 替换双方 `(course,rail)` 槽位。shared station 还可选在两端局部 rail 网格之外，于是“桥梁碰到两芯”被误记成“双芯共享”；裸两 rail 也没有形成完整交替 course band | shared rail 必须由两端同 course/同 lane 的局部 rail 合并为唯一 Member，同一 MemberId 同时替换两个 core 槽位，并从负端 core 最外物理面贯穿正端最外物理面；原局部 rail 删除。每条 rail 在每端取得两个 lower core cross-seats，桥内 `C+1` 至少生成一组承压于两条 rail 的正交 diaphragm；超过 720 cm 且无权威中间实体时 fail closed | 新增固定反例：删除任一 core 槽位引用、保留被替换局部 rail、把 shared rail 缩到只碰内侧面、删除 diaphragm、把四个 endpoint lower contacts 降为两个，均须在 Beam-C 前拒绝。先跑纯数据/最小几何 fixture，再只跑固定 `SeamRelease.E6`；旧 `SharedCourseCount=2`、4907 Brick 和 30/30 永久不足以证明本合同 |
| M7-BC-046 | 当前编辑器结果由六个芯体分别长出六片薄楼体，除 M7-BC-045 的两根 rail 外彼此不连接；局部四面证书和全局 Ground DAG 仍可全绿 | 生成器按每个 grounded component 独立 raster/flood-fill，并要求每个 component 自己完成四面 facade/post；该数据模型必然把多个 core 当成多个建筑壳。全局 Load DAG 只证明所有构件各自向地可达，不证明它们属于同一个共同外框 | 增加候选级 `FBuildingGroupPlan`，显式列出全部 component/core/span/member；按 group 整体轮廓生成一套共同 perimeter、band 和 exterior posts，并以两轴 core spokes/shared course 接入所有 cores。四面证书提升到 group 级；局部 component 只保留 provenance。`ProtectedVoid` 继续从 group envelope 扣除 | 最小 group fixture 必须包含多个 independently grounded cores，但最终 common-shell 图只有一个连通分量、覆盖全部 core ids，四面各至少两个 grounded exterior-post stations。构造“每 core 局部 mask=0x0f、group 图仍分裂”的反例必须报 `BuildingGroupCommonFrameDisconnected`；再以一个无 span 普通叶证明共同外框不依赖桥 rail 才连通 |
| M7-BC-047 | 芯体连接处视觉上只挨半块积木，端部退后半格；外框延伸也从这个退后位置起步，虽有正面积 Bearing，实际承压面和抗偏心余量都明显不足 | core X/Y rails 在 transverse station bounds 终止。固定截面为 36 cm 时，每轴只越过交点中心 18 cm，交叉投影只有 `18×18=324 cm²`；旧门只要求 overlap/area 为正，没有冻结完整截面接触 | 不改 Brick 粗细。X/Y core rails 都在两端 cross station 外延 `B/2=18 cm`，逐 crossing 要求两个平面 overlap 维度均至少 36 cm、面积至少 `1296 cm²`。core-derived through/facade 从完整 cap 外端面再外挑至少一个 `B=36 cm` 单元或连续到 group facade；同轴段只端面相接，禁止 cap/shell 正体积穿透 | 固定 positive fixture 逐 crossing 对账 `OverlapX/OverlapY/Area`；把任一端点退回 18 cm 的 mutation 必须报 `CoreBearingAreaInsufficient`。包络容不下完整 cap+外挑时稳定失败，禁止用半格退缩、扩大容差或改粗构件恢复绿灯；该合同通过后才运行 `SeamRelease.E6` 和一个普通边界叶 |
| M7-BC-048 | 第二遍 support closure 注释称“最终 fail closed”，实际计划仍可接受只托住 Member 自身中心的候选；封死该退路后，若把所有长梁一律强制端部承座又会在屋顶/芯体占位处制造假失败。固定 ColumnBreak.E6 最终表现为 Q44 common ThroughCourse 的质量加权合力落在现有右支撑域外，而所需下一格 carrier/cap 位于 ProtectedVoid 投影内 | 候选选择有两处独立的 self-centre fallback，没有受 `ClosurePass` 限制；直接上载荷 min/max 和固定 end-zone 都不是 Beam-C 完整反力传播。改为同构 reaction-weighted DAG 后，计划目标 `X=-180.3` 与旧 Beam-C `X=-188.64` 指向同一支点。Q42 carrier 延长 `36 cm` 时 `Blocked=1,Void=1,Blocker=-1`，证明当前矛盾是 generic common row 与 span/void 语义分工，而不是预算、容差或少一根柱 | pass 0 可用 self-centre 只作初始几何成形；pass 1 禁止降级，并在 pruning 后以最终 reaction-weighted resultant 复扫。不得把 carrier/cap/post 放入 ProtectedVoid。先建立 Q44 stable-signature fixture，判断该 row 是否应由 SharedCourse/diaphragm 接管，或应在 void 两侧合法分段/重路由 load path；只实现一个被 fixture 证明的拓扑方案 | `BeamC3V3-G1-ColumnBreak-E6-FinalSupportAudit-20260807-200653.log` 证明旧 pass-1 假闭合；`...Pass1NoSelfFallback-20260807-200908.log` 与 `...DirectLoadFinalClosure-20260807-201514.log` 证明 end-zone/direct-range 过保守；`...ReactionWeightedClosure-20260807-201952.log` 将目标收敛到 Q44；`...CarrierBlockerIdentity-20260807-202826.log` 固定 ProtectedVoid blocker。相同稳定签名/void blocker 再出现一次即停止，不跑第三轮，不改 margin、gap、截面、Seed、密度或预算 cap |
| M7-BC-049 | 把 WFC→芯体→耦合点→共同外框全部放在一次生成中，视觉发现错误时只能从最终成品反猜上游；旧文档还把“完整静态建筑”称为 Stage-1，容易把显示过滤误当生产停点，并继续在后段 support closure 上消耗时间 | 生成结果没有阶段不可变边界和上游/下游 Hash 链；预览 Actor 只接受完整 D1 Brick，无法分别显示 WFC、芯体选址/配对意图和实际芯体/shared。旧生成器在每个 component 的 core 后立即发射 shell/roof，等所有 core 完成才绑定 shared，代码顺序也不符合用户审核顺序 | 现行 Stage 0～5 重新定义为 SemanticEnvelope、CoreAndShared、CouplingCourses、CommonExteriorFrame、Floor/Infill/Roof、StaticDAG。Stage 1 在每个 core 发射后真实 early-continue 跳过 shell/roof，全部 core 完成后冻结 `FSharedCourseIntent`、替换双方 slot、加 diaphragm、重建本阶段 DAG并在 common-frame 入口前返回；未来 Stage 2～4 选择稳定 `BeamC3StageNotImplemented`。`AABTSM73BeamD1PreviewActor` 提供生产停点与三层诊断下拉，不改地图二进制 | fresh `ABTS.M73DAG.BeamC3V3.Staged` 3/3：Stage 0 为 3 WFC volume、0 V3/Beam-A member；`SeamRelease.E6` Stage 1 为 6 grounded cores、1 pairing intent、4 shared rails、775 members，静态 DAG accepted；Stage 2 fail closed。日志 `Saved/Logs/BeamC3V3-Staged-Stage0-Stage1-Final-20260807.log`。未运行 5×6、BeamD15、Chaos 或可见 PIE，`Physical=NotEvaluated`；新增原生预览组件待用户 fresh Editor 视觉验收 |
| M7-BC-050 | Stage 1 视觉验收发现 Core Placement/Pairing Intent 被透明 WFC 包络包住，内部意图不可读；三棱柱包络还出现表面明暗/剔除异常 | 包络可见条件写成“非 CoreAndShared 即显示”，因此 WFC 与 Intent 层有意外交集，ProtectedVoid 也同时进入两层。ProceduralMesh 又为每个面发射正反两份完全共面的透明三角形，造成自混合/深度排序伪影；`TriangularPrismX/Y` 的原始五个面绕序全部朝内 | 三层改用显式 visibility mask：WFC 仅 Envelope+ProtectedVoid，Intent 仅 CoreIntent+PairIntent，Member 仅真实 Core/Shared/Diaphragm，集合两两无交集。程序化包络每面只发射一份三角形，修正 PrismX/Y 的全部外向绕序并保留显式 flat outward normals | `ABTS.M73DAG.BeamC3V3.Staged.PreviewDiagnosticContracts` fresh NullRHI 1/1：验证三层 mask 两两无交集，Box 12、PrismX 8、PrismY 8、Pyramid 6 个三角形均从体心朝外。日志 `Saved/Logs/BeamC3V3-PreviewDiagnosticContracts-Final-20260807.log`。ForceUnity Development Editor 链接成功；尚待用户 fresh Editor 再次视觉确认透明排序和背面剔除 |
| M7-BC-051 | 相邻 WFC 基座各生成一个扁平双轨 core，会重复消耗砖块且窄轴抗倾覆余量有限；直接取联合 AABB 又会填平 L 形缺口、角点或 ProtectedVoid。首个多轨测试还因旧 `[1]` 上界把第三轨截在 core 包络之外，重建 seat 时表现为 `RebuiltSeatUnavailable` | 旧 `CollectRoots` 只承认竖直接触，同高整边相接的 Body 永远分成多个 core；core 又固定每层 2 rails。扩成数组后仍沿用“两站位”索引会制造数据/几何身份分裂。另一个 anti-fake 风险是 region 虽合并，但 core 全落在一个旧 component 内，获得虚假的多轨/节省统计 | 冻结原垂直 component 身份后，仅对同 building/ground、正面积重叠或整边相接的基座派生 `CoreMergeRegion`；角点和正宽间隙不连。候选逐 course/rail 检查原 WFC 实体并集覆盖，footprint 必须正面积覆盖 region 的全部原接地 components；同等最小边长先最小化长宽差。`RailCount=clamp(ceil(2*sqrt(N)),2,5)`，上下层建立 `R²` 完整 36×36 bearing；所有 bounds 使用首/末站位，新增独立 merge Hash 与互斥诊断层 | ForceUnity Development Editor 成功；定向相邻双基座 fixture 1/1 通过：2 原 components→1 region→1 方形 3-rail core、每接口 9 个完整 bearing。fresh `ABTS.M73DAG.BeamC3V3.Staged` 5/5 通过，含 `SeamRelease.E6` shared、四层诊断互斥和未来阶段 fail-closed，日志 `Saved/Logs/BeamC3V3-Staged-CoreMerge-20260807-Current.log`。未跑 5×6、BeamD15、Editor/PIE 或 Chaos；一般无法容纳跨全部来源 footprint 的 region 当前应 fail closed，不得改为 AABB 填洞或调参 |

| M7-BC-052 | 不等截面 shared lane 与 720 cm 分段已实现并可编译，但固定 `SeamRelease.E6` 两次稳定报 `SharedCourseCrossSegmentTooLong:Length=936`；容易继续切更多段、偏置 child 位置或换 Seed 猜测 | 分段只能吸收两端芯体内部余量；唯一中段仍必须进入两芯各至少 36 cm。当前能达到 course 44 的高层子芯体彼此过远，使跨芯下界本身为 936 cm。加入 opening-side child reach 约束后同一身份不变，说明相关 WFC branch 没有产生既高且靠桥口的 endpoint candidate；不是 720 分段、预算或末级排序问题 | 冻结 `FSharedCourseLanePlan`、donor/receiver、冲突 rail 省略、连续 tails 与唯一 cross-core segment 合同；不放宽 720、不把拆开的 opening bridge 伪装成跨芯。停止生产 E6，先建毫秒级 `SharedEndpointReachability` 夹具，逐 branch 输出高度、bridge inset、Body/Crown 覆盖、composite conflict 与首拒原因。只有 endpoint core id 改变或新增权威中间实体后才允许一次 E6 | ForceUnity 19-action 全链和两次 4-action 增量均成功；fresh 固定测试两次各 1/1 发现、均以完全相同 `Volume=0 Course=44 Lane=0 Length=936 Opening=-701.760..-408.000` 失败。日志 `BeamC3V3-UnequalShared-Stage1Boundary-20260808-1.log`、`...-2.log`；检查点 `M73BeamC3V3Checkpoint_20260808_UnequalSharedCourse.md`。未跑 Staged 宽门、5×6、BeamD15、Chaos 或可见 PIE |
| M7-BC-053 | `SharedCourseCrossSegmentTooLong:936` 不能判断是 WFC 无解还是端点选址错误；第一次生成专用 endpoint 又依次暴露“普通 child 先占 lane”和最终 selector 仍选普通 child 的假失败 | 固定 E6 的 WFC 实际存在 `468 cm` 合法组合：负/正 inset 为 `72.24/30 cm`。旧计划选了退后过远的高层 child。若先生成全部普通 child，再加桥端，会把 280 个负端可达 cell 分成 126 个主芯冲突和 154 个非重叠接地点后全部误拒；若最终仍按距离选 core，又会让 `TowerChild` 抢占已预留的 `SharedEndpoint`，表现为横向 lane 不相交 | 新增不发射 member 的 `SharedEndpointReachability`，逐 cell 记录 span/component/side、source/branch、ground、Body/Crown 全高覆盖、inset、理论下界与首拒原因。Stage 1 在普通 child 前按 span/side 预留无同层冲突的接地 endpoint，child 避让其 lane；endpoint 不要求与 PodiumMain 投影重叠。core 冻结 `SharedEndpointSpanVolumeId` 与侧别，最终配对精确优先该身份 | ForceUnity 链接成功。fresh `SharedEndpointReachability` 1/1：1264 candidates、最短 468 cm、测试体约 83～105 ms。fresh 固定 E6 1/1：8 cores、1 intent、4 shared lanes、793 members、Static DAG accepted、Physical not evaluated。随后 fresh `ABTS.M73DAG.BeamC3V3.Staged` 精确发现 7 项且 7/7 通过；日志 `BeamC3V3-SharedEndpointReachability-20260809-1.log`、`BeamC3V3-SharedEndpoint-Stage1Boundary-20260809-5.log`、`BeamC3V3-SharedEndpoint-Staged-20260809.log`。未跑 5×6、BeamD15、Stage 2+、Chaos 或可见 PIE |
| M7-BC-054 | 把 `1x1` 可达性见证直接当生产桥端会形成约 72×72 cm 的极细高塔；直接把生产足迹扩大后，固定 E6 先连续两次以 `InvalidSupportedSpanContract:Seats=1` 失败，修正逻辑 lane 分段验证后又出现 8 个 `BeamCSupportResultantOutsideHull` | 最小见证只证明 WFC 可达，不代表生产最优。扩大后的 shared lane 总长超过 720 cm，产生两端各 252 cm tail；每个 tail 只跨外侧一个下承 rail，合力约 `-1045.6/-142.4 cm`，落在 `-1098..-1062/-126..-90 cm` 单支座域之外。验证器另有一处遗留：错误要求每个 tail 都有两个下承座，而第 21 节冻结的是逻辑 lane 并集覆盖；即使修正这点，真实静态合力仍不成立 | 分离 `MinimumWitness` 与生产足迹。生产端在 18×18 有界域内联合枚举，优先最大化双方共同短边、再方正度和面积；两端复用同一横向 stations，普通 child 避让完整足迹。当前额外要求完整 shared lane 外端到外端不超过 720 cm，使固定 E6 使用单一真实 member；不放宽合力门。超长 tail 只有新增真实内侧承座/端部传力合同后才可恢复 | 最终 ForceUnity 14/14 成功；fresh `ABTS.M73DAG.BeamC3V3.Staged` 精确发现 7 项且 7/7 通过。固定 E6 两端均为 `108×108×1728 cm`，4 条 shared lane 均为单一不超过 720 cm 的贯通 member。最终日志 `BeamC3V3-MaxSharedEndpoint-Staged-20260809-183700.log`；此前失败日志 `BeamC3V3-ExpandedSharedEndpoint-Stage1-20260809-180641.log`、`...-181246.log` 和 `BeamC3V3-ExpandedSharedEndpoint-ResultantDiag-20260809-182106.log`。物理稳定性未评估；未跑 5×6、BeamD15、Stage 2+、Chaos 或可见 PIE |
| M7-BC-055 | WFC 语义包络存在多个独立高层投影，但 Stage 1 的芯体视图只在其中部分投影中心生成子芯体；静态门仍为绿色 | 普通子芯体先按上层 Body 收集 seed，随后却以 `RootPath(DerivationPath)` 去重。同一语义根下的分离塔体因此只保留一个 `TowerChild`。旧验证只证明已发射 core 自洽，没有建立“每个可见高层投影必须有 core”的反向覆盖合同 | 在耦合基座顶面把非 `CoupledGround` Body 的 XY 投影按正面积重叠/完整边相接做确定性连通分区；每个 `FHighProjectionRegionPlan` 独立且唯一绑定一个接地 `TowerChild`，首两层必须由本分区 source Body 完整覆盖。`PodiumMain` 严格止于基座顶；region/source/core 双向身份、唯一性与计数全部 fail closed，彻底移除 RootPath 去重 | UE 5.8 ForceUnity 全链接两次成功。共享旧 RootPath 的双塔 fixture 1/1；fresh `ABTS.M73DAG.BeamC3V3.Staged` 精确发现 7 项且 7/7 通过。固定 E6 为 `HighRegions=6`、`BoundHigh=6`、`Children=6`、`Main=3`、`Cores=11`、`Members=1165`、Static DAG Accepted。日志 `BeamC3V3-HighProjection-GroundedFixture-20260809-1.log`、`BeamC3V3-HighProjection-SeamReleaseE6-20260809-1.log`、`BeamC3V3-HighProjection-Staged-20260809-1.log`；未跑 5×6、BeamD15、Stage 2+、Chaos 或可见 PIE |
| M7-BC-056 | 耦合基座只到最短接地分支的一半，视觉上明显低于 WFC setback；直接抬到最高语义分隔后，固定 E6 的 East 第二高层分区先报全部候选为 lane conflict/shared reach/no coupling，容易再次陷入调高度、调容差或换 Seed | 旧基座高度只是确定性半高，不是形态分隔。第一次抬高只改了包络切片，没有让主芯体面对全部上部投影联合选址；普通 `TowerChild` 还遗留桥口 reach 门，而专用 `SharedEndpoint` 已经接管该职责。联合预留生效后仍假失败，是最佳 Y station 恰为合法坐标 `-1`，却与 `INDEX_NONE` 共用哨兵 | 从非 Crown Body 起点由高到低选择最高合法语义 seam，检查 span 下表面、Crown、上方两层 Body 延续并量化到 72 cm；真实重切 volume、重建 ID、重映射 span supports。主芯体候选必须在每个独立高层投影内部预留 X/Y station，普通 child 移除旧 shared reach；候选是否选中改用独立布尔值，禁止空间坐标充当状态哨兵 | ForceUnity 全链接成功。固定 E6 三基座 `Core 504->1152`、`East 648->2448`、`West 576->1368 cm`；最终 `Volumes=25 Cores=11 Main=3 Children=6 HighRegions=6 BoundHigh=6 Shared=4 Members=1533 StaticDAG=Accepted Physical=NotEvaluated`。fresh Shape Grammar V2 8/8、Beam-C3 V3 Staged 7/7；日志 `BeamC3V3-SemanticPodium-DAG5Bv2-20260809-1.log`、`BeamC3V3-SemanticPodium-SeamReleaseE6-20260809-6.log`、`BeamC3V3-SemanticPodium-Staged-20260809-1.log`。未跑 5×6、BeamD15、Stage 2+、Chaos 或可见 PIE |
| M7-BC-057 | Stage 1 视觉验收通过后，旧 `Stage1.Matrix` 仍会生成共同外框/楼层/屋顶并检查已被不等截面 shared lane 取代的双端槽位等式，不能作为当前 CoreAndShared 的 5×6 门；新增严格 Stage 1-only 矩阵后仅 11/30 通过 | 19 个失败分三类：4 个 E2 在未来 shell band 的 `EmptyOccupiedBand` 提前失败；12 个宽基座叶要求一个最多 648 cm 的 `PodiumMain` 同时在 3～6 个分离高层投影内部提供 X/Y station，报 `ContinuousGroundedCoreUnavailable`；3 个叶所有语义候选失败但调用层输出 `Last=` 空串，丢失权威拒因。相邻更高 Tier 可通过，证明不是单调预算或总体高度问题 | 保留新的 `Staged.Stage1CoreAndSharedMatrix` 作为唯一当前矩阵。修复顺序冻结为：先保留每个 semantic candidate 的 Last/Best reject；再把与 Stage 1 无关的 shell/floor band 构造移到未来阶段或证明其 core 必要性；最后为宽耦合基座设计多个 `PodiumMain` 接地网络或等价可追溯主/子归属。不得扫描 Seed、Attempt、轨数、基座比例、预算、容差或 720 cm | ForceUnity 4/4 成功。fresh matrix 精确 Found=30，Passed=11，Failed=19，约 55 秒；通过格为 ColumnBreak E1/E3/E6、DropTrigger E1/E2、SeamRelease E1/E5/E6、SlideRelease E1、TipOver E1/E6。三类代表 `ColumnBreak.E2`、`DropTrigger.E3`、`DropTrigger.E6` 均在独立 fresh 进程 1/1 复现。主日志 `BeamC3V3-Stage1CoreAndShared-Matrix-20260809-1.log`，隔离日志 `BeamC3V3-Stage1Matrix-Isolated-*-20260809-1.log`；Physical 未评估，未进入 Stage 2、BeamD15、Chaos 或可见 PIE |
| M7-BC-058 | `Stage1CoreAndSharedMatrix` 的 3 个 `NoSemanticCandidate` 叶只报告 `Last=` 空串，无法判断是 Profile、Shape Grammar、语义里程碑还是后续芯体规划失败 | Stage Preview 循环把临时 `CandidateError` 直接用于最终汇总；Profile/Shape 失败会填写它，但 `MeetsSemanticVisualMilestone == false` 不写错误，因而“全部候选都在语义门失败”会稳定丢失原因。只看最后一次也可能错过更接近成功的候选 | 每个 Attempt 独立记录 CandidateSeed/Gate/Reason，汇总三类拒绝计数、最后拒绝和按违反谓词数→Volume/Span/Roof 缺口排序的最佳语义候选。新增 `Staged.StagePreviewFailureIdentity`：目标未来成功也通过，若失败则要求所有诊断字段非空；不改变候选选择和任何生成参数 | Development Editor 增量链接成功；fresh 诊断契约 1/1。三个隔离叶均为 Profile0/Silhouette0、全部 Attempt 在 SemanticMilestone 被拒：ColumnBreak E5 最佳 20/21，DropTrigger E6 最佳 22/25，SlideRelease E6 最佳 21/25；跨度和屋顶均已满足。日志 `BeamC3V3-StagePreviewFailureIdentity-Best-20260810-103309.log` 与 `BeamC3V3-Stage1-NoSemantic-*-20260810-*.log`。证明三项尚未进入芯体、预算、720 cm 或静态 DAG；下一步仍按顺序调查 E2 EmptyBand |
| M7-BC-059 | 四个 E2 Stage 1 叶在芯体发射前稳定报 `BeamC3V3EmptyOccupiedBand` | `ComponentBands`、occupied raster 和外部空域 mask 只服务未来 `CompleteStaticDAG` 的 shell/floor/post/common-frame；`CoreAndShared` 不消费这些数据。Stage 1 提前构造未来 band，把语义轮廓刻意留空的高度切片误判成当前失败 | 把 band schedule/raster 严格门控到 `CompleteStaticDAG`；Stage 1 明确发布空 `BandBaseCourseIndices`，不填空 WFC、不改变芯体、Seed、预算或容差 | Development Editor 增量链接成功。`ColumnBreak/SeamRelease/SlideRelease/TipOver.E2` 分别在 fresh NullRHI 隔离进程 1/1 通过，静态 DAG Accepted、Physical NotEvaluated；日志 `BeamC3V3-Stage1-BandBoundary-*-20260810-*`。下一步才进入多 PodiumMain 合同与三投影 fixture |
| M7-BC-060 | 12 个宽耦合基座叶稳定报 `ContinuousGroundedCoreUnavailable:HighProjectionEntries=3..6`；单个最长 720 cm 主芯体无法同时在全部分离高层投影内提供 X/Y station | 旧合同把“一栋建筑一个耦合基座”错误等同于“一个基座只能有一个 PodiumMain”，且单个候选还必须覆盖全部原始接地组件。这不是轨数、预算、Seed 或容差问题 | 一个 CoreMergeRegion 允许多个真正接地且止于同一 seam 的 PodiumMain。按高层投影 coverage mask 有界保留候选并做确定性最小 set-cover；组合无同轴 lane 冲突。每个 region 显式记录 direct main 与 child，child 必须与该 main 重建双向正交 bearing | 三投影纯数据夹具修改前精确复现、修改后 1/1；原 `DropTrigger.E3`、双投影层级夹具、`SeamRelease.E6` 均 1/1。第一次全矩阵从 11/30 提升至 27/30，全部旧 MainCore/EmptyBand 失败消失。日志 `BeamC3V3-MultiPodiumMain-*.log`，Physical NotEvaluated |
| M7-BC-061 | 多主芯体后仅 `ColumnBreak.E5`、`DropTrigger.E6`、`SlideRelease.E6` 在芯体前因 Volume=20/22/21 低于 21/25 失败；增加 Attempt 仍不能越过 | 高 Tier milestone 完成深度 0 split、深度 1 setback 后，代码仍在所有剩余深度无条件 `Stack`，把 72 软叶预算退化为线性堆叠。普遍释放加权细节虽补足体积，却改变 `BridgedArcology` 桥端层级并使已批准 `SeamRelease.E6` 无可用 endpoint lane | 非桥接原型在显式 macro step 后回到既有加权 Stack/Setback/SplitX/SplitY；Tier 0/1 保留基础 stack，`BridgedArcology` 保留已批准的线性上层和 shared-endpoint 拓扑。不降低体积门、不增 Attempt、不扫描 Seed。任何全局 grammar 修复先隔离复核桥接基线 | ForceUnity 增量链接成功；DAG5Bv2 8/8，三个原语义失败叶、`SeamRelease.E6`、多主芯体夹具、旧层级夹具均 1/1。最终 Stage1-only 5×6 精确 30/30，约 276.9 秒，日志 `BeamC3V3-Stage1Matrix-Final-20260810.log`；未运行 Stage 2+/BeamD15/Chaos/可见 PIE，非桥接 E5/E6 需视觉复核 |
| M7-BC-062 | Stage 1-only 5×6 静态门 30/30 后，PIE 仍发现 `ColumnBreak.E6` 裙房分隔偏低、`DropTrigger.E6` 宽基座中心大片无芯体；容易把 `High==Bound` 和 DAG Accepted 再次误当视觉完成 | 两个独立合同缺口叠加：semantic podium 只从非 Crown volume 的 `Min.Z` 枚举候选，未进入集合的可见截面边界没有任何拒因；多 PodiumMain set-cover 只覆盖高层投影并优先最少 main，没有基座面积、最大空洞或质心分布目标。因此所有 child 都可绑定且静态 DAG 全绿，同时轮廓分隔和芯体空间分布仍错误 | 每个 applied root 与 building-wide common probe 逐候选发布 seam、量化顶面、span cap、接受状态和首拒原因；每个 merge region 以 36 cm 精确基座并集发布 main/全部芯体覆盖、未覆盖格、最大空洞半径和质心间隙。先硬门诊断完整性与计数闭合，质量指标首轮只观测；下一步先分类 ColumnBreak 可见边界身份，再为宽基座增加有限支承需求点，禁止调 Seed/Attempt/轨距/容差/720 cm | Development Editor 编译成功（61.74 秒）。fresh NullRHI 矩阵 30/30，叶测试约 286.8 秒，日志 `BeamC3V3-Stage1Matrix-Diagnostics-20260810.log`。`ColumnBreak.E6` 三作用域均 Candidates=0、回退 720/504 cm；`DropTrigger.E6` 全芯体覆盖 29.4402%、2193 格未覆盖、最大空洞 572.049 cm、质心间隙 180 cm。全矩阵候选明确拒绝为 ConsumesCrown 21 次、AboveIncidentSupportedSpanUnderside 12 次。Physical 未评估，Stage 1 视觉合同未完成 |
| M7-BC-063 | 诊断已证明 ColumnBreak 的可见分隔全部是 Crown 起点、DropTrigger 的 child 也全部绑定后，仍容易直接固定抬高裙房或继续给 set-cover 调面积/距离权重 | 排除 Crown 使合法可见边界永远不进入候选；直接使用 Crown 面又会吞掉屋顶并失去 Body 承接。宽基座的最少投影覆盖没有“共同基座中央必须有 main”的离散需求，同样数量的合法组合可以把中部留空 | Crown onset 可作候选见证，但量化顶面必须在其下保留至少两个 36 cm Body course，重切不得消费 Crown；逐候选发布 `StartsCrown`。基座从精确 36 cm 占用并集求一个实际落在实体内的 support anchor；最少 set-cover 同数目优先覆盖，必要时至多增加一个无 lane 冲突、完整接地 main，无候选则 fail closed。覆盖率/空洞仍只观测，不冻结经验阈值 | ColumnBreak E6 由零候选回退改为 Left/Right top `1368/1008 cm`，E5、SeamRelease E6 隔离回归通过。DropTrigger E6 为 3 main/10 cores/1060 members，AnchorCovered=1，覆盖 34.8456%、最大空洞 494.263 cm、质心间隙 0。三投影夹具 1/1；fresh Stage1-only 5×6 为 30/30、约 396.3 秒；ForceUnity 10/10。日志 `BeamC3V3-Stage1Matrix-SemanticBoundary-PodiumAnchor-20260810.log`。Physical 未评估，仍须视觉复核 |

### 9.2 第一版已证明无效或不足的路线

下列结论以 [Beam-C3 暂停检查点](M73BeamC3Checkpoint_20260805.md) 的完整日志和恢复后撤回记录为准。
除非合同、权威几何或新的逐跳证据已经发生可说明的变化，否则不得换一个参数或 Seed 重复试验。

| 路线 | 结论 | 再次尝试的前置条件 |
| --- | --- | --- |
| 按距离合并相邻 Z 区间 | 无效且会制造假承接 | 必须改为最终真实 `BearingContact` 路径；距离接近本身永远不是承接 |
| 只恢复 `CorePost` 角色或旧 Member ID | 不足以恢复实体下端面和向地路径 | 先证明最终几何链存在，再提供闭合前后身份 remap；不能只改语义 |
| 批量删除普通 frame/roof donor | 会破坏承重祖先、屋顶合同并诱发更多 C2 补柱 | 逐组事务删除，先证明完整祖先/后继 load cone、屋顶保护和删除后 720 cm |
| 扩大 Portal、Biaxial、PhysicalBridge、Full-section face 或容差覆盖 | 只能改善局部候选，不能替代锚端向地承接和预算可行性 | 必须有固定失败站点的逐跳接触证据，说明缺失确由候选覆盖造成 |
| Candidate Gate、Host 深度、Bounds、MaxBays、Attempt、Reserve、复用 Tie 柱等参数排列 | 已撤回；曾改变搜索顺序或局部数量，但未同时改变最终 DAG、Bearing、跨度和 Brick 窗 | 先提出新的结构因果链及预期 Hash 变化；禁止参数网格搜索和特殊 Seed 绕门 |
| 加入精确 25/75 Z lane 后直接继续重试 | lane 可能被权威重闭合吞并；“Added”不等于最终实体改变 | 必须比较重闭合后的 Member/Bearing/DAG 身份，并按同一失败 DAG 限制事务 |
| 放宽 720 cm、49/199、1499 窗，跨 Source 借根，虚构 Bearing 或隐式锁定 | 违反冻结合同，不属于修复 | 只能由设计/集成所有者明确修改稳定合同；功能工作树不得自行采用 |

### 9.3 后续复杂修复的执行阶梯

1. **先证伪物理前提。** 目标若是摩擦稳定，最小 Chaos 夹具是架构门，不是最后的展示验收。最小夹具失败时，
   静态证据只能帮助定位，不能成为继续扩大生产实现的理由。
2. **先做可行性，再做搜索。** 先计算预算、几何和硬合同下界；不可行就返回上游设计，不用更多 Seed、Attempt
   或 donor 启发式掩盖。
3. **一次只改变一个可解释主变量。** 修复可包含完成该假设所必需的配套代码，但实验记录必须能指出是哪一个
   因果变量、预计改变哪个权威签名；所有参数元组登记后去重。
4. **最小失败例先行。** 固定 Profile/Tier/Seed、节点和失败站点，先跑精确 Filter；专项没有产生预期的
   pre/post 差异时，不得运行完整 Beam-C3、D1 或生产矩阵。
5. **以最终权威状态判定进展。** 候选数、进入分支、临时 Member 数和 `Added` 日志都不是进展；重闭合后的
   Brick、Bearing、rooted evidence、Failure DAG 与认证结果才是。
6. **逐级扩大证据且不得越级。** 专项通过后先同输入双次重放，再跑受影响套件和完整静态回归；最后由 fresh
   可见 PIE/Chaos 证明动态稳定。NullRHI、截图、编辑器预览与 Chaos 互不替代。
7. **把停止视为正常结果。** 同身份两次、单假设两轮、60 分钟无新增结论或同路线 4 小时未越过最小门槛时，
   立即冻结现场。检查点至少记录源码/二进制身份、精确命令、唯一日志、已排除假设、禁止重复路线和下一项
   最小证伪实验。
8. **宽门槛只做里程碑，不做盲调内循环。** 重型构建、完整 D1/D1.5、5×6 和正式 PIE 串行执行；轻量
   NullRHI 仍遵守多工作树并发上限、唯一日志、预期测试数和 fresh 进程合同。
9. **视觉拒收先转成可反证合同。** 本轮固定状态机为 SharedSlot/BuildingGroup/FullFaceBearing 三个最小
   anti-fake → `SeamRelease.E6` → 一个无 Span 普通边界叶 → G0/G1 → 一次 5×6 → 一次 BeamD15 →
   用户视觉。前一状态未过不运行后一状态；禁止扫描 Seed、Attempt、密度、gap、预算 cap、截面、容差、
   摩擦或 solver。若 Matrix 只暴露确定性数量窗变化，只允许一次 owner-kind 精确计数调查并回到设计层。

## 10. 待集成工作树提炼

当前建议按以下顺序上收到 [DevelopmentTroubleshooting.md](DevelopmentTroubleshooting.md)：

1. `M7-WT-002`：安装版/源码版 UE BuildId 不一致导致模块缺失。
2. `M7-DAG3-001`、`M7-DAG4-001`：编辑器预览、PIE 真实 Actor 和诊断覆盖层生命周期。
3. `M7-DAG5-001`～`004`：候选搜索不是深度扩展，以及下游预算导致所有 Seed 同门失败。
4. `M7-BA-001`～`007`：Bearing Contact、公共边界实际 Gap、Z 柱站位和全局装配。
5. `M7-BB-001`～`008`：Port 假绿灯、斜撑延期、语义屋顶、桥端逐梁承托与 split/merge 顺序穿透。
6. `M7-BC-002`、`003`：声明 Bearing 与最终 Brick 真实接触的权威切换。
7. `M7-D1-003`～`010`：屋顶逐层直接承接、聚合 Crown、长轴屋脊、闭合无进展保护，以及 5×6 叶子拆分和阶段计时。
8. `M7-BC-004`～`017`：静态 DAG 不等于 Chaos 自稳定、四柱闭环、低 Tier 普通框架替换、
   C2 最终硬预算、严格根系普通楼层网、Host/高度候选时序、短 Z 承接面，以及 v9 的
   17/17、5×2、Drop Tier 4、Source-aware 跨 Bay Portal、按失败 DAG 有界的实体 cap、Column 高 Tier，
   以及尚待执行的完整 D1/D1.5、5×6 与 PIE 证据层。
9. `M7-BC-018`～`051`：先证伪物理前提、先做预算可行性、按最终权威身份判进展、分层测试、
   单变量实验卡、同 Hash/no-progress 停止、60 分钟/4 小时时间盒与可恢复 checkpoint；这些条目适合由
   集成工作树提炼为全项目开发效率原则。`M7-BC-042`～`044` 还固定了“总 DAG 绿灯不能证明存在显式
   core”、shared course 必须绑定绝对相位和连续接地 endpoint、以及一次计数调查后冻结预算窗口的
   anti-fake 数据合同与视觉拒收回退顺序；`M7-BC-045`～`047` 进一步固定“同 member 双槽位且完整桥带”、
   “一个 building group 共同外框”和“`36×36 cm` 完整承压面加外挑”，并证明局部数量、局部四面 mask
   与正面积 Bearing 都可能形成视觉假绿。
   `M7-BC-048`～`051` 又固定了最终反力身份与 ProtectedVoid 的边界、“生产真实早停 + 诊断层同 Hash
   且可见集合互斥”，以及“相邻基座派生合并但不得填平空洞、多轨必须真正跨来源并形成 R² bearing”
   替代从完整成品反猜上游或以大 AABB 冒充大芯体的工作法。

## 11. 相关文档

- M7 总导航：[M7BuildingDevelopmentRoadmap.md](M7BuildingDevelopmentRoadmap.md)
- 多工作树规范：[ABTSMultiWorktreeDevelopmentGuide.md](ABTSMultiWorktreeDevelopmentGuide.md)
- 共享排错总文档：[DevelopmentTroubleshooting.md](DevelopmentTroubleshooting.md)
- DAG 总路线：[M73RecursiveSupportDAGProceduralBuildingGenerationResearch.md](M73RecursiveSupportDAGProceduralBuildingGenerationResearch.md)
- DAG5：[M73DAG5CandidateSearchSemanticEnvelopeAndProductionDesign.md](M73DAG5CandidateSearchSemanticEnvelopeAndProductionDesign.md)
- Beam-A：[M73BeamAStructuralIRPreviewDesign.md](M73BeamAStructuralIRPreviewDesign.md)
- Beam-B：[M73BeamBMotifWFCAndGraphGrammarDesign.md](M73BeamBMotifWFCAndGraphGrammarDesign.md)
- Beam-C / C2 / C3：[M73BeamCLoadDAGAndStaticProxyDesign.md](M73BeamCLoadDAGAndStaticProxyDesign.md) · [M73BeamC2RealContactAndLoadClosureDesign.md](M73BeamC2RealContactAndLoadClosureDesign.md) · [M73BeamC3CribCoreStabilityDesign.md](M73BeamC3CribCoreStabilityDesign.md)
- Beam-C3 V3：[M73BeamC3V3SkeletonFirstBuildingGenerationDesign.md](M73BeamC3V3SkeletonFirstBuildingGenerationDesign.md) · [M73BeamC3V3Checkpoint_20260807.md](M73BeamC3V3Checkpoint_20260807.md)
- Beam-D0 / D1 / D1.5：[M73BeamD0GameplayProfileCatalogDesign.md](M73BeamD0GameplayProfileCatalogDesign.md) · [M73BeamD1RealBrickAndMaterialRolesDesign.md](M73BeamD1RealBrickAndMaterialRolesDesign.md) · [M73BeamD15VisualComplexityLadderDesign.md](M73BeamD15VisualComplexityLadderDesign.md)

## 12. Beam-C3 V3 Stage 1 空间覆盖补充

| ID | 现象 | 根因 | 修复方向 | 证据/状态 |
| --- | --- | --- | --- | --- |
| M7-BC-064 | `TipOver.E6` 静态门为 `High=Bound=Children=7`，PIE 仍看到基座右侧空缺和高柱无全高芯体；若只看入口计数会再次形成假绿灯 | PodiumMain 规划是“最少数量覆盖全部入口投影”的 set-cover，只附加单个中心 anchor，不含联合覆盖、重叠面积、最大未覆盖岛、四向缩进或子芯体全高 lane 余量。逐层诊断还证明 7 个高层分区在 `1440 cm` 裙房顶面已经彼此独立，没有一分三；真正缺口是固定 footprint 的 west child 3 在收缩 Crown 起点受完整 rail 覆盖和 composite lane conflict 限制，于 `3240 cm` 终止，而 WFC 分支持续到 `3876 cm`。旧合同只证明入口绑定，不证明 terminal branch 持续到顶 | 先保留每个原始接地来源覆盖、未覆盖岛、main 四向缩进/两两重叠/选择理由、seed/邻接/逐 course 连通分区/split/terminal binding 诊断。下一步先建立“最终分支到最高完整 course 均有接地 child”的合同，再定义有限的 main 联合覆盖与重叠目标，同时保护 child 可用 lane。禁止调 Seed/Attempt/轨距/预算/720 cm/容差 | Development Editor 全链接成功；fresh `TipOver.E6` 1/1 Success，Static DAG Accepted、Physical NotEvaluated。基座 1680 格中 main 921、any 951、uncovered 729；main 重叠 `44064 + 22032 cm^2`；west child `3240 < 3876 cm`。日志 `BeamC3V3-TipOver-E6-RootCause-Final-20260810.log`。主芯体耦合/均匀覆盖明确登记为待解决，本轮未修改生成算法、未跑 5x6/Chaos/可见 PIE，未触碰用户地图 |
| M7-BC-065 | `TipOver.E6` 的入口 region 与 child 数均闭合，但固定 footprint 子芯体在收缩 Crown 前提前终止；旧日志只能说“某条 composite lane 已占用”，无法区分 WFC 无全高形状、main 先占 lane、sibling 冲突、shared reservation 或缺少正交耦合 | 旧顺序先按入口 coverage 选 PodiumMain 并发射完整 course，再枚举 child；main 的 set-cover 完全不知道每个终端分支的全高固定-footprint候选。child 即使在 WFC 中有通顶位置，也可能在进入实际选择前被 main-first lane 占用；旧合同只验证入口绑定，没有验证 terminal course | 先在未发射 main 时逐 region 枚举并保留最高完整 course 的固定-footprint WFC 见证；PodiumMain 候选增加 full-height compatibility mask，按 `coverage + compatibility` 有界保留；set-cover 只接受覆盖全部 full-height region 且 main 间无 lane 冲突的组合。落地 child 时只准使用全高见证，并分别记录 main/sibling/reservation/coupling 拒因；不能回退较矮 child。主芯体重叠和均匀覆盖仍作为独立问题，不在本修复中调参 | Development Editor 编译成功；fresh `GroundedPodiumCoreHierarchy` 1/1，日志 `BeamC3V3-JointSelection-PureFixture-20260810-191623.log`；fresh `TipOver.E6` 1/1，日志 `BeamC3V3-TipOver-E6-JointSelection-20260810-191829.log`。803 个 main 中 252 个零全高兼容，访问/可行组合 21600，选 3 main；7 个 child 达到 required course `107/107/50/50/60/60/60`，west child 由 90 提升至 107。Static DAG Accepted、Physical NotEvaluated；未跑 5×6/Chaos/可见 PIE，用户地图未触碰 |
| M7-BC-066 | `TipOver.E6/730000` 可通过，但 `710000` 右侧 main、基座及两个 terminal child 同时消失；旧测试仍因入口 region/child 计数自洽而假绿。扩大到 `DropTrigger.E5` 后联合搜索又在根状态失败，且单叶一度超过十分钟无新证据 | 需求全集错误地由已有 main/入口绑定反推，造成“无 main→无 child demand→无需 main”的循环论证；随后又把“每个独立接地 child 必须直接接触某个 main”误作存在门，而 DropTrigger E5 的一个合法终端 region 对 retained main 的直接接触候选数为零。另有局部 diagnostic ID 汇总时原地改写造成跨 component 碰撞，以及无状态去重/feasibility 缓存导致组合搜索膨胀 | 先由 WFC Body/Crown course-slice DAG 独立生成 terminal demand 全集，再枚举全高接地 child；main 按空间 `CoverageMask` 覆盖裙房入口，对每个 main 组合做全体 child 的精确无冲突 assignment。直接 main/child bearing 只作偏好，无接触时记录最近 main 为 Stage 1 归属，建筑级耦合留给 Stage 2；真实接触仍双向验证。分离 `LocalProjectionIndex`/全局 ID，预留精确 shared endpoint；规范化去重 main 状态、缓存 child feasibility、候选最少 region 优先。十分钟无阶段证据即停止并先补计时/剪枝，禁止扫 Seed/加 Attempt/调 720 cm、轨距或容差 | 固定 `TipOver.E6/710000` 为 Main 3、Children 8、Terminal 8/8、Static DAG Accepted；两个纯数据夹具各 1/1。最终五个 Profile 各 6/6、总计 30/30，日志 `M73-BeamC3V3-Stage1-FinalMatrix-*-20260811.log`，单 Profile 约 81--169 秒。Physical NotEvaluated，未运行 Stage 2+/Chaos/可见 PIE；M7-BC-064 的 main 重叠/均匀覆盖仍开放 |
| M7-BC-067 | Stage 1-only 5x6 虽能 30/30，但 Profile 组耗时约 81--169 秒，单叶可能超过一分钟；旧日志只有 C3V3/BeamC 总时间，无法判断是 terminal demand、child/main 候选、联合搜索、IR 发射还是静态 DAG 占主导，也没有发行可用的超时门 | 验证长期把“最终完成”优先于“交互时限”，候选枚举和 set-cover 只做结果有界，没有墙钟预算；因此算法退化时可持续消耗时间，开发者只能在整叶结束后看到一个汇总值，容易再次形成长时间调参与路径依赖 | 冻结代码/文档基线后，为单个 `CoreAndShared` 叶发布 `TerminalDemand / ChildCandidate / PodiumMainCandidate / JointSelection / MemberEmission / StaticDAG` 六段计时和权威总墙钟；候选及搜索每 256 状态轮询，超过 10000 ms 以 `BeamC3V3Stage1Timeout` 和阶段名失败关闭。D1 Summary、Stage1 日志和 CandidateTiming 必须同步暴露证据；进程启动与矩阵调度另计，不得用提高门限、扫 Seed/Attempt 或削弱几何合同换取通过 | 基线提交 `83b1ebc621ec4303908b039a0b0a5f323973edbe`。Development Editor 编译成功；fresh `Stage1TimingBudget` 1/1。固定 `TipOver.E6/710000` 于 `10007.372 ms` 在 `PodiumMainCandidate` 正确失败关闭，分段为 Demand `0.236`、Child `1146.978`、Main `8860.000 ms`，证明下一性能工作应先处理 main 候选。日志 `BeamC3V3-Stage1TimingBudget-Final-20260811.log`、`BeamC3V3-Stage1Timing-TipOver710000-Final-20260811.log`；地图始终排除 |
| M7-BC-068 | 加入 10 秒门后，`TipOver.E6/710000` 的 main 阶段枚举 582335 个矩形并产生 1611770 个不同长轨道查询、约 6477 万次缓存命中；main 优化后，`SeamRelease.E6` 又在生产级 shared endpoint 联合阶段超时，证明单纯给完整矩形结果做 memo 仍会被区间数量和全高重复扫描拖垮 | 缓存键仍含轨道起止点，区间组合数接近矩形数；所有可能进入保留桶和必然被前 12 名严格支配的候选都先从地面扫到顶部。shared endpoint 又独立重复同一套 source/连续覆盖工作。热点不是 DAG、构件发射或兄弟冲突本身，而是对同一 course/axis/cross row 的不同长度区间重复做连续几何证明 | 将连续轨道精确分解为 36 cm 原子固体，按 row 建 coverage prefix；中心 source 按半格/course/Body-Core 精确缓存。只有可能改善 `CoverageMask + CompatibilityMask` 前 12 名的 main 才做全高验算；相等候选不剪。最终 child 复用已证明的全高候选，生产 shared endpoint 使用同一等价缓存；存在性搜索先访问较小 witness，但不改变最终视觉排序。保留六阶段 10 秒 fail-closed 门和 main/joint 规模日志 | ForceUnity Development Editor 全链接成功。fresh 30/30：算法总计 `32859.48 ms`、平均 `1095.32 ms`、中位 `956.73 ms`、最慢 `SeamRelease.E6=3506.38 ms`。固定 TipOver 710000/730000/750000 为 `1121.85/809.72/840.95 ms`，710000 的 `Stage1Hash=6381458846136252022` 未变。日志 `BeamC3V3-Optimize8-Stage1-5x6-20260811.log`、`BeamC3V3-Optimize8-TipOverSeeds-20260811.log`；Physical NotEvaluated，地图始终排除 |
| M7-BC-069 | 屋顶语义合并、上层分叉和 Body 堆叠都只存在于生成器内部；自动化只能看到最终 `HighProjectionRegions`，编辑器也无法判断一个 Crown terminal 对应几个真实方形 Body 支撑柱。直接用屋顶叶数作为芯体需求会在“两个 Body 共用一个屋顶”时少算，也会在一个 Body 上方屋顶分叉时多算 | 旧 `BuildRequiredHighProjectionDemands` 以逐 course 连通分量的叶节点定义几何 terminal，它适合冻结当前 child 选择，却没有发布原始 WFC Volume DAG、接触面的 split/merge、逐 course 占用或独立 Body demand；因此计数相等只能形成假绿灯，计数不等也无法解释 | 新增只读 `SemanticSupportDemandDAG`：直接复用已接地的 `FVerticalSupportWitness`，发布 Volume node、按接触面二部连通分量建立的 merge ledger、36 cm course occupancy bitset，以及按最高非 synthetic Body 定义的独立 semantic terminal demand。独立 Hash 不进入 Core/Support/FinalGeometry Hash；Preview Actor 第 7 层互斥显示原始方形 Body、support edge 和连续 fit/ground projection。本轮不让新图参与 PodiumMain、TowerChild 或 shared 选型 | ForceUnity Development Editor 成功；fresh `SemanticSupportMergedRoofDemand` 1/1 固定旧 geometry terminal=1、新 Body demand=2、ledger=2→1；`PreviewDiagnosticContracts` 1/1；TipOver 710000/730000/750000 原 Stage1Hash 保持 `6381458846136252022 / 4360458529242908803 / 2446638802680686583`；fresh Stage1-only 5×6 30/30，算法总计 `38903.91 ms`、平均 `1296.80 ms`、最慢 `3788.97 ms`。日志 `BeamC3V3-SemanticSupport-MergedRoof-20260811-155710.log`、`BeamC3V3-SemanticSupport-PreviewContracts-20260811-155750.log`、`BeamC3V3-SemanticSupport-TipOverSeeds-20260811-155832.log`、`BeamC3V3-SemanticSupport-Stage1-5x6-20260811-155934.log`；Physical NotEvaluated，地图始终排除 |
| M7-BC-070 | 第 7 层同时绘制方形 Body、连续 fit/ground projection 实体盒与支撑边；复杂 WFC 中内部边线被实体盒遮挡，无法单独检查图连通关系 | 支撑需求图数据正确，但 Preview Actor 原先把“完整语义上下文”和“只看边线”绑定成唯一显示方式，缺少纯表现过滤；改变生成数据或材质透明度都会扩大问题范围 | 在第 7 层增加 `Hide Support Demand Volumes (Lines Only)` 复选框：只关闭 Body 与 demand Box，始终保留 Body→Body/Crown 支撑线。开关不参与生成、Hash 或物理，仅在该诊断层显示，并在日志发布状态 | UE 5.8 ForceUnity Development Editor 全链接成功；fresh NullRHI `PreviewDiagnosticContracts` Found=1、Passed=1、Failed=0，日志 `BeamC3V3-SupportLinesOnly-20260811-162932.log`。启动期 UnifiedError 自测仍输出既有 `Condition failed` 噪声，发生于目标测试入队前；目标测试明确 `Result={Success}`。地图继续排除 |
| M7-BC-071 | 共同 `CoupledGround` 使全部 semantic demand 回溯到同一 DAG 根；若直接以根数作为局部裙房/主芯体数量，连续基座内部的多个真实 Body 支撑区再次被吞并。反过来，按屋顶 terminal 或任意视觉切缝分区也会重复少算/多算 | DAG 根表达语义接地连通，不表达共同基座内部的荷载汇水范围；当前又尚未生成最终 brick ground-seat。缺少位于二者之间、能精确覆盖 ground occupancy 且把每个 Body demand 唯一归属的诊断层 | 以 terminal ground projection 为种子，在 course-0 occupancy 上构造确定性 Manhattan support province；正面积重叠 seed 先合组，全部占用格互斥且完备分配，等距按稳定 demand ID 决胜，四邻域形成边界。逐省份发布保守 fully-occupied/proposed top、归属、邻接、fallback 和独立 Hash；第 8 诊断层显示地面/候选顶面/归属线。本轮严禁消费省份修改芯体 | ForceUnity Development Editor 成功；`SemanticSupportMergedRoofDemand` 1/1、`PreviewDiagnosticContracts` 1/1、TipOver 三 Seed 1/1、Stage1-only 5×6 30/30。5×6 省份数 `1..8`、nearest seed fallback 总计 0、最慢 `SeamRelease E6=6423.79 ms<10 s` 且 Demand 段仅 `2.42 ms`。日志 `BeamC3V3-SupportProvince-MergedRoof-20260811-170530.log`、`BeamC3V3-SupportProvince-Preview-20260811-170624.log`、`BeamC3V3-SupportProvince-TipOverSeeds-20260811-170711.log`、`BeamC3V3-SupportProvince-Stage1-5x6-20260811-170805.log`；Physical NotEvaluated，地图继续排除 |
| M7-BC-072 | 支撑省份进入 PodiumMain 选择后，`ColumnBreak.E6` 可通过，但 `DropTrigger.E6` 在 JointSelection 稳定撞到 10 秒门；早期版本预计算约 1897 万条 main-child 关系并访问 121246 个完整 main 组合，删除字符串状态开销后仍只是在单位时间内访问更多无用状态 | 新增省份需求本身只需毫秒；真正热点是为所有 retained main 预计算精确 child 位集、找到可行最小方案后继续枚举同数量 tie、以及直到完整覆盖后才证明数量下界。单纯换容器、扩大门限或减少省份都会掩盖而不解决组合爆炸；坐标字段另曾错误使用 `INDEX_NONE=-1` 判空，导致合法负格坐标被静默当成无 anchor | 省份 anchor 使用显式存在位；main 候选增加省份覆盖 mask，发射后逐省份绑定实际 PodiumMain 并发布独立 Hash。联合选择前移单 main 的 full-height/shared 必要条件；child 位集按需缓存；确定性贪心只提供可行上界；`region+province+中心 anchor` 唯一覆盖签名 BFS 提供严格数量下界。上界等于下界才跳过等价几何枚举，否则继续精确搜索并保留 10 秒 fail-closed。禁止调 Seed/Attempt/36 cm/720 cm/轨距/桶容量或删除诊断需求 | UE 5.8 ForceUnity Development Editor 全链接成功；fresh `DropTrigger.E6` 由超时降至 `2009.92 ms`，Joint `68.88 ms`，8 省份→5 main，覆盖上下界同为 5；`ColumnBreak.E6` `2488.63 ms`，4 省份→2 main 且 shared 保留；合并屋顶夹具 2 省份→2 main；TipOver 三固定种子通过。fresh Stage 1-only 5×6 为 30/30，平均 `919.24 ms`、中位 `729.00 ms`、最慢 `SeamRelease.E6=3536.32 ms<10 s`，日志 `BeamC3V3-ProvinceMain-Stage1-5x6-20260811-184252.log`。所有叶 Static DAG Accepted、Physical NotEvaluated；可见 Editor 视觉仍待本停点验收，地图继续排除 |
| M7-BC-073 | `TipOver.E6/750000` 的 semantic demand DAG 明确显示右上支撑，但旧 `High=Bound=Children=8` 与 Static DAG 仍通过；只看数量无法判断 child 是否真的属于该 Body、是否有 demand 共用同一 child、是否存在孤儿 child，或 child/main 只是逻辑归属 | semantic demand 与旧 course-slice HighRegion 是两套身份。旧门只验证每个 HighRegion 绑定一个独立接地 child；HighRegion 的合并来源可让两个 terminal Body 都沿血缘命中同一个 child，同时另一个 child 无 semantic owner。`PodiumMainCoreCellId` 也允许记录最近 main，而不要求真实跨芯 bearing，因此静态 DAG 对这些错配天然假绿 | 增加只读 `SemanticDemandCoreBinding`：逐 demand 发布 source/spatial 候选、region/child/main、Body 重叠、continuous-fit、实际跨芯 bearing、复用倍数与映射理由；汇总 unmapped/ambiguous/outside/no-direct/reused/orphan 及独立 Hash。第 10 互斥层画 demand→child→main，逻辑但无接触的后半段加粗，无候选 demand 竖直标错。诊断异常暂不改变生成接受，下一修复必须建立 semantic demand 到 child 的空间唯一合同 | UE 5.8 ForceUnity Development Editor 全链接成功；fresh Preview 合同 1/1，TipOver 710000/730000/750000 专项 1/1。750000 保持 `FinalGeometryHash=-5115885695694694918`，但精确暴露 `Demand 4/6→Child 4`、Demand 4 overlap=0/outside、ReusedChildren=1、OrphanChildren=1、NoDirectMain=3。日志 `BeamC3-DemandCore-Preview-20260811.log`、`BeamC3-DemandCore-TipOverSeeds-Final-20260811.log`；Physical NotEvaluated，未运行 5×6/Chaos/可见 PIE，用户地图继续排除 |
